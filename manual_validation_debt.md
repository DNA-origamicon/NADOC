# Manual Validation Debt — shift register

**What this is.** A queue of features that were shipped with the *accepted caveat*
that their **live gesture / visual appearance was never hand-checked in the running
app** — only verbatim-moved + unit-tested + smoke-gated. Sources: the "regression
caught by" / final caveat column of `main_js_extraction_log.md` (the `NOT
hand-exercised` / `NOT hand-driven` / `not visually confirmed` rows) and the USER
TODO column of `issues_fix_log.md`. These are real validation debt: a silent
behavior break would pass every automated gate we have.

**Intake (push, not just mining).** The initial PENDING queue was *mined* from those
two logs, but new items should be **pushed here directly** by the loop that creates
the debt: a carve-up extraction session (`main_js_carveup.md` step 6b) or a fix
session (`issues_ledger.md`) that ships a stateful/gesture region without hand-checking
its live gesture/visual appends a PENDING `MV-N` row itself — manual op + which
extractions/fixes it discharges + a fixture hint. Don't wait for a future re-mine.

**Why a shift register.** The debt is large and each item needs its own
context-dig (exact menu path, element ids, fixture, edge cases) to turn into
runnable manual steps. Doing them all at once is expensive and produces a wall of
instructions nobody executes. Instead we process **one item per loop**: a loop
pulls the head of the PENDING queue, generates an explicit USER TODO block for it,
and shifts the head off so the next loop starts on the next item.

Items are **deduped by manual operation, not by extraction number** — one live
gesture (e.g. the design-mode cluster Move/Rotate commit) validates several
extractions at once (#71/#78/#79/#80/#81). Each entry lists every extraction / fix
it discharges.

---

## The loop protocol (what each `/loop` iteration does)

1. **Read this file.** The next item is the one marked `▶ HEAD` in the PENDING
   queue below (top of the list).
2. **Dig the context** for that one item: grep the codebase for its real UI entry
   points — menu labels, button/element ids, keyboard shortcuts, the fixture that
   exercises it. **Verify the fixture actually exists** (`ls workspace/` /
   `Examples/`); if none does, say so in the block and either name the closest one
   or flag that a fixture must be built first.
3. **Write the USER TODO block** into the GENERATED section (append, newest at the
   bottom). Structure every block as: **SETUP** (which file to load, how to reach
   the feature) → **MAIN CASES** (numbered, the happy paths) → **EDGE CASES**
   (numbered, the things likely to silently break — boundary inputs, cancel/revert,
   re-edit, cross-layer effects) → **PASS CRITERIA** (what "it works" looks like,
   precise enough to catch a regression) → **WATCH FOR** (known-suspect behaviors,
   e.g. a flagged latent bug to confirm/deny).
4. **Shift the register:** move the processed item's PENDING line into the DONE-
   GENERATING list (it stays in the queue conceptually, just past the head), move
   the `▶ HEAD` marker to the new top PENDING item, and update the counts + the
   `## Next loop` pointer at the bottom.
5. **Commit:** `docs(manual-debt): generate manual ops for <item-id>`.
6. **When the user later runs a block and reports back,** move that item from
   GENERATED → **VALIDATED** (pass) or **REGRESSION FOUND** (fail, with a one-line
   note + open a fix). That transition can happen in any loop, out of band. When an
   item is VALIDATED, its runnable GENERATED block is **deleted** (the VALIDATED
   summary replaces it) to keep this file lean.

**Loop ends** when PENDING is empty (every item has a generated block). A second
pass over GENERATED → VALIDATED happens as the user actually executes them.

---

## Register state

- Total items: **46** (19 mined MV-1..MV-19 + MV-LNK + MV-RSZ + MV-SCAF + MV-EXT + MV-PRIM + MV-PRIM-PLACE + MV-CIRCLE + MV-PRIM-FACE + MV-OXDNA-SEED + MV-DELJOBS + MV-PHOTO-1 + MV-PHOTO-2 + MV-CONJ + MV-DELDEP + MV-ENGINES + MV-OXLIVE + MV-SOLVPREP + MV-EXT-ADD + MV-OXLIVEFIELD + MV-E2R + MV-E2R-RELAX + MV-DUPPIVOT + MV-DUPTAUT + MV-DUPMENU + MV-MRSEL + MV-FLEXSIM + MV-MRDNA-JOBS + MV-CANDO-JOBS, CanDo FEM panel pushed 2026-07-03)
- PENDING (no manual ops yet): **56** (12 mined + MV-SCAFMENU + MV-RSZ + MV-SCAF + MV-EXT + MV-PRIM + MV-PRIM-PLACE + MV-CIRCLE + MV-PRIM-FACE + MV-OXDNA-SEED + MV-FLEXSIM + MV-DELJOBS + MV-OXSEQ + MV-OXSPIN + MV-OXTRAJ + MV-OXRESUME + MV-EFIELD + MV-PHOTO-1 + MV-PHOTO-2 + MV-BENCH + MV-ANCHGLOW + MV-CONJ + MV-DELDEP + MV-MD-PROT + MV-ENGINES + MV-OXDNA-CONFIG + MV-OXLIVE + MV-OXREPS + MV-OXCHAIN + MV-SOLVPREP + MV-OXALIGN + MV-OXSEED-NAMD + MV-MDSPIN + MV-EXT-ADD + MV-OXLIVEFIELD + MV-OXLIVE-CUDA + MV-OXSTALE + MV-E2R + MV-E2R-RELAX + MV-DUPPIVOT + MV-DUPTAUT + MV-DUPMENU + MV-MRSEL + MV-MRDNA-JOBS + MV-MRDNA-SEED + MV-MRDNA-NAMD)  ·  MV-OXNATIVE ✅ VALIDATED 2026-06-20
- GENERATED (ready to run): **1** — MV-5
- VALIDATED: **9** — MV-1 (cluster Move/Rotate, 2026-06-07), MV-2 (overhang orientation, 2026-06-07), MV-3 (selection-rules UX, 2026-06-07), MV-4 (parts/groups multi-select box, 2026-06-07), MV-LNK (assembly linker completion, 2026-06-07), MV-6 (belt polymerize, 2026-06-07), MV-7 (coalesced part-refresh, 2026-06-07), MV-OXDNA (oxDNA relax/production/display/RMSD, 2026-06-14), MV-OXREPS (atomistic display 2026-06-21 + trajectory scrub/play 2026-06-22; surface+flex-map lightly-owed)
- REGRESSION FOUND: **0** (MV-1 + MV-2 + MV-3 + MV-LNK enhancements/bugs found during validation were shipped same session)

---

## PENDING queue (ordered; `▶ HEAD` = next loop processes this)

Priority = (user-facing value) × (risk a silent break slips every automated gate).
Core editing + default-selection UX first; niche/visual last.

| # | id | feature / manual operation | discharges | fixture hint | why deferred |
|---|----|----------------------------|-----------|--------------|--------------|
| | **MV-MRDNA-PANEL** | **mrDNA / ARBD / CUDA rows in Help ▸ MD Engines (in-app)** — open **Help ▸ MD Engines** and confirm three new rows render with correct red/green dots: **(a) mrDNA** (red here) → **Install (CPU)** button; clicking it streams the real `setup-mrdna.sh` (git clone + patch + editable install — GPU-independent, works on this box) with a progress bar/log, then the row flips **green** on success (mrDNA can build models but still can't SIMULATE without ARBD). **(b) ARBD** (red here) → **Download…** → popup shows the **ARBD download link** (KS UIUC license-gated portal), a "needs CUDA toolkit (nvcc)" prereq, and a **"Already downloaded it?"** block with a **Browse…** button that opens a **folder navigator** starting in the user's Downloads folder (the **Windows** one on WSL, `/mnt/c/Users/…/Downloads`, resolved by `fs_browse.default_downloads_dir`), files sorted newest-first with `arbd*.tar.*` highlighted; navigate + click the ARBD tarball → path fills the box → **Check & install** verifies it's source (CMakeLists/src), unpacks to `~/arbd-src`, streams **cmake + make**, then on success pops the **"one step left"** modal offering **three** finishes: **Finish install (no password)** (copies the built Linux binary to `~/.local/bin/arbd`), **Run it for me (password)** (backend runs `sudo -S make install` for terminal-averse users; password used once, never logged), or Copy the sudo line. **WSL-aware:** the panel shows a "Running in WSL — engines install on the Linux side" banner, and when a Linux `arbd` is already built but not on PATH (the classic "sudo make install wasn't finished" snag — detected via `mrdna_bridge.find_arbd_build`) the ARBD row flips to a green "already built — finish below" block with the no-password + password options. **(c) CUDA toolkit** (red here) → **How to install…** → popup with the **NVIDIA CUDA link** + `apt-get` commands + Copy. Verify the manual_step Copy button copies the sudo line and "Done" re-checks status. | (new, 2026-07-02 — this session) | this machine for (a) mrDNA end-to-end; a **CUDA GPU box (RTX 3080 Ti)** for the real ARBD compile + `sudo make install` detection | live API verified (`/api/engines/status` shows all 3 rows with right method/prereqs/links; `/api/engines/arbd/scan-download` finds a fabricated tarball then empties on cleanup); backend pinned (`test_engines.py` +9, `test_engine_install.py` +2, `test_engine_artifact.py` +9 real fabricated ARBD tarballs, `test_engines_ws.py` +4 incl. arbd build→manual_step + mrdna sim round-trips); frontend pinned (`md_engines_logic.test.js`, `md_engines.test.js` rows/KS-link/Browse-picker, `file_picker.test.js` nav+format, `test_fs_browse.py` sort/highlight/Downloads-default). The old fixed-folder `scan-download` check was **replaced** by the folder navigator (`fs_browse.py` + `GET /engines/browse` + `ui/file_picker.js`) — this fixes WSL, where the real Downloads is on the Windows side, not `~/Downloads`. **The no-password finish was LIVE-verified end-to-end 2026-07-02**: on this WSL box ARBD built to `~/arbd-src/build/arbd` but wasn't on PATH; `install_arbd_binary` copied it to `~/.local/bin/arbd` and the panel flipped ARBD to green (`find_arbd()` resolves it, dir on PATH). **NOT hand-driven:** the `sudo -S make install` password path (would need entering the real password) — its guard paths (empty pw / not-built) are pinned; and the real ARBD **compile** still needs a CUDA box. **NOT verified here:** the real ARBD **compile + `sudo make install`** (this box has **no CUDA/nvcc**, cannot build ARBD) — the build/streaming path is only exercised via stubbed cmake/make; do the genuine compile on the 3080 Ti. The mrDNA one-click install + all popups/links/scan ARE reproducible here. | 
| | **MV-MRDNA-JOBS** | **mrDNA relaxation panel — run + monitor + CG display (in-app)** — open the **Dynamics** tab; the new **mrDNA** collapsible sits under the **oxDNA** panel. Expand it: the availability badge should read **"mrDNA + ARBD ready (GPU)"** (green) on this box (both installed; live-probed this session). Load a design (e.g. the loaded 26-helix Bundle, or `Examples/26hb_platform_v3.nadoc`), then click **▶ Run mrDNA relaxation** (single button — no relax/production split; mrDNA's coarse stage IS the relaxation). Watch the **jobs list** row + **detail block**: status ticks **queued → running** (progress bar fills, ETA counts down) **→ completed** (~15–30 s for a small design; a real 2000-step run on the loaded Bundle completed in **17 s → 1066 CG beads** this session). On completion the two display toggles enable: tick **"mrDNA display (deform model)"** → the NADOC model bends to the relaxed coarse positions (restores on untick); tick **"CG beads (5 bp/bead cloud)"** → the view switches to a **standalone CG representation**: the ~1066 coarse beads appear as a sphere cloud **with their bond connections drawn** (backbone chain + crossovers, ~1040 line segments this session) AND **the native NADOC model hides** (whatever rep it was in — only the CG beads+sticks show); untick → the native model returns. Confirm the **Advanced** card (coarse steps / output period / CUDA device) applies, **Stop** cancels a running job (kills the ARBD child), **Delete** removes a finished job (and drops its display), and **Show jobs for all designs** filters the list. Leaving the Dynamics tab clears the displays. | (new, 2026-07-02 — this session) | any design with helices (`Examples/26hb_platform_v3.nadoc`); this box has mrDNA + ARBD + GPU | **Backend fully live-validated this session** (real GPU: create→queued→running→completed, 1066 beads / 10400 relaxed positions extracted, display + beads endpoints return real data, delete cleans up). Backend pinned `tests/test_mrdna_jobs.py` (13: job round-trip, availability, progress, reconcile restart-recovery, HTTP create/list/stop/delete + display/beads); frontend pinned `mrdna_display.test.js` + `mrdna_jobs_panel.test.js` (17: toFemUpdates/beadsToPoints/controller deform+beads independence, formatProgress/jobDisplayName/detailStatusText/coarseStageChip). Full backend suite 3703 pass, frontend 1962 pass, `just smoke` render gate green. **NOT hand-driven:** the in-browser Run-button click → live jobs-list/progress tick → the visual model-deform + CG-bead-cloud toggles are WebGL-gesture + Tier-3 visual, human-eye only. | 
| | **MV-CANDO-JOBS** | **CanDo FEM shape-prediction panel — run + monitor (in-app)** — open the **Dynamics** tab; the new **CanDo FEM** collapsible sits under the **mrDNA** panel. Load a design with loop/skip marks (`workspace/6hb_curved.nadoc` = ±9 ins/del, analytic ~88° bend). Expand it → click **▶ Coarse** (linear preview). Watch the **jobs list** row + **detail block**: status ticks **queued → running** (progress bar fills, ETA counts down) **→ completed**; the detail summary reads **"Coarse (linear) solve · N bp nodes · RMSF a–b nm"**. On this box a 1200-node coarse job = ~4 s with RMSF off, ~40 s with RMSF on (the 200-mode NMA dominates — the **Advanced** card's "Compute RMSF" checkbox toggles it; "Load steps" applies to Fine only). Click **⤴ Fine (nonlinear)** → a slower (~1 min) background job with the corotational solve. Confirm: **Stop** cancels a running job (best-effort — the current scipy solve finishes then discards), **Delete** removes a finished job, **Show jobs for all designs** filters the list, and leaving the Dynamics tab is clean. The **"Predicted shape (deform model)"** toggle is **disabled** (wired by Item 2 — not yet functional). | (new, 2026-07-03 — this session; Phase-5 Item 1) | `workspace/6hb_curved.nadoc` (loop/skip bend) or any routed design with helices | **Backend fully live-validated this session over HTTP vs the running server**: create→queued→running→completed, status/progress/ETA correct, `display` (2364 positions) + `rmsf` (1200 nodes, 0.30–0.68 nm) endpoints return real data, list/delete clean up. Backend pinned `tests/test_cando_job.py` (5: persistence round-trip, stage-name-tracks-solver, linear job completes+caches, progress+reconcile, stray-running stop). Frontend pinned `cando_jobs_panel.test.js` (15: formatProgress/jobDisplayName/candoJobIsActive/solverLabel/detailStatusText/stageChip/formatSummary). Full frontend 2032 pass; backend 3831 pass (2 real-sim failures were CPU-contention flakes, pass in isolation). **NOT hand-driven:** the in-browser Coarse/Fine-button click → live jobs-list/progress tick → detail-summary render + Stop/Delete gestures are WebGL/DOM-gesture, human-eye only. |
| | **MV-MRDNA-NAMD** | **mrDNA fine-stage → NAMD managed-MD seed + panel button (in-app)** — the mrDNA panel (Dynamics tab) now has a **"Use as NAMD seed"** button under Stop/Delete, enabled ONLY once a **fine-stage** relaxation completes (`seedReady`: `status==completed && fine_steps>0`). **To exercise:** run a mrDNA job with **fine steps > 0** to completion (coarse-only leaves the button greyed) → click **Use as NAMD seed** → status line reads "Building NAMD seed + solvating…" → a new **managed MD job** appears in the **Molecular Dynamics** panel below, tagged **"mrDNA seeded"** (badge + "Seeded from mrDNA job …" tooltip), seeded from the job's fine-stage relaxed structure (mirrors the oxDNA "Use as NAMD seed" path). Start that MD job and confirm its minimization starts from the CG-relaxed structure (fewer startup clashes than ideal-B-DNA). **Also:** a coarse-only job's button stays disabled; an unknown/невalid job 400s fast. | (new, 2026-07-02 — this session; NAMD parity for the mrDNA→MD seed) | a **fine-stage** completed mrDNA job (`5edf26369a37` = `6hb_sim_v2`, 200k fine, is one) + **NAMD installed** (the MD create route 400s without it) + GROMACS for solvation | backend pinned: `assert_mrdna_namd_seed_available` gating (unknown/coarse-only/valid) + `build_namd_seed_from_mrdna` real-job integration (recentered atomistic model, `test_mrdna_md_seed.py`); provenance (`seed_mrdna_job_id`) round-trips `md_job.py`; route branches mirror the oxDNA seed path (create + refit + prep worker). Frontend pinned: `seedReady` (`mrdna_jobs_panel.test.js`), `seededBadge` mrDNA (`md_jobs_panel.test.js`); boot-clean (`smoke.spec.js` green). **NOT hand-driven:** the in-browser button click → status → MD-job-appears-with-badge gesture + the actual NAMD run from the mrDNA seed — needs NAMD+GROMACS installed + a real fine job; human-eye only. |
| | **MV-MRDNA-SEED** | **mrDNA fine-stage relaxation → GROMACS MD seed (end-to-end)** — the new `POST /api/design/export/gromacs-mrdna-start` route seeds a GROMACS package from a COMPLETED **fine-stage** mrDNA/ARBD job's relaxed structure (via `nuc_pos_override_from_arbd_strands`, crossover nucleotides INCLUDED at the ~0.3-0.5 nm CG gap), sibling of the oxDNA `gromacs-cg-start`. **To exercise:** on a CUDA box, create a mrDNA job with **`fine_steps > 0`** on a routed sequenced design (e.g. `Examples/26hb_platform_v3.nadoc`), let it run coarse→fine→completed, then POST the route with that `mrdna_job_id` → poll `gromacs-status` → download `gromacs-result`. **PASS:** (a) the ZIP builds without error; (b) unzip + run the bundled GROMACS EM → it converges in **~hundreds** of steps (not ~12k) with **no ~10¹² kJ/mol LJ spike at step 0** — the crossover O5'/O1P clashes are pre-resolved by the CG gap (the whole payoff vs the ideal-B-DNA path); (c) compare step count against `gromacs-start` (ideal) on the same design to confirm the reduction. **Gating (verify the 4xx surfaces, no frontend yet — drive with curl):** unknown job id → 404; a **coarse-only** (`fine_steps=0`) job → 409 "coarse-only"; a not-yet-completed job → 409; a completed-but-missing-output job → 409. **NOTE:** seeds from the JOB's design **snapshot**, not the live active design — confirm editing the design after the run doesn't change the seeded structure. | (new, 2026-07-02 — this session; route established, particulars TBD: coarse-vs-fine default, ssDNA/overhang handling, NAMD parity) | a CUDA box (RTX 3080 Ti) + mrDNA + ARBD + GROMACS; a **fine-stage** completed mrDNA job (none on disk yet — the one existing job `a9f7cbd6e785` is coarse-only) on `Examples/26hb_platform_v3.nadoc` | gating + wiring pinned `tests/test_mrdna_md_seed.py` (7 fast: `resolve_md_seed_inputs` rejects non-completed / coarse-only / missing-snapshot / missing-fine-output, passes+returns fine inputs; route 404 + coarse-only 409) + 1 skip-guarded integration (`build_md_seed_override` on a real fine job → non-empty, finite, crossover keys present) that **skips until a fine-stage job exists**. Full backend suite 3749 pass. **NOT verified here:** the whole GPU→ARBD-fine→override→GROMACS-EM end-to-end and the step-reduction payoff — needs a CUDA box + a real fine run + GROMACS; the override extraction itself (`nuc_pos_override_from_arbd_strands`) is separately unit-pinned in `test_mrdna_pipeline.py` but never run through `build_gromacs_package` on real fine output. **ssDNA/overhang handling added 2026-07-02** (`nuc_pos_override_ssdna_from_arbd` + `_ssdna_runs`, merged in `build_md_seed_override`): mrDNA's relaxed `NAS` beads now seed overhangs (spline / root-translate / leave-ideal, do-no-harm selector by clearance to the ds body) so overhangs follow the relaxed body instead of seeding detached (restores the ss/ds junction bond a ds-only override leaves broken — validated on the user's real 200k fine job `5edf` + OH6hb; the clash oracle caught + fixed a coincident-atom LJ=2e37 bug). **Extra live check when a CUDA fine run + GROMACS are available:** unzip the mrdna-seeded package and confirm EM step-0 has no LJ spike from *overhang* atoms specifically (esp. dense bundles with long overhangs — a KNOWN unsolved case where even ds-only clashes; may need soft-core/POSRE-warmup EM). Pins: `tests/test_mrdna_md_seed.py` (`_ssdna_runs` + skip-guarded junction/clash oracle). |
| | **MV-SCAFMENU** | **Unified scaffold context menu (in-app)** — load a design with a scaffold that has both dsDNA and ssDNA (unpaired) regions (`Examples/mini_hinge.nadoc` has unpaired scaffold runs; or any `.nadoc` with a scaffold). **Right-click the scaffold in several places** and confirm you get the **SAME styled menu** each time (no more raw-HTML buttons on the cone): (a) on a **terminus cone**, (b) on a **paired (dsDNA) bead**, (c) on an **unpaired (ssDNA) bead**. The menu must list: **Nick here · Assign sequence… · [flexible section] · [loop/skip section] · Representation ▸ · Delete strand**. In the flexible section: on a **paired** bead "Mark flexible segment" is **greyed/disabled** (hover tooltip "Only unpaired… ssDNA"); on an **unpaired** bead it's **enabled** → click it → the contiguous run marks flexible (bows into an arc). Once marked, right-clicking that run shows **Unmark + Relax this / Relax all**; "Clear all flexible segments" appears whenever any marks exist. Exercise each item end-to-end: **Nick** splits the scaffold; **Assign sequence…** opens the scaffold modal; **Add loop/Add skip** insert; **Representation ▸** repaints the region; **Delete strand** removes it. Confirm **overhang tips** on the scaffold still open the **overhang** menu (not the scaffold menu). | (new, 2026-07-01 — this session) | `Examples/mini_hinge.nadoc` (unpaired scaffold runs) or any scaffold design | build + 1921 vitest pass; smoke render+teardown gates green; menu built with the shared `_menuBase`/`_menuItem` primitives (same as sibling menus). **NOT hand-driven:** the 3D right-click on cone-vs-paired-vs-unpaired scaffold pixels + the greyed-Mark tooltip + each action's visual result are WebGL-gesture, human-eye only (LESSONS H7) |
| | **MV-MRSEL** | **Selection-driven Move/Rotate + revamped panel (in-app)** — load a cluster-bearing design (`Examples/26hb_platform_v3.nadoc`). **(a) Auto-open:** turn on the **cluster selection filter** and click a cluster in 3D → the **Move/Rotate tool opens automatically** on that cluster (gizmo appears, right-sidebar panel shows). Same when clicking a row in the **Movable Clusters** sidebar. **(b) Re-target:** with the tool open, click a *different* cluster → the gizmo re-attaches to it (fields/pivot repopulate). **(c) Auto-close:** click empty space (deselect) → the tool auto-commits pending transforms and closes. **(d) Sticky manual open:** open the tool via the toolbar/`T` hotkey, then deselect → it must STAY open (only auto-opened sessions auto-close). **(e) ±45° buttons:** click **+45** on X → the cluster rotates 45° about world-X; click again → 90°; **−45** reverses; combine across X/Y/Z. **(f) Reset:** click **Reset** → cluster returns to identity pose; Cancel restores prior. **(g) Snap 45°:** tick **Snap rotation to 45°** → drag a rotation-gizmo ring → it snaps to 45° increments; untick → free drag. **(h) Restyle:** the panel spacing/fonts/buttons match recent panels. **(i) DUPLEX pivot (bugfix 2026-07-01):** open the tool on a directly-connected overhang **duplex** cluster (`workspace/2x2_OH_test.nadoc` → `Duplex 1`) → click **+45** — the duplex must **spin about its rotation-point, NOT teleport to a distant location**; **Cancel** must restore it. (Root cause was the number boxes reading the stored translation while the gizmo pivot was centroid-rebased; fixed by reading fields from the gizmo's pending transform + marking `setTransform` dirty. e2e-pinned by `duplex_gizmo_pivot.spec.js`: gizmo lands ON the duplex and +45 rotates cleanly about it for both centroid and root pivots, no teleport.) Also verify selecting a specific **Rotate about <OH> root** in the Pivot dropdown then +45 still spins about that root with no jump. | (new, 2026-07-01 — this session) | `Examples/26hb_platform_v3.nadoc` (multi-cluster) + `workspace/2x2_OH_test.nadoc` (`Duplex 1`, for the pivot bugfix) | frontend pinned (`rotation_math.test.js` stepEulerDeg compose/no-op; `move_rotate_panel.test.js` +4 step/reset/snap; `translate_rotate_tool.test.js` +8 decideSelectionAction + 2 fields-from-pending) + `just smoke` green (console-error + teardown). **NOT hand-driven:** the 3D cluster-pick auto-open/re-target/auto-close gestures, the gizmo-ring snap-drag, the panel restyle appearance, AND the duplex +45 spin-not-teleport / cancel-restores are WebGL-gesture + Tier-3 visual, human-eye only |
| | **MV-CONNLINK** | **Duplex / ds-linker as a MOVABLE LINK in a constrained drag (in-app)** — load `workspace/2x2_OH_test.nadoc` (direct duplex). Select the **non-parent** regular part (the one that is NOT the duplex cluster's parent — Cluster 2 for this fixture), Move/Rotate → "Constrained (tethers)" → drag it. The **duplex must visibly SWING** to follow (hinging between the two parts) while the OTHER part stays put; the dragged part is limited by the duplex's reach (not a rigid 0.67 nm leash to a frozen duplex). Release commits **both** the part and the duplex pose (undo reverts both together). **Stability watch:** the ~0.67 nm near-rigid bonds may oscillate/jitter (flagged short-tether PBD risk) — if so, note it. Then load `workspace/3x6_hinge_interior_linker.nadoc` (ds linker) → "Constrained (tethers)" → drag a part: the ds rod is now a **rigid strut** — you can't push the parts closer NOR pull past the rod length (bilateral), and it pivots at both ends. Dragging the duplex's PARENT part instead → the duplex rides it rigidly (static tether to the other part), no independent swing. | (new, 2026-07-01 — this session) | `workspace/2x2_OH_test.nadoc` (duplex swing) + `workspace/3x6_hinge_interior_linker.nadoc` (ds rigid strut) | backend pinned (`test_connection_tethers.py`: parent-static-vs-non-parent-movable-link split, 2 bonds + dragged flags, ds `rigid=True`); frontend pinned (`flexible_relax_solver.test.js` chain follow-anchored; `cluster_gizmo.test.js` `ssTetherViolated` bilateral); Playwright probe confirmed endpoint→payload→arm clean. **NOT hand-driven:** the live WebGL drag — duplex visibly swinging, no jitter, ds rigid-strut feel, dual A+duplex commit — is human-eye only |
| | **MV-CONNTETHER** | **Connected overhangs constrain a regular cluster's move ("Constrained (tethers)") (in-app)** — load a design where a regular part cluster is joined to another by an applied **direct duplex** (`workspace/2x2_OH_test.nadoc`) OR a **ds/ss linker** (`workspace/3x6_hinge_interior_linker.nadoc`). Select a regular cluster → Move/Rotate opens. In the Pivot dropdown confirm the option is now labeled **"Constrained (tethers)"** and appears even with NO ssDNA flexible-segment marks (connection-only clusters). Pick it → drag the gizmo: the cluster must move **freely until the connection goes taut**, then be held — a direct duplex at ~0.67 nm (near-rigid: pivots/wobbles about the junction, can't pull apart), a ds linker at its duplex length (~9.7 nm for 30 bp), an ss linker at n·0.59 nm. Confirm live-responsive (updates each frame), release commits once, undo reverts. Combine with ssDNA marks on the same cluster → BOTH tether sets constrain simultaneously. **Stability watch:** the ~0.67 nm direct-duplex tether is near-rigid — if the drag oscillates/jitters, that's the flagged short-tether PBD risk (same as MV-DUPTAUT). | (new, 2026-07-01 — this session) | `workspace/2x2_OH_test.nadoc` (direct duplex) + `workspace/3x6_hinge_interior_linker.nadoc` (ds linker); no ss-linker fixture handy | backend pinned (`test_connection_tethers.py` 3: duplex both-parents 0.67 nm mirror, ds-linker=duplex-length, availability) on real fixtures; frontend pinned (`move_rotate_panel.test.js`: async merged payload + connection-only availability); Playwright probe confirmed endpoint→dropdown→select clean. **NOT hand-driven:** the WebGL free-until-taut DRAG feel + short-tether stability + ss-linker path are human-eye only |
| | **MV-DUPMENU** | **Duplex overhang right-click routes orientation to the gizmo; standalone keeps the panel (in-app)** — load `workspace/2x2_OH_test.nadoc`. **Right-click a duplex-backed overhang** (OH1/OH2, part of `Duplex 1`): the menu top items must read **"Move / Rotate duplex"** + **"Reset Orientation"** (NOT "Edit Orientation"). Click "Move / Rotate duplex" → the Move/Rotate tool activates on the duplex cluster (gizmo appears on it). Back out, click **"Reset Orientation"** → the duplex pose returns to identity (the duplex snaps back to its un-rotated seated position) and undo reverts it. Then **right-click a STANDALONE overhang** (extrude a fresh overhang that isn't connected): its menu must still show **"Edit Orientation"** (opens the orientation panel) — the panel is retained for non-duplex overhangs. | (new, 2026-07-01 — this session) | `workspace/2x2_OH_test.nadoc` (duplex) + a freshly-extruded unconnected overhang (standalone) | frontend pinned (`overhang_orientation_menu.test.js` +4: duplex swaps to gizmo items, activates cluster, cluster-reset not whole-overhang reset, standalone keeps panel; `design_queries.test.js` +1 `duplexClusterForOverhang`) + boot-clean (both duplex e2e green). **NOT hand-driven:** the 3D right-click gesture + the gizmo-appears + reset-snaps-back visuals are human-eye only |
| | **MV-DUPTAUT** | **Overhang-duplex "Constrained (taut bonds)" free-until-taut drag (in-app)** — load a design with an applied overhang connection so a duplex cluster exists (`workspace/2x2_OH_test.nadoc` → `Duplex 1`). Enter **Move/Rotate**, select the duplex cluster, set the pivot dropdown to **"Constrained (taut bonds)"** (a toast confirms: "drag the duplex — connection bonds won't overstretch"). Drag the gizmo: the duplex should move **freely until a connection bond goes taut** (~0.67 nm), then be held — like the ssDNA-constrained flex drag. Confirm: (a) small drags move freely; (b) pulling further stops when a bond is taut (doesn't stretch a bond indefinitely); (c) **CRITICAL stability check** — the motion must **not oscillate/jitter/explode** (the PBD projector is tuned for multi-nm ssDNA; these ~0.67 nm bonds are near-rigid — if it oscillates, that's the flagged risk and needs the analytic 1-DOF rotate-about-root→root-axis fast path instead); (d) release commits once, undo reverts. | (new, 2026-07-01 — this session) | `workspace/2x2_OH_test.nadoc` (has `Duplex 1`) | backend pinned (`test_duplex_cluster_tethers.py` 3: two bond tethers, anchors resolve to real beads, moving-on-duplex-helix/fixed-off) + live-verified endpoint; frontend pinned (`move_rotate_panel.test.js` +2 dup:taut option/handler + fallback) + **e2e** `duplex_taut_constraint.spec.js` (endpoint→client→key-format→geometry resolution + mode arms). **NOT hand-driven:** the gizmo-ring drag + the free-until-taut FEEL + the short-tether stability are WebGL-gesture + empirical, human-eye only |
| | **MV-DUPPIVOT** | **Overhang-duplex cluster rotation-point dropdown + gizmo spin (in-app)** — load a design with an applied overhang connection so a duplex cluster exists (`workspace/2x2_OH_test.nadoc` → `Duplex 1`). In the cluster sidebar confirm the duplex cluster shows the **⛓ tag**. Enter **Move/Rotate**, select the duplex cluster. In the pivot dropdown confirm three duplex options: **"Rotate about <OH1> root"**, **"Rotate about <OH2> root"**, **Centroid**. Pick a root → drag the gizmo's rotation ring: the duplex must **spin about THAT overhang's root bead** (not the centroid), and the rest of the structure stays put. Switch to the other root / centroid → the pivot point visibly moves. Confirm the geometry does **not jump** at the instant of selecting a new pivot (only the pivot marker moves). Undo reverts the spin. | (new, 2026-07-01 — this session) | `workspace/2x2_OH_test.nadoc` (has `Duplex 1`); any applied overhang connection works | backend pinned (`test_duplex_cluster_rotation_point.py` 3: points=2 roots+centroid, pivot-move geometry-neutral, root≠centroid spin) + **live-verified** (GET points, POST OH1 root → beads byte-identical, validation passed); frontend pinned (`move_rotate_panel.test.js` +3: dup options list, dup:root→setClusterRotationPoint, centroid routes to duplex path). **NOT hand-driven:** the dropdown-select + gizmo-ring-drag spin-about-root gesture on the WebGL canvas + the "pivot doesn't jump" visual are human-eye only |
| | **MV-E2R** | **End-to-root Apply regenerates B as A's RC binder (in-app)** — load a design with two extruded overhangs whose B-side overhang is a TIP on a bundle-anchored staple (e.g. a hinge with overhangs, or build one: route a bundle → extrude two overhangs on different leaves). Open the right-sidebar **Overhang Connections** section, pick overhang A in slot A + overhang B in slot B, set the connection-type picker to **End-to-Root**, give A a sequence (per-side **Gen** or type one), click **Connect** then **Apply** (Apply is now ENABLED for end-to-root). Confirm: (a) overhang **A's free end becomes double-stranded** in 3D — a new antiparallel binder domain appears along A's overhang helix; (b) in the **caDNAno 2D editor**, overhang B's staple now extends as the antiparallel complement on A's helix row (same bp columns as A); (c) overhang **B disappears from the Overhangs list** (it's consumed into the binder); (d) the binder reads the reverse complement of A; (e) **Relax is now ENABLED** for end-to-root (see MV-E2R-RELAX). Save → reload (.nadoc) → the binder persists and **no phantom B overhang reappears**. | (new, 2026-06-29 — this session) | a design with two extruded overhangs, B on a bundle-anchored staple (hinge w/ overhangs, or build one); `assign_scaffold_sequence` for the RC check | backend fully pinned (`test_headless_build.py::test_apply_end_to_root_regenerates_b_as_rc_binder_of_a` real routed 6hb + round-trip guard red-test; `test_connection_versions.py::test_apply_end_to_root_splices_rc_binder_into_b` HTTP; oracle `assert_end_to_root_binder`); panel re-enable pinned via existing `versions`/`bindrelax` vitest. **NOT hand-driven:** the in-browser pick→Connect→Apply gesture + the 3D duplex + caDNAno-row representation + B-leaves-list visual are human-eye only |
| | **MV-E2R-RELAX** | **End-to-root Relax = kinematic solve closes the splice junction (in-app)** — with an APPLIED end-to-root connection (from MV-E2R) where A's part and B's part are in DIFFERENT clusters joined by a ClusterJoint (the common 1-DOF hinge case), click **Relax** in the Overhang Connections section. Confirm: (a) the stretched backbone bond between B's root and the binder (A's free-tip end) **closes to ~one backbone-bond length** — the parts swing together; (b) A's overhang duplex **visibly re-orients** (swings about A's root) AND the jointed cluster rotates; (c) the move is reversible. Edge: no joint between the two clusters → B's cluster **rigid-translates** to close it; both overhangs on the SAME rigid body → only the duplex swings (no cluster move), gap shrinks but may not fully close. | (new, 2026-06-29 — this session) | applied end-to-root with A/B in separate clusters + a joint (hinge); reuse the MV-E2R fixture + add a joint | backend fully pinned (`tests/test_end_to_root_relax.py`: translate→exact 0.67 nm, 1-joint→0.668 nm from 5.0, swing-persists-as-rotation, same-body swing-only, endpoint, 404); frontend wiring pinned via `bindrelax` vitest. **NOT hand-driven:** the live click→3D swing+joint motion + chord-closes visual are human-eye only |
| | **MV-EXT-ADD** | **Additive empty-space extrude (no longer wipes the part)** — open a populated design (`Examples/6hb_test.nadoc`), then **right-click empty 3D space → Extrude** (or **Tools → Extrude**). Confirm: **no "replaces the current part" confirm dialog appears** (it's gone). Pick a plane in the dropdown, select empty lattice cells **away from the existing helices**, enter a length, **Extrude**. The existing structure must remain intact and a **new disconnected bundle** appears in the SAME design (helix count grows, undo reverts just the new bundle). Repeat several times → each adds another bundle, never resets. Picking cells that **overlap existing DNA** should still red-flag + block (conflict guard unchanged). On a **truly empty** workspace, Extrude still starts a fresh bundle as before. | (new, 2026-06-20 — this session) | `Examples/6hb_test.nadoc` (populated) + an empty new part for the fresh-bundle path | frontend routing `freshBundle = newBundle && helices===0` → `createBundle` else `addBundleSegment`; backend append path already pinned (`test_extrude_segment_appends_fresh_disconnected_helices`) + verified live via curl (2→4 helices). **NOT hand-driven:** the canvas right-click → cell-pick → build gesture + the "existing structure intact" visual are human-eye only |
| | **MV-OXCHAIN** | **oxDNA chained field runs + full-lineage trajectory view** — load a sequenced design (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics ▸ oxDNA ▸ **▶ Relax**, let it finish. **(a) Chain runs:** select the completed relax job → set an E-field direction + ≥1 anchor → **Start Production** (creates field run 1). When it completes, **select field run 1 in the list** → change the E-field direction → **Start Production AGAIN** — this should be ALLOWED (previously refused) and create field run 2 **seeded from field run 1's end state** (a grandchild). Confirm the job list **nests** them with increasing indent (relax → Field 1 → Field 2) and each is selectable. **(b) Full-lineage trajectory:** with Field 2 selected, toggle **View trajectory** → the slider/playback should span the WHOLE lineage (relax stages → field 1 → field 2), starting on the un-deformed design and ending on field 2's last frame, with **numbered tick markers** at each run boundary ("→ field 1", "→ field 2"). **(c) Cascade delete:** delete the relax job → confirm dialog warns it removes ALL dependent runs; deleting removes the whole chain. | (new, 2026-06-20 — this session) | `workspace/6hb_OxDNA_test.nadoc`; needs ≥1 completed relax + 2 chained field runs | guard-lift (`/field` + `/run` accept a child parent), `_lineage_jobs` walk + numbered boundary markers in `composite_trajectory`, recursive delete cascade, and `flattenJobTree`/`descendantIds` nesting are route/unit-pinned (`test_oxdna_relaxation.py` +2: lineage-walk + cascade; `test_headless_oxdna_build.py` chained grandchild; `oxdna_jobs_panel.test.js` flattenJobTree/descendantIds/deleteConfirm). NOT hand-driven: the live multi-run GPU gesture + on-screen nesting/scrub/marker visual + cascade dialog — needs real chained GPU runs; human-eye only |
| | **MV-SOLVPREP** | **Live NAMD-seed solvation progress bar + ETA + hang detection** — load a sequenced design (`workspace/6hb_OxDNA_test.nadoc`), Dynamics ▸ oxDNA ▸ **▶ Relax** to completion → select the relax job → **"Use as NAMD seed"** (or the MD **Relax** button on any sequenced design). Instead of an indeterminate spinner, the Molecular Dynamics job detail should show a **determinate bar that fills smoothly** while preparing: a phase message that steps through "Reconstructing relaxed atomic model… → Building DNA topology… → Adding explicit water (GROMACS)… → Placing ions / Building solvated topology (PSF/PDB)… → Building elastic-network restraints… → Writing simulation configs", a **"~Xm Ys left" ETA** that counts down, and a **"Step i of N · <label>"** line. The POST returns immediately (job appears in `preparing` right away — no frozen UI). Confirm: (a) a **bad/empty oxDNA seed** still rejects fast with a toast (not a hung job); (b) an **unsequenced** design fails the job with the "no sequence assigned" reason; (c) if a phase overruns its estimate, an **amber "…may be stalled" warning** appears under the bar; (d) on success the job flips to `queued` (autostart runs it); on prep failure it flips to `failed` with the error text. | (new, 2026-06-20 — this session) | sequenced design + a completed oxDNA relax to seed from (`workspace/6hb_OxDNA_test.nadoc`); needs GROMACS+NAMD installed | tracker fraction/ETA/warning/fail + sidecar unit-pinned (`test_md_prep_progress.py` 17); progress threaded through solvation w/ gmx stubbed + `_run_watched` hung-proc kill pinned (`test_md_prep_wiring.py` 5); bad-seed fast-400 pinned (`test_md_create_with_bad_oxdna_seed_returns_400`). **NOT hand-driven**: the live browser gesture — the bar filling/phase-stepping/ETA-ticking, the stall warning, and the real GROMACS hard-timeout kill — needs GROMACS+NAMD + a real multi-minute solvation to watch; human-eye only. **ALSO (2026-06-20 follow-up):** the seeded job must **appear in the MD panel even when it was already open** (the `nadoc:md-job-created` refresh path); the **MD-panel cards** should render like oxDNA (run controls on top; collapsible **Jobs** card + **Advanced** card + the **Benchmark** card; expand/collapse each); and an **orphaned `preparing`** job (kill or reload the server mid-prep) should flip to **failed** with "Preparation was interrupted…" rather than hang. VoltronCore (14,774 nt) is a good stress case — its prep may legitimately fail on memory; both its oxDNA runs (incl. the production run) are confirmed seedable. |
| | **MV-OXEQUIL** | **oxDNA equil-readiness: auto-escalate instead of crashing at equil** — load a large strained sequenced design (`workspace/VoltronCore.nadoc`, 14,774 nt; enable a hard surface + a few anchors to reproduce the original strain) → Dynamics ▸ oxDNA ▸ **▶ Relax**. Previously the equil stage aborted at config load (oxDNA FENE: "Distance between bonded neighbors … exceeds acceptable values"). Now MD-relax that leaves a backbone bond past the FENE cliff should **auto-escalate the relax (longer + dt 0.001 + stronger force cap) up to 3× then either complete via the capped equil or FAIL with a readable FENE diagnostic** — NOT a cryptic rc=1. Watch the job tick: relax may re-run (stage rewinds), then equil completes (or a clear "could not bring bonds within FENE range after 3 attempts" error). Confirm a normal small design still completes in one pass (no spurious retries). | (new, 2026-06-19 — this session) | `workspace/VoltronCore.nadoc` (the crashing job was `175dcf0ba36b`); small control = `workspace/6hb_OxDNA_test.nadoc` | site-based FENE metric calibrated to oxDNA's own abort report, capped-equil + escalate-retry unit-pinned (`test_escalate_md_relax_spec_schedule`, `backbone_fene_stretch`, `run_oxdna_health_check.fene_safe`, `test_runner_retries_then_fails_when_not_equil_ready`) + real-binary lifecycle green; the **live multi-minute GPU re-run on VoltronCore (auto-escalate→complete vs the old equil crash) is human-eye only** |
| | **MV-MDSPIN** | **MD-job running indicators (spinners + Health card)** — load a sequenced design, start an MD (NAMD) job. Confirm: a **spinning circle** replaces the status dot on the **running job's list row** (and survives a panel reload while running); a **spinner sits next to the current stage** in the Stage timeline (replacing the `…`); the metrics now live in a **collapsible "Health" card** (chevron toggles open/closed) whose **header shows a spinner** while the job is active but no metric exists yet; the card body shows **"Preparing simulation…" / "Waiting for first metrics…" with a spinner** until NAMD emits its first ENERGY line, then per-card values that are still `—` on a running job show a **small spinner** (not a dash); and the progress line carries a spinner while active (esp. during "Preparing package"). Spinners must NOT visibly restart/stutter each 1.5 s poll (list-signature guard). **Display MD toggle:** turning it on shows a **spinner next to the status** while the trajectory loads (clears once a frame is on screen, driven by the `nadoc:md-display-state` loading/frame events); and for an **oxDNA-seeded** run with **no MD frame written yet**, the toggle shows the **inherited oxDNA-seed positions** ("Inherited oxDNA-seed positions … — no MD frame yet") instead of "Waiting for trajectory output", handing over to live MD frames once they appear. | (new, 2026-06-20 — this session) | any sequenced design + GROMACS+NAMD to run a real job; an oxDNA-seeded run for the inherited-positions path | pure helpers pinned (`mdJobIsActive`/`makeSpinner`/`mdHasMetrics`/`mdListSignature`/`mdShouldShowInheritedSeed` in `md_jobs_panel.test.js`); `just smoke` console-error gate passed (panel + Health card + display wiring init clean). **NOT hand-driven:** the live spinning visuals, the inherited-seed→MD handover, and mid-run reload during a real NAMD run are human-eye only |
| | **MV-OXSEED-NAMD** | **oxDNA→NAMD seed runs the restrained ladder without blowing up** — load `workspace/6hb_sim_tests.nadoc`, run a sequenced oxDNA relax to completion → **Use as NAMD seed** → in Molecular Dynamics press **▶ Start** (relaxation, NOT Start Production). The seeded job should run the restrained ENM ladder (00_min → 01–04 ENM k0.5→k0.1→k0.01→MGHH) from the seeded structure and **complete without "atoms moving too fast"** (the old from-seed minimize→unrestrained jump FATAL'd at ~step 5000). Then **Start Production** (now enabled only after a relaxation checkpoint) should run stably. Confirm: there is **no longer a "relaxation can be skipped" confirm** on Start, and a seeded job with no relaxation checkpoint does **not** show production-ready. | (new, 2026-06-20 — this session) | `workspace/6hb_sim_tests.nadoc` + a completed oxDNA relax to seed from; needs GROMACS+NAMD | deformed-axis reconstruction (clash 58951→6301 on the real seed, dsDNA frame-corruption eliminated) is oracle-pinned (`TestDeformedHelixAxes`); ladder-wiring (no from-seed skip) unit-pinned (`test_md_milestone1` seeded-must-relax + not-production-ready). **NOT hand-driven:** the live multi-minute NAMD ladder run on the real seed (completes vs the old blow-up) is human-eye only |
| | **MV-OXSTALE** | **❌ FAILING hand-check (2026-06-24) — bug persists across multiple fix rounds (snapshot-restore → feature-log seek → roll client-sync → `nadoc:design-changed` refetch), each green in unit tests yet still broken in-app. The fix path is now AUTOMATION, not more manual back-and-forth → see `design_automation_backlog.md` Tier 7 (AF-25 headless seek + AF-26 end-to-end roll/return oracle, to be made RED on the current build first). Re-validate by hand only AFTER AF-26 goes green.** Out-of-date job guard + roll popup (design edited after a relax) — load a sequenced design (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics ▸ oxDNA ▸ **▶ Relax** to completion. Then **edit the design** (e.g. add/remove a strand, a crossover, or assign/clear a sequence). Confirm: (a) **every job row gets a ⚠** out-of-date marker immediately (not after a delay); (b) **Relax stays enabled**, and **Display / View-trajectory / Flexibility still work** (they read the job's own snapshot); (c) clicking **Full Sim** (production) OR **◉ Live** on a stale job pops a **"Design has changed"** dialog offering **Roll & run** / **Cancel** — Cancel does nothing; **Roll & run** SEEKS the Feature Log cursor back to the job's run-point: the post-job overhang disappears from the model, the ⚠ clears, AND — switching to the **Feature Log tab** — the cursor sits at the job's step with the later entries (the overhang) shown as inactive/forward; you can slide the rail forward to bring them back. A **persistent toast** also offers **↩ Return to latest** (saved as a "Latest" loadout). **Verify (the two-part report): (i) the ⚠ clears after Roll, AND (ii) the Feature Log cursor actually moved (visible in the Feature Log tab) and the overhang is gone from the model** — not just a silent snapshot swap. Sequence assignment now shows as its own Feature Log step ("Assign scaffold/staple sequence"). **(c2) MANUAL seek:** instead of Roll & run, go to the **Feature Log tab and drag the rail back to the job's run step yourself** — the job's ⚠ must clear on its own (the current state now matches what it was run at), even though you never touched the oxDNA panel; sliding forward again re-adds the ⚠. (d) Same for an **MD job**: edit the design after an MD prep → its row gets a ⚠; **Append production** on a stale MD job pops the same dialog + roll. (e) A purely **display change** (drag a cluster, move the camera) must NOT mark jobs stale. (f) Confirm no **internal server error** on a stale run (cleanly refused/rolled). | (new, 2026-06-23 — this session) | `workspace/6hb_OxDNA_test.nadoc` + CUDA oxDNA + (for MD) GROMACS/NAMD | fingerprint sensitivity/insensitivity + 409 guard (unit+route) + list out_of_date + **roll restores snapshot+sequences & clears the flag** pinned (`test_oxdna_staleness.py` + `test_md_staleness.py`); shared `job_staleness.js` (`ensureJobCurrent`) frontend tests. Backend/frontend/smoke green. **NOT hand-driven:** the in-browser ⚠-on-edit, the roll-or-cancel dialog, the structure+sequences restoring, the ⚠ clearing, the Return-to-latest loadout toast, and the MD-job parity are human-eye only |
| | **MV-OXLIVE-CUDA** | **Live oxDNA field session is snappy on a real design (now CUDA + in-memory readout)** — load a sequenced design with crossovers (`workspace/6hb_OxDNA_test.nadoc`, or a bigger one like `18hb2.nadoc`/`VoltronCore.nadoc` to feel the win) → Dynamics ▸ oxDNA ▸ **▶ Relax** to completion → select the relax job → set an E-field direction + ≥1 anchor → **◉ Live**. Confirm the structure **follows the field in near-real-time** as you **drag the field gizmo** (re-aim) — frames should update multiple times/sec, NOT the multi-second-per-frame crawl the old CPU path gave on larger designs. Also exercise **anchors-only** and **free-dynamics (no field)** live sessions. The display must match the old file-path frame (no visual jump/explosion) and **■ Stop Live** must tear down cleanly. **(2026-06-23 follow-up) Mid-run reconfigure:** while Live is running, **tick the hard-floor "Apply"** (and separately **enable the E-field** with an anchor present) — the running structure must **respond seamlessly from its current pose** (settle onto the floor / deflect under the field) with **no teleport back to the relaxed pose**; toggling them back off must remove the effect. Enabling a field with **no anchor** must warn and not apply. | (new, 2026-06-23 — this session) | `workspace/6hb_OxDNA_test.nadoc` (small) + `18hb2.nadoc`/`VoltronCore.nadoc` (feel the speedup); needs the CUDA oxDNA + patched oxpy | backend flipped live stepping CPU→**CUDA** (benchmarked 15–163× on this box) + in-memory `configuration_full_from_particles` readout (no per-frame file round-trip) + cached design_ref/adjacency; convention + equivalence pinned (`test_configuration_full_from_particles_matches_file_readout`, `test_unwrap_precomputed_adj_matches_builtin`, real-oxpy `test_configuration_map_matches_file_readout_real_oxpy`) and CUDA field-steering verified off-app (free beads swing 21.8 nm on a +x→−x re-aim, anchors hold). **NOT hand-driven:** the in-browser gizmo-drag→structure-follows responsiveness at CUDA speed is human-eye only |
| — | **MV-OXNATIVE** | ✅ **VALIDATED 2026-06-20** (user ran a real GPU relax — bp starts high, no startup collapse) — oxDNA-native seed: relaxation starts bonded, no startup collapse — load a sequenced design (`workspace/6hb_OxDNA_test.nadoc` or `18hb2.nadoc`) → Dynamics ▸ oxDNA ▸ **▶ Relax**. The structure should **NOT visibly collapse/melt then recover** in the early MC/MD-relax stages — the designed base pairs now start at oxDNA's native bonding geometry, so the **bp-retention health card should read high (~0.9+) from the first sample** instead of dropping to ~0 and climbing back. Confirm via **View trajectory**: the first frame already shows an intact bonded duplex (not splayed). The md_relax **escalate-retry should fire less** (no FENE over-stretch at the seed) on well-formed designs. Final relaxed structure/health is unchanged — only the early transient is gone. | (new, 2026-06-20 — this session) | `workspace/6hb_OxDNA_test.nadoc` / `18hb2.nadoc`; needs the CUDA oxDNA binary | `oxdna_native_seed_map` shift pinned (`test_oxdna_native_seed_bonds_pairs_at_frame_zero`: frame-0 bp 0→1.0, FENE over 246→0; +orientation-preserved +no-op-without-pairs) and the prepare→conf.dat path confirmed (frame-0 bp=1.0). **NOT hand-driven:** watching a real GPU relax start at bp~1.0 with no on-screen collapse is human-eye only |
| — | **MV-1** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — design-mode cluster **Move / Rotate** tool: right-click cluster → gizmo/panel → ✓ commit | #71 #78 #79 #80 #81 | a cluster-bearing design (e.g. `Examples/26hb_platform_v3.nadoc`) | 3D pointer-pick on a cluster + gizmo-handle drag not drivable at pixel precision (LESSONS H7) |
| — | **MV-2** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — **Overhang Orientation** panel: right-click a rendered overhang → Edit Orientation → step/apply/reset/auto-close; also the migrated context menu | #64, fix #7 | `Examples/NS_trans_fix.nadoc` (51 overhangs) ✓ + `workspace/OH6hb_test.nadoc` (2) ✓ | WebGL raycast on 1 of N overhang beads not drivable |
| — | **MV-3** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — **selection-rules UX** (drill levels, crossover tube, yellow hover+snap, Ctrl+click, cluster Tab removal) | fixes #10 #11 #12 #14 #15 | `Examples/6hb_test.nadoc` ✓ + `Examples/2hb_xover_val.nadoc` ✓ | — |
| — | **MV-4** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — Assembly **multi-select purple union BoxHelper** (parts + groups selection): Ctrl-lasso ≥2 instances → purple box around the union; white at 1; drops to 0 → clears | #34 | `workspace/Belt_test1.nass` (parts+groups) or any ≥2-part `.nass` | needs a built ≥2-part assembly + Ctrl-lasso multi-select |
| — | **MV-5** | *(generated — see GENERATED)* Assembly **right-click context menu**: right-click a part → linker-relax (enabled/disabled), attach-to-belt, select; pan-suppress | #69 | `workspace/Linker_Assem_test.nass` / `Belt_test1.nass` | assembly + linker/belt multi-step setup; right-click router |
| — | **MV-6** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — **Belt polymerize**: built belt assembly → Polymerize along belt → evenly-spaced copies | #32 | `workspace/Belt_test1.nass` / `belt_test.nass` | needs a built belt assembly |
| — | **MV-7** | ✅ **VALIDATED 2026-06-07** (see VALIDATED) — **Coalesced assembly part-refresh**: edit a part in part-context + save burst → shared instances refresh once (not per-instance) | #38 | a multi-part assembly with ≥2 instances of one source part | needs multi-part assembly + part-editor save burst |
| | **MV-RSZ** | **3D overhang-resize through the scaffold boundary**: select a staple end whose tail extends past the scaffold (an inline overhang) → grab the cyan extrude arrow → drag the tip *inward past the scaffold end* → the overhang shrinks away and the strand becomes flush (or shorter), matching the cadnano editor (no hard stop at the boundary) | (new, this session) | a design with an inline overhang on a staple end (e.g. `Examples/NS_trans_fix.nadoc`, or extrude one) | live pixel-precise 3D arrow-drag past the boundary; `terminalRunLength` + backend merge unit-pinned, gesture human-eye only |
| | **MV-SCAF** | **Section-router scaffold routing of irregular multi-section designs (teeth / dumbbell), seamed AND seamless** — now default-on for any multi-section design via both `auto_scaffold_seamed` and `auto_scaffold_seamless` (decompose trunk+windows → route each → 2-opt splice → buried nick). Programmatically pinned (1 strand, full coverage, inter-tooth gap clearance, buried single-helix nick, matched/polymerizable seamed trunk, fully-seamless cycle trunk) but the **live 3D weave is NOT hand-validated across designs**: trace one continuous strand through all teeth, gaps visibly open, buried nick mid-bundle, seamed far ends puzzle-fit for polymerization, seamless teeth show NO trunk seams (HC dumbbell still falls back to a seamed trunk). ALSO re-run idempotency (ISSUE-9: running autoscaffold twice corrupts faces). | ISSUE-8, this session | `tests/fixtures/teeth.nadoc` + `10-6-10hb_seamed.nadoc`; pre-baked `workspace/teeth_{section,seamless}_routed.nadoc` + `dumbbell_*_routed.nadoc` | **"tests-pass-but-visually-wrong" area** — already burned multiple sessions on a corrupt fixture; gap/nick/seam math can all pass while the rendered route is wrong. Needs an eyeball across teeth + dumbbell + other multi-section designs, both modes, + a double-run idempotency check |
| ▶ HEAD | **MV-8** | Assembly **config animation**: assembly with a saved feature-log configuration → "animate to configuration" tweens instances | #68 | an assembly that has ≥1 saved configuration (may need to create one) | needs assembly WITH a saved configuration |
| | **MV-OXDNA-CONFIG** | **Clicking an oxDNA job repopulates every card with that run's conditions**: load a sequenced design → Dynamics ▸ oxDNA → run a **relaxation** with a non-default Advanced setting (e.g. salt 0.6, or anchors/hard-surface enabled), and **branch an E-field run** (set a direction + magnitude + an anchor) → then click back and forth between jobs in the list. On each click, **Advanced** (backend/device/salt/mc-md-equil steps/bp-gate), **Hard surface** (Apply + axis + offset + stiffness), **Anchors** (the exact fixed strands as chips + the purple 3D glow), and **E-field** (Apply + magnitude + direction; the field arrow appears) all switch to **what that run used** — selecting a plain relax turns the field/surface cards off; selecting the field child shows its arrow + anchors. Confirm a running job polled every 1.5 s does NOT clobber the cards mid-edit (only an explicit click repopulates). | (new, 2026-06-19 — this session) | a sequenced crossover-routed design (`workspace/6hb_OxDNA_test.nadoc`) with ≥1 relax + ≥1 field child | `run_config` persisted on the job (`test_run_config_roundtrip`, `test_run_config_persisted_for_panel_cards` — relax + field child, camelCase anchors); `runConfigForJob` + `axisForNormal` + `applyConfig` on all three setup modules vitest-pinned. **NOT hand-driven**: the live click-to-repopulate gesture — chips/arrow/glow appearing on selection, card values flipping, the no-clobber-on-poll behavior — human-eye only |
| | **MV-TRAJ-KF** | **Trajectory keyframe plays an oxDNA trajectory in an animation** — load a sequenced crossover-routed design with a **completed oxDNA job** (`workspace/6hb_OxDNA_test.nadoc`) → Scene tab ▸ **Animation** panel → create an animation → click **+ Add Trajectory**. A **purple keyframe** with a **"Trajectory" badge** appears, keeping the text/delete/**Pose** controls but with a **State row = oxDNA-job dropdown + a frame-range slider with stage-marker ticks** (mc/md_relax/equil/production). Pick the job → the slider enables, ticks appear, label reads "frames a–b / N"; drag the **start** and **end** handles to bound the range (start can't pass end). Add a following normal keyframe, then **▶ Play**: during this keyframe's **hold** window the model should **deform through the selected trajectory frames** (CG beads stepping through the relaxed→production motion) while the camera lerps to the keyframe's pose; **Export WebM/GIF** should bake the same motion deterministically. Confirm leaving/stopping restores the live design geometry, and the **+ Add Trajectory** button is disabled in assembly mode. **Phase-1 known limitation:** the boundary into the *next* keyframe jumps from the trajectory's last frame back to design-CG (no morph). | (new, 2026-06-21 — trajectory-keyframes Phase 1) | a sequenced crossover-routed design with a **completed** oxDNA job (`workspace/6hb_OxDNA_test.nadoc`) | model + create/patch route-pinned (`test_keyframe_trajectory_create_patch_roundtrip`/`_defaults`); range + stride math unit-pinned (`trajectory_range.test.js` 17); player branch reuses `applyFemPositions`/`framesToUpdates`; **atomistic/surface follow (Phase 1b)** route-pinned (`composite_trajectory_atomistic`/`_surface`) + validated on real job `6ad892196278` (atomistic 66150 floats = design atom count; surface meshes per-frame); **photo-mode (Phase 3)** works by construction (same `player.seekTo→_applyAt` path); `just smoke` 23/23. **NAMD (Phase 2)** also pinned: `md_composite_trajectory` ported from the live Display-MD nadoc path (`ws.py` untouched), `GET /md/jobs/{id}/trajectory` validated on real 2hb DCD (frame-0 RMSD 0.154 nm) — `tests/test_md_trajectory.py`. **NOT hand-driven (human-eye, needs a completed oxDNA OR NAMD trajectory + app running):** (1) job-pick (dropdown lists **[oxDNA] and [NAMD]** jobs with timestamps) → range-drag → ▶ play → on-screen CG trajectory motion + Export WebM/GIF; (2) with **atomistic or surface rep ON** (oxDNA **and** NAMD now — Phase 2b), those reps step through the trajectory (oxDNA atomistic = design template moved; **NAMD atomistic = the MD run's own heavy atoms swapped in**, NAMD surface uniform-coloured; snapped to a downsampled subset; "building frames" notice + loading spinners appear); (3) the same animation run through **Photo mode** preview + high-res export reflects the trajectory; (4) **a NAMD-job trajectory keyframe** deforms the model through the DCD frames aligned on the design; (5) disabled in assembly mode |
| — | **MV-OXREPS** | ✅ **VALIDATED MANUALLY 2026-06-21** (atomistic ball-and-stick reads good — proper duplexes, correct colours/bonds, forward/reverse phase balanced; surface + flex-map + trajectory-scrub still live-owed) — **OxDNA display / flexibility map / view-trajectory follow in ATOMISTIC + SURFACE reps** (previously CG beads/slabs only): load a sequenced design with a **completed oxDNA job** (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics ▸ oxDNA. For EACH of the three toggles — **OxDNA display** (relaxed frame), **Flexibility map** (RMSF average), **View trajectory** (scrub) — turn it on, then switch the scene representation to **Ball & Stick / VDW** (atomistic) and to **Surface** via the View menu (F5/F6/F7): the heavy geometry should **rebuild to the oxDNA frame**, not the design (atoms/mesh deform to the relaxed/average/scrubbed structure). Switch reps back and forth WHILE a toggle is on → the freshly-built atomistic/surface mesh re-overlays the current frame (`nadoc:representation-change` re-apply). For **View trajectory**, scrub the slider with atomistic/surface active → it steps through frames; flip the new **"Atomistic/surface detail"** dropdown between **Coarse** (snaps to a downsampled bake — fast) and **Fine** (rebuilds the exact frame every step — the amber "can be very slow" warning shows). **Toggle the display OFF mid-reconstruction** (esp. Fine on a big design) → it cancels cleanly and the atomistic/surface mesh **restores to the plain design** (no stuck oxDNA overlay). Confirm protein/hybrid jobs still pose correctly. | (new, 2026-06-21 — this session) | a sequenced design with a **completed** oxDNA job (`workspace/6hb_OxDNA_test.nadoc`); for surface/atomistic any reasonably small design so reconstruction isn't punishing | backend single-frame reconstruction (`frame_atomistic_flat`/`frame_surface_json` + relaxed `_relaxed_full_map` + `production_rmsf(include_average_frame)`) routes (`display-atomistic/-surface`, `rmsf-atomistic/-surface`) — atom-count-matches-design + surface-shape pinned (`test_frame_atomistic_flat_matches_design_atoms`, `test_frame_surface_json_shape`, `test_production_rmsf_average_frame_opt_in`); controller apply/coarse-bake/fine/cancel/repr-reapply vitest-pinned (`oxdna_display.test.js` +8, `repKind`); `just smoke` 23/23. **Bond-stretch fix (2026-06-21, found via this MV's first hand-check):** ball-and-stick showed long bonds spanning the model because `build_atomistic_model` leaves nucleotides with NO frame override (loop/insertion copies — override is `copy_k==0` + 3-tuple-keyed only) at their DESIGN position while neighbours move; `atomistic_renderer.applyPositionLerp` now HIDES any bond >`_MAX_BOND_NM` (1.0 nm) so those don't draw as cross-structure lines (`atomistic_renderer.test.js` 3). Same fix benefits the trajectory-keyframe atomistic path. **Residual:** the few un-overridden nucleotides still sit at design positions (a few stray atoms, no bonds) — a deeper backend fix to the override (cover insertion copies) is the real cure, deferred. **Deeper fix 2 (2026-06-21, "mesh of short wrong bonds"):** diagnosed — NOT a serial/bond-list bug (the reconstruction reproduces the design's own geometry to median bond-diff 0.000 nm; bends reconstruct cleanly); the short crisscross bonds are per-nucleotide MC **noise** in a raw (un-minimised) CG frame propagating into the inter-residue O3'→P backbone bonds (the NAMD-seed path tolerates this because NAMD minimises afterward; display shows it raw). Fix: `_frame_atomistic_overrides` now runs the VALIDATED crossover-preserving per-domain smoother (`_smooth_cg_positions_per_domain`, σ=2.0) over the reconstructed backbone sites — DISPLAY/trajectory only, NAMD-seed path untouched. Synthetic-noise pin (`test_frame_overrides_smoothing_tames_noise_bonds`): stretched bonds 217→2 (≥10× fewer), twist preserved (median unchanged), no clashes, atom count identical. **Human-eye still owed:** confirm on the REAL relaxed structure that the atomistic ball-and-stick now reads clean (no mesh) AND that the smoothing didn't visibly over-flatten crossovers/overhangs; σ is tunable in `_frame_atomistic_overrides`. **NOT hand-driven (human-eye, needs a completed oxDNA job + app running):** all of the above live — the heavy mesh deforming to each frame, the coarse/fine toggle + warning, rep-switch re-overlay, and the toggle-off cancel/restore. **NOTE:** MD-job atomistic already worked live (ballstick stream); MD **surface** + the Display-MD last-frame default are a SEPARATE follow-up (untouched here — touches validated `ws.py`/`md_panel` streaming). **RECONSTRUCTION REWRITE (2026-06-21, supersedes BOTH band-aids above):** the CG→all-atom reconstruction now **stamps each nucleotide by its OWN oxDNA rigid frame** (a1/a3 from the .dat, a2=a3×a1) via a fixed calibration `R=F·Q, origin=backbone_site+F·c` (`build_atomistic_model(frame_override=…)` + `_oxdna_rigid_frame`), instead of overriding only the backbone position and re-deriving each base's orientation from `position−axis` (which amplified MC positional noise into the crossing-bond mesh). (Q,c) is fit EMPIRICALLY per (strand_dir, helix_is_forward) bucket on an ideal duplex (Kabsch, residual <1e-6, asserted in `_rigid_frame_calibration`) and reproduces `build_atomistic_model(design)` to <0.01 Å — atom count, serial→(name,element,residue) map, and bond list ALL identical, so the renderer's serial-keyed bond list stays valid. **The σ=2 per-domain smoother is GONE** (`_frame_atomistic_overrides` removed; the placer needs no positional smoothing); the renderer's >1 nm bond-hide is kept ONLY as a backstop. **Insertion copies are now covered** (`copies=True` copy-aware readers + copy-aware override key) → no atoms stranded at the design position. Pins: `test_rigid_frame_calibration_buckets_exact`, `test_rigid_frame_placer_reproduces_design_build`, `test_rigid_frame_placer_is_rigid_under_reorientation`, `test_frame_atomistic_covers_insertion_copies` (backend 2937 green; frontend 1622; ruff clean). **HUMAN-EYE OWED (NOT VERIFIED IN APP — no live oxDNA job available in this session):** on a REAL relaxed job (`workspace/6hb_OxDNA_test.nadoc`), confirm ball-and-stick / VDW now reads CLEAN (no crossing/over-stretched backbone bonds, no short-bond mesh) and the dsDNA reads as B-DNA (no over-flattened crossovers/overhangs); confirm surface + trajectory scrub + flexibility-map reconstruct cleanly under the new placer. (rmsf "flexibility map" average_frame stays copy-0-only — insertion copies in the AVERAGE structure fall back to the renderer cutoff, acceptable for a smoothed view.) **VALIDATION + BACKBONE-CLOSURE FIX (2026-06-21):** there is now a programmatic audit of every atomistic bond — `backend/core/atomistic_validation.py`, route `POST /oxdna/jobs/{id}/display-atomistic-audit`, CLI `just audit-atomistic`, skill `/validate-atomistic`. It found the screenshot's long bonds were systematic backbone discontinuity (sequential O3'→P median 0.91 nm); the fix `_close_sequential_backbone` (display-only) re-seats the phosphate linker between rigid C3'/C5' anchors → audit confirms **backbone max 3.16→0.81 nm, hidden-by-renderer 266→0, rigid stamp still 0 violations**. Live eyeball still owed: on a real relaxed job, confirm the ball-and-stick backbone now reads as a CONNECTED chain (no long sticks) and that the mild residual stretches/clashes at frayed regions look acceptable; run `just audit-atomistic 6hb_OxDNA_test` to get the per-bond numbers for whatever job you're viewing. **BASE-COLLAPSE FIX (2026-06-21 later — the user's 2nd screenshot):** the rigid-frame placer was COLLAPSING base pairs (WC C1'-C1' median 0.48 vs 0.94 nm; bases crushed onto partners → bonds looked like they joined adjacent/opposite bases) because oxDNA's relaxed a1 doesn't map to the all-atom base direction the calibration assumed. The audit was blind (only checked bond lengths). Fixed: reverted display to the axis-derived reconstruction (`build_display_model` — correct WC pairing 0.94) + kept backbone closure; added inter-base geometry (WC + stacking C1'-C1', `wc_collapsed`) to the audit so this can't regress silently. Audit now: **WC 0.94 OK, stacking 0.47, hidden 266→4, stamp clean**. Live eyeball owed: on a real relaxed job the duplex should now read as a proper paired/stacked helix (NOT crushed); `just audit-atomistic <stem>` reports WC C1'-C1' median (should be ~0.9-1.0, NOT ~0.5). **INDEX-MAPPING FIX (2026-06-21):** the renderer built atoms from the LOADED design while positions came from the JOB snapshot — if the design was edited after the job ran (different sequence → different atoms/serials), every serial mapped to the wrong atom (scrambled colours/bonds). Fix: `display-atomistic` returns `topology_hash`; the frontend rebuilds the renderer from `GET /oxdna/jobs/{id}/atomistic-model` (the job's own atoms+bonds) before overlaying positions. **FORWARD/REVERSE PHASE FIX (2026-06-21):** REVERSE-lattice helices were mis-phased (WC C1'-C1' 0.715 vs FORWARD 0.958) — `_atom_frame`'s reverse azimuthal correction applies the design's per-lattice-direction groove offset (±150°), but oxDNA relaxation erases that distinction, so the display must use the uniform forward branch (`build_atomistic_model(relaxed_oxdna_phase=True)`). Audit now: WC FORWARD 0.958 / REVERSE 0.961 (balanced); `wc_helix_imbalanced` guards it. ✅ **VALIDATED MANUALLY 2026-06-21** — user confirmed the atomistic ball-and-stick reads good (proper duplexes, correct colours/bonds, no half-helix phase split). **TRAJECTORY PER-FRAME VALIDATION (2026-06-21):** the trajectory-scrub correctness is now machine-proven across the WHOLE composite, not just frame 0 — `atomistic_validation.audit_trajectory_frames` audits a sampling of the whole-lineage composite, reconstructing each frame through the SAME `build_display_model` sink the scrubber uses; route `POST /oxdna/jobs/{id}/trajectory-audit`, CLI `just audit-trajectory`, skill `/validate-atomistic --trajectory`. Per-frame gate (`summary.all_invariants_ok`) = 0 rigid-stamp violations + no `wc_collapsed` + no `wc_helix_imbalanced` (forward/reverse phase balanced) + no non-finite atom + identity preserved; over-stretch/clash counts reported but NOT gated (inherent raw-CG roughness, same as the single relaxed frame). Verified on the REAL 3-stage job `c1299e0b07b5`: all 8 sampled frames (0→198 of 199) read **ALL FRAMES SOUND** (WC C1'-C1' 0.93–1.10 nm, stamp Δ=0, fwd/rev balanced, identity preserved). Tests: `test_atomistic_validation.py` +6 (clean multi-frame all-sound, collapsed-frame caught, explicit indices, not-ready, `_sample_frame_indices`, route). **TRAJECTORY-ATOMISTIC LOAD FIX (2026-06-21, user reported "fragile / can't load atomistic at all"):** root cause was performance — heavy-rep trajectory was (a) re-reading+PBC-unwrapping+Kabsch-aligning the WHOLE trajectory (~14 s for a 199-frame 6hb) on EVERY frame request, and (b) **pre-baking 40 atomistic frames upfront** (≈9 s/all-atom-build each) → ~9 minutes before frame 0 showed → effectively never. Fixes: (1) backend memoizes the aligned composite frames per file-signature (`_ALIGNED_CACHE` in `oxdna_health.py`) so a completed job aligns ONCE, not per frame (27 s→16 s warm); (2) frontend `oxdna_display.js` now fetches heavy frames **lazily — one snap-grid cell at a time on visit, cached** (grid 12 atomistic / 8 surface), never a big upfront batch, so the first atomistic frame appears after one rebuild (~16-27 s) instead of 9 min, and scrubbing within a cached cell is instant; (3) a `nadoc:oxdna-heavy-status` "⏳ Building atomistic frame…" spinner (controller→panel via `onHeavyStatus`) so it never looks frozen; (4) rep-switch-during-trajectory already rebuilds the current frame for the new rep (`reapplyForRepr`) and CG↔atomistic is seamless (CG overlay always applied under the hidden mesh). Tests: `oxdna_display.test.js` (lazy per-cell fetch + heavy-status events), `test_oxdna_relaxation.py` (align-cache by file signature + invalidation). main.js Δ +4 (one `onHeavyStatus` wiring block). **PLAYBACK FIX (2026-06-22, user "I can't actually play the trajectory"):** in a heavy rep the 8 fps play loop ticked far faster than each ~2–16 s all-atom frame could build → ticks piled up, view never advanced (looked broken). Now **clicking ▶ pre-builds every coarse playback frame first** (player `onBeforePlay` async hook → controller `prebuildHeavy`, concurrency 3, warms the align cache on the first cell), with a **⏳ spinner on the button + "Preparing playback… building frame k/N" progress**, THEN runs the loop — now smooth because every frame is cached (instant), and replays are instant. During playback the controller **forces coarse** (`setPlaying(true)`) even if the detail dropdown says fine, so a per-tick fine rebuild can't stall the loop (fine still applies to manual scrub). Clicking ▶ again mid-prepare cancels; toggling the display off cancels the prebuild. Measured on real job `c1299e0b07b5`: full 12-frame atomistic coarse grid prebuilds in ~36 s (10 s cold align + ~2.3 s/warm frame), then loops smoothly; CG plays instantly (no prebuild). Tests: `oxdna_trajectory_player.test.js` (+2: prepare-gated play, cancel-mid-prepare), `oxdna_display.test.js` (+3: prebuildHeavy builds-each-cell-once/CG-noop, setPlaying forces coarse). main.js Δ 0 this pass. ✅ **TRAJECTORY VALIDATED MANUALLY 2026-06-22** — user exercised the View-trajectory atomistic path and confirmed it works: slider scrub loads the nearest frame, and ▶ play pre-builds the coarse frames (spinner) then loops smoothly. Surface-rep + flexibility-map under atomistic/VDW remain the only lightly-owed live items (lower priority; same shared pipeline, correctness machine-proven by the per-frame audit). **FLEX-MAP HEAVY-REP PARITY (2026-06-22):** the flexibility-map (RMSF average) overlay already reconstructed atomistic/surface from the average structure (`rmsf-atomistic`/`rmsf-surface` → `frame_atomistic_flat`/`frame_surface_json`), but unlike trajectory it gave NO "building…" feedback during the multi-second rebuild (panel looked frozen) and the surface path was untested. Fixed: the panel now renders a "⏳ Building atomistic/surface average structure…" notice on the flex status line (the `nadoc:oxdna-heavy-status` event now drives `_renderFlexStatus()` too, gated on `mode()==='rmsf'`), and `oxdna_display.test.js` gained the surface-RMSF apply pin + a flex-map heavy build-status pin (+3 tests, frontend 1632 green). **FLEX-MAP HEAVY-REP COLOURING + CACHE (2026-06-22, same session):** two correctness gaps closed. (1) **Colouring** — the atomistic atoms + surface mesh now show the SAME rigid→flexible viridis ramp as the beads (previously they reconstructed to the average structure but kept CPK/strand colours). Atomistic: a scalar overlay keyed `helix:bp:dir`→hex (`atomistic_renderer.applyScalarColors`/`clearScalarColors`, resolver branch, `color_resolver.test.js`), built frontend-side from the same `rmsfColorMap` the beads use. Surface: backend `rmsf-surface` now emits **per-vertex RMSF** (`frame_surface_json(color_mode='rmsf', rmsf_by_key)` → nearest-atom KD-tree `_vertex_rmsf`; `_rmsf_average_frame` returns the rmsf map), the frontend converts it through `rmsfToVertexColors` (viridis, same scale) and forces it through any colour mode (`surface_renderer.applyScalarVertexColors` + `scalar` flag in `_rebuildTopology`). Dragging the RMSF **scale widget recolours both heavy reps live** (`recolorRmsf` re-applies, no re-fetch). (2) **Cache** — the computed flex map is now kept across toggle-off (`_rmsfCache` keyed by job id), so re-toggling the SAME job shows it instantly with no recompute; `refresh()` (and a new child run = new id) still re-fetch. Tests: backend `test_frame_surface_json_rmsf_emits_per_vertex_flexibility`; frontend `oxdna_display.test.js` (+ atomistic-colour, surface-scalar, cache-no-refetch, recolour-heavy, clear-on-stop, `rmsfToVertexColors`) + `color_resolver.test.js` — frontend **1638 green**, backend `test_oxdna_relaxation` 143 + `test_oxdna_surface` 17 green, ruff clean. **HUMAN-EYE OWED (NOT VERIFIED IN APP — no live GPU oxDNA job this session):** on a completed job (`workspace/6hb_OxDNA_test.nadoc`), turn on **Flexibility map**, switch the scene to **Ball & Stick / VDW** and to **Surface** → the heavy geometry rebuilds to the RMSF AVERAGE structure (not the design), **coloured rigid→flexible (dark-purple→yellow) matching the beads**, the flex status line shows the building spinner during each rebuild; drag the RMSF scale slider → atoms/mesh recolour live; toggle the map OFF then ON → it reappears instantly (cached) and OFF restores normal CPK/strand colours (no stale RMSF tint on the design). |
| | **MV-OXLIVE** | **OxDNA display follows a RUNNING job + next-frame countdown**: load a sequenced crossover-routed design (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics ▸ oxDNA → **▶ Relax** → while the job is still **running**, toggle **OxDNA display** on. The deformed NADOC model should **re-update to the newest relaxed frame as the sim writes each one** (it visibly steps forward every print_conf_interval, ~10 frames/stage — not frozen until completion), and the display-status line should read **"Showing relaxed positions (… ) · next frame ~Xs"**, the estimate counting down between frame writes and resetting after each update. Confirm it also follows a **production / E-field** run, that toggling off / leaving the Dynamics tab stops the follow, and that a completed job still shows the final frame with no countdown. | (new, 2026-06-19 — this session) | a sequenced crossover-routed design mid-relax (`workspace/6hb_OxDNA_test.nadoc`) | backend `next_frame_eta_seconds` + `frame_index` from the live rate unit-pinned (`test_job_progress_next_frame_eta`, `test_print_conf_interval`); the live-follow re-fetch on frame_index advance + countdown render are closure wiring in `oxdna_jobs_panel.js` — **human-eye only** (needs a real running GPU job to watch the model step + the estimate tick) |
| — | **MV-OXDNA** | ✅ **VALIDATED 2026-06-14** (user-confirmed all gestures: relax + production status flow, step-count + ETA, OxDNA display deform, Show RMSD, per-design filtering). **oxDNA panel — relax/production statuses + display + name**: load a **sequenced** design (crossover-routed, e.g. `workspace/6hb_OxDNA_test.nadoc`) → Dynamics tab → single **"oxDNA"** panel → badge "oxDNA ready · …" → click **▶ Relax** → a row in the **scrollable list (this design's jobs only)** shows the correct **file name** (not stale metadata) and the detail status steps **queued → Running · X/3 stages → completed**, progress bar fills, stage timeline glyphs flip (·→○→●), health cards update. Then **Start Production** (enabled only after relax completes) → a 4th `production` stage runs to completed. Toggle **OxDNA display** → the NADOC model **deforms to the relaxed positions and SHOWS (compact, in place — not scattered/arcs-only)**; untoggle restores. Unsequenced design → Relax shows the "needs sequences" message. Confirm Export oxDNA ZIP, Stop/Delete, leaving the tab clears display. | (2026-06-14 — oxDNA Phase 1 + consolidation/production/display-PBC/name) | a **crossover-routed sequenced** design (`workspace/6hb_OxDNA_test.nadoc`; the crossover-less 6hb_primitive disperses by design + is unsequenced) | status MACHINE validated with the real binary at runner (`test_runner_real_binary_status_lifecycle`) + HTTP (`test_oxdna_http_lifecycle`: create→completed→display→**production**) levels; PBC-unwrap (`test_read_configuration_unwrapped`), production guard, name-from-source, and sidebar rendering (`detailStatusText`/`stageChips`/`jobDisplayName`/`toFemUpdates`) all unit-pinned; panel boots clean (smoke). The unwrap was verified to produce a compact structure at the design centroid on the user's REAL job data. NOT hand-driven: the live browser gesture (Relax+Production tick, the actual on-screen model-deform) — human-eye only, needs a sequenced design + a real GPU run |
| | **MV-BENCH** | **Dynamics-panel Benchmark cards** (oxDNA + NAMD subsections): load any design → Dynamics tab → expand the oxDNA (or Molecular Dynamics) section → expand the **Benchmark** collapsible card → click **⏱ Benchmark this machine** → button shows a **spinner**, a **loading bar + ETA** advances, **every other control in BOTH Dynamics panels is disabled**, status steps `Building… → Running — <label>… → Best: <config>`, per-trial results + proxy note fill → click **Apply & save as default** → Backend/CUDA-device (oxDNA) or Threads/CUDA-device (NAMD) inputs pre-fill to the winner + the design saves (per-machine, keyed by hostname). Also verify **Cancel** mid-run (stops, re-enables controls, keeps old defaults) and the **single-config warning** (on a no-GPU machine, oxDNA Benchmark `confirm`s "nothing to compare" before running). | (2026-06-18/19, this session) | any design (synthetic proxy is size-driven, independent of content; e.g. `Examples/6hb_test.nadoc`) | oxDNA sweep GPU-verified end-to-end at the runner level (CPU ~480 vs CUDA ~1100–1180 steps/s on RTX 2080S incl. the timing/ETA path, recommendation + temp-dir cleanup) and the route/apply/metadata/**cancel/ETA/409-guard** paths TestClient-pinned + the full panel flow (sweep/cancel/guard/lock/spinner/bar) vitest-pinned (8 tests); **NOT hand-driven**: the live browser gestures — spinner/bar/ETA animation, panel-lock, Cancel click, confirm dialog, apply-fill (dev server was wedged mid-reload during the session). NAMD sweep additionally needs GROMACS+NAMD binaries + a real run to confirm ns/day timing. |
| | **MV-ANCHGLOW** | **E-field + anchor setup viz**: load a sequenced design → Dynamics → oxDNA → open **Electric field** card → a **thick 3D arrow** (solid cyl+cone, not a thin line) shows the field direction and drag-reorients/-resizes it; tick **Apply** (enable field, set pN>0). Then in the **Anchors** card, select an overhang/domain/cluster in the 3D view → **Add selection as anchor** → those beads light up in a **purple glow**; remove/clear drops the glow; disabling the field or leaving the Dynamics tab clears it. | (2026-06-19, this session) | a sequenced design with selectable overhangs/clusters (e.g. `Examples/NS_trans_fix.nadoc` or `workspace/6hb_OxDNA_test.nadoc`) | arrow-is-solid-mesh + anchorGlow-layer-wired live-verified (throwaway e2e scene introspection); `resolveAnchorEntries`+controller+gizmo unit-pinned (9+6 tests); boot/render console-error gate green. **NOT hand-driven**: pixel appearance (arrow thickness/colour, purple halo over the *right* beads after a real 3D selection→add-anchor) — Tier-3 human-eye. |
| | **MV-CONJ** | **Conjugate Manager — design an azide-oligo conjugate**: with a design that has overhangs AND an imported protein (File ▸ Import PDB…), open the manager TWO ways: (a) **Tools ▸ Conjugate Manager…** and (b) **right-click the rendered protein → "Conjugate protein to ssDNA…"**. A **windowed** modal (Overhangs-Manager size/style, centred ~860×600) opens: **central 3D viewport** shows ONLY that protein and must be **fully operable — rotate / zoom / PAN** around it; surface-accessible sites are dotted (Lys green / Cys orange / N-term magenta). **Left panel:** an **Overhangs list** (rows of the design's overhangs); selecting a row auto-fills the **"ssDNA handle"** field with that overhang's **reverse complement**; an **Azide modification** radio toggles **5′ / 3′**; a **numbered, scrollable "Conjugation sites" list (right)** that is bidirectionally selectable with the 3D markers (click a list row → its marker GLOWS + scrolls into view; click a marker → its row highlights); a **Conjugate** button renders the preview **ssDNA in NADOC's bead/slab + connecting-cone model** on the SELECTED site, pointing radially outward, azide-end bead (red) at the chosen 5′/3′ end; and **Apply / Cancel** buttons. **Apply** (enabled only once an overhang AND a site are selected) **commits to the real design** — creates the ssDNA handle as a real **OH_BINDER strand bound to the overhang** (magenta, appears in 3D/cadnano/spreadsheet) AND **moves the protein** so its selected conjugation residue sits at the binder terminus matching the chosen azide end (5′/3′ → nearer overhang end), then **closes**; **Cancel/✕/Esc/backdrop** discard + close. A **loading spinner** sits over the viewport while the surface sites compute. Confirm: spinner shows then clears; pan/orbit/zoom work; site↔marker selection syncs + glow; bead/slab/cone handle looks like ssDNA pointing outward; **after Apply the binder strand + the placed protein appear in the MAIN design view** (protein conjugation residue on the chosen end) and **one Undo reverts both**; 5′ vs 3′ azide visibly lands the protein on opposite ends. | (new, 2026-06-19 — this session; iterated: full-screen marker view → windowed → bead/slab + site-list + Apply-commit) | a design WITH a **sequenced** overhang (e.g. `Examples/NS_trans_fix.nadoc`) + an imported protein PDB | SASA/candidate geometry + the conjugate route (binder created with `binds_overhang_id`+RC, protein attachment with right `attach_end` via nearest-terminus, **single-undo reverts both**, 5p/3p flip the end) backend-pinned (`tests/test_protein.py` 33 incl. azide-flip + `tests/test_conjugation.py` 7); RC / radial / perpendicular / backbone-points pure helpers + the modal DOM (overhang→handle, site-list select, Conjugate-renders, **Apply commits+closes / Cancel closes**, teardown) + context-menu vitest-pinned (`conjugate_manager.test.js` + `conjugate_manager_logic.test.js` 27). Backend Apply path also exercised live (crambin: binder + attachment created, undo reverts). **NOT hand-driven**: the live windowed modal — spinner timing, 3D pan/orbit, marker glow on a real pixel-pick, bead/slab/cone handle appearance, and the post-Apply MAIN-view result (binder strand drawn + protein on the chosen end, 5′ vs 3′ opposite ends) — Tier-3 human-eye. |
| | **MV-ENGINES** | **MD Engines install/status — the MISSING-engine paths** (this machine has every engine installed, so none of the install UI renders here): on a machine **lacking** oxDNA/NAMD/GROMACS, (a) **Help ▸ MD Engines (install / status)…** lists each engine with a red/green dot, GPU summary, and the right action button per engine; (b) the **oxDNA Dynamics** + **Molecular Dynamics** sidebar sections show an amber **install gate** banner ("⚠ … is not installed", "Set up engines…" button) atop the body while their required engine is missing, and the gate disappears once installed; (c) clicking **Install (CUDA/CPU)** on oxDNA/ANM-fork runs the build over `/ws/engines/install` with a live **progress bar + streamed log**, then on success the panel/gate refresh to green; (d) on a box **with a GPU but no `nvcc`**, the oxDNA action is **blocked from auto** and instead pops the **command popup** naming "CUDA toolkit (nvcc)" as the missing prereq (NEVER silently builds CPU); (e) **NAMD** → **Download…** popup with the license-gated link (opens the NAMD site) + the extract command + Copy; (f) **GROMACS** → guided popup (conda line first, apt alt); (g) **NAMD "Check download & install"**: in the NAMD Download popup, after downloading the license-gated tarball, click **Check download & install** → it scans `~/Downloads`, shows "Found NAMD_*…CUDA build" (or a CPU-on-GPU warning, or a path box if not found), then **Check & install** verifies the file contains `namd3`, extracts to `~/Applications`, and the panel/gate flip to green (psfgen too). Confirm the auto-build actually compiles + is detected, the fallback-to-popup fires when a build fails or the toolchain is missing, AND the NAMD verify-extract-detect round-trip. | (new, 2026-06-19 — this session) | a machine **without** oxDNA/NAMD/GROMACS (or temporarily unset `$OXDNA_BIN`/rename `~/oxDNA` + `~/Applications/NAMD_*`); GPU-but-no-nvcc box for path (d) | status model + GPU-aware install planning + the build orchestration/streaming/verify + fallback-on-error all backend-pinned (`tests/test_engines.py` 17 + `test_engine_install.py` 9, incl. "GPU box never planned CPU", nvcc-missing-blocks-auto, build-into-conventional-`build/`, step-failure + binary-not-detected raises); the panel/gate/dispatch + download-popup vitest-pinned (`md_engines_logic.test.js` 15 + `md_engines.test.js` 4); the **status panel opening + live GPU/engine rendering is hand-verified** (Help▸MD Engines opens, shows the real RTX-2080-but-no-nvcc summary + all engines green, zero console errors; smoke 22 green). **NOT hand-driven**: every MISSING-engine affordance — the gates, the auto-build progress bar/log, the command/download popups, the GPU-no-nvcc steer — because all engines are present on this machine. **Phase 2 added the simulation switch** `NADOC_ENGINES_FORCE_MISSING=oxdna,namd,gromacs just dev` → makes those engines report missing on THIS machine so the whole UI renders (auto-build runs a side-effect-free dry-run → falls back to the popup); the `/ws/engines/install` round-trip is now TestClient-pinned (`tests/test_engines_ws.py`). So the **missing-engine UI is reproducible without a fresh box** — hand-check it via the switch. Still needs a genuinely clean box (Docker/CI) only for the REAL compile + the GPU+nvcc vs GPU-no-nvcc build outcomes. **Phase 3 (download-finish): the NAMD verify→extract→detect round-trip is LIVE-verified** against a real uvicorn in a sandboxed `HOME` (fake tarball in `~/Downloads` → scan → WS install → namd+psfgen both detected; real `~/Applications` untouched) and TestClient-pinned (`test_engines_ws.py`); the scan endpoint is live read-only verified. NOT hand-driven: the browser "Check download & install" gesture (needs `NADOC_ENGINES_FORCE_MISSING=namd` + a NAMD tarball in `~/Downloads`). |
| | **MV-DELDEP** | **Feature-log delete = roll back geometry + dependents dialog**: build a design with a few feature-log steps (e.g. bundle → 2 segments → auto-break). (a) Delete the **last** step (×) → its geometry disappears. (b) Delete an **earlier independent** step (a parallel segment on different cells) → it vanishes, later independent steps stay. (c) Delete a step that **later steps depend on** (e.g. the segment under the auto-break) → a **"Delete dependent features?"** modal lists the dependents (F-numbers + labels) with **Delete all (N) / Revert to here / Cancel**; Cancel leaves everything; Delete-all removes the step + its dependents; Revert rolls back to before the step. Confirm one Undo restores after any delete. | (2026-06-19, this session) | any extrusion-built design; e.g. `Examples/26hb_platform_v3.nadoc` or a fresh bundle+segments | dependency analysis + cascade/decision/rollback fully backend-pinned (`tests/test_feature_dependencies.py` 9 + `test_feature_log_snapshot.py` surgical/parallel/cascade + protein/relax inversions; suite 2671) and the panel→client→dialog wiring builds clean. **NOT hand-driven**: the live × click → modal appearance, the three buttons' effects, and the on-screen geometry rollback — dev servers were down this session (WSL2 `--reload` wedge). Tier-3 human-eye. |
| | **MV-9** | **Assembly open from library**: open a `.nass` from a library row → enters assembly mode cleanly | #59 | any `workspace/*.nass` | part-open exercised live; assembly-open path verbatim-only |
| | **MV-10** | **Autosave write-back + server-restart recovery**: workspace-backed file + edit burst → debounced save; kill+restart backend → silent recovery badge | #53 #55 | any workspace-backed `.nadoc` | needs workspace file + edit burst + a real backend restart |
| | **MV-11** | **Two-real-tab cross-doc sync** + co-editing "saved" badge sibling indicator | fixes #2 #4 | same file opened in two browser tabs | BroadcastChannel can't cross Playwright contexts |
| | **MV-12** | **Overhang-binding context menu**: right-click an overhang-binding line → Bind/Unbind/Delete | fix #6 | a design with non-empty `overhang_bindings` (none known — may need to build) | no fixture has bindings; WebGL right-click |
| | **MV-13** | **FRET / fluorescence glow**: fluorophore-labeled design → View menu Fluorescence / FRET toggles → emitters glow, FRET quench scaling | #24 | a design with fluorophore-modified strands (none known — may need to build) | scaffold-only parts have no fluorophores; glow logic unit-tested only |
| | **MV-14** | **Representation option sliders** real mouse-drag (not JS-dispatch): expand the Representation sidebar section, drag each slider | #83 (partial) | any scaffolded part | panel section collapsed by default → `.fill()` needed visibility; drove via JS-dispatch instead |
| | **MV-15** | **Properties panel for an overhang-/assembly-anchored protein**: select a protein attached to an overhang (and one in an assembly) → panel shows the overhang id + attach-end / part-instance anchor, not just the free case | fix #18 (ISSUE-5) | needs a design with a protein attached to an overhang (none known — build via `/design/protein/attachments`) + an assembly with a protein | only the FREE-anchor branch was eyeballed live; overhang/assembly anchor branches are unit-tested only (no fixture) |
| | **MV-17** | **File-load progress overlay**: open this tab as a part editor (`?part-instance=<id>&assembly-doc=<docId>` — i.e. dive into a part from a live assembly) → the `#file-load-progress` overlay appears ("Opening Part"), the progress bar fills, the log lines append, "▸/▾ Details" toggles the log pane, and it auto-hides green on success (or turns red + shows the actions row + main-menu button on failure) | #87 | a live assembly with ≥1 part instance (e.g. `workspace/Belt_test1.nass`), then dive into a part | only fires on the `?part-instance=` part-editor boot path — needs a spawned part tab off a live assembly; boot console-error gate constructs the factory but never SHOWS the overlay |
| | **MV-18** | **Blunt-end (domain-end) menus**: load a design → left-click a rendered blunt/domain-end ring → the right-sidebar action panel appears ("helix N bp M") → Extrude opens the continuation slice plane (amber ghost), Bend/Twist start the deform tool at that end; ALSO right-click the ring → context menu at cursor → same Extrude/Bend/Twist; outside-click dismisses the ctx menu | #88 | a design with exposed blunt ends (e.g. `Examples/26hb_platform_v3.nadoc`) | panel/ctx appear only on a 3D domain-end ring pick (WebGL raycast at pixel precision); the action paths' store/slicePlane/deform effects are unit-pinned, the teardown `hidePanel` is smoke-covered, but the live click→panel→action gesture is human-eye only |
| | **MV-19** | **Force-Crossover tool (3D forced ligation) live gesture/visual**: load a design → click the `fxover` toolbar button (teal active state, End filter button lights, mode line "FORCE CROSSOVER…") → hover a 5′/3′ end (standard yellow snap glow) → click it (locks GREEN anchor) → only OPPOSITE-polarity ends on OTHER strands now hover-highlight, and hovering one shows the yellow crossover **arc preview** → click it → the two strands merge into one (arc renders, cadnano editor + sidebars update) → tool resets to step 1. Esc in step 2 drops the anchor; Esc in step 1 exits (restores prior selection level + mode line). Confirm lasso/Ctrl-multi-select are inert while active. | (new) | `Examples/26hb_platform_v3.nadoc` (52 staple ends) | topology (2 strands→1, ForcedLigation record, clean deactivate) verified end-to-end via the `__nadocForceXover` dev hook; the pixel-level raycast snap, glow COLORS, arc-preview appearance, and button/mode-line visuals are Tier-3 human-eye only |
| | **MV-16** | **Atomistic / surface representation VISUAL**: load a design → F6 VDW (space-fill atoms render, CG hidden) / F7 Ball & Stick (bonds render) / F5 Surface (molecular SES/VdW *mesh* appears, correct shape, opacity + probe-radius sliders + strand/uniform colour buttons affect it) / per-region overlay (pin a strand→VDW/surface while base stays full — overlay coexists with CG, no z-fight); F4 restores. Confirm the actual rendered geometry *looks right*, not just the panel toggles. | #86 | a representative `.nadoc` (e.g. `Examples/26hb_platform_v3.nadoc`); per-region needs a design with `representation_overrides` (mixed_representation work) | Tier-3 golden-image "does it look right" check (deliberately NOT automated — needs a pinned rasterizer); the F6/F5/F4 panel-toggle + zero-console path IS covered by the #86 exercise, but the mesh/atom *appearance* is human-eye only |
| | **MV-PRIM** | **Primitive hover-preview animation VISUAL**: Tools → Add Primitive → the right-sidebar Primitives panel lists 6HB/18HB cards with static poster thumbnails → **hover a card** → the poster swaps to a looping GIF that smoothly tweens the structure through its saved camera poses and **loops seamlessly** (no jarring jump at the wrap) → leaving the card reverts to the poster. ALSO a **larger preview pins to the upper-right of the workspace** (below the selectable strip, against the right panel's left edge, not spilling over the left sidebar) showing the animated GIF + name caption; it dismisses on leave. Confirm the motion reads well, the structure is recognizable at ~360 px, and the corner preview's placement/size feels right at different window sizes + with the left sidebar expanded/collapsed. ALSO: re-run `just build-primitives` and eyeball the regenerated `<name>.gif`/`.poster.png` (e.g. decide whether the origin-axes helper should be hidden before capture). | (new, 2026-06-11) | seed primitives `workspace/Primitives/6hb_primitive.nadoc` + `18hb_primitive.nadoc` | poster→GIF src-swap on hover + the catalog/API wiring ARE test-covered (`primitive_library.test.js`) and `buildCameraPath` is unit-pinned; but the rendered GIF's smoothness / seamless-loop / thumbnail recognizability is a Tier-3 golden-image "looks right" check, human-eye only |
| | **MV-PRIM-FACE** | **Add a primitive onto an existing part's FACE (beam, flat end)**: open a part with exposed blunt ends (e.g. an 18HB beam, or `Examples/26hb_platform_v3.nadoc`) → Tools → Add Primitive → **select a 6HB card** (placement arms; the origin slice plane shows) → **click a blunt-end ring on the part's end face** → the slice plane JUMPS to that face (mode line "PLACE PRIMITIVE ON FACE…", continuation lattice: amber = existing helix-ends) → **hover** → the 6-helix ghost footprint follows on the face's lattice, snapping to shape-preserving cells, extruding AWAY from the body → **click a lattice-appropriate spot** → the footprint commits as a `bundle-continuation` (Feature Log entry, revertable): cells landing on existing helix-ends EXTEND them coaxially, cells on empty spots become fresh helices. Esc cancels. Confirm: the ring-click actually retargets the armed footprint (pointer coordination), the ghost sits on the face correctly, overlapping cells continue (one fused helix, no break) vs fresh cells start new, and direction is away from the body. ALSO confirm a NORMAL blunt-end click (no primitive armed) still opens the blunt-end action panel (not retarget). | (new, 2026-06-11 — primitive-on-face) | a part with exposed blunt ends (18HB beam / `Examples/26hb_platform_v3.nadoc`) + seed `workspace/Primitives/6hb_primitive.nadoc` | the continuation TOPOLOGY reuses the already-tested `bundle-continuation` route; **the ring-click → retarget → commit GESTURE is now e2e-verified** (`frontend/e2e/primitive_face.spec.js` drives the real raycast: arm → ring-click flips slice to continuation → click grows helix count; zero console errors). **Bug found+fixed this session:** the slice plane's placement pointerdown swallowed the ring click (lattice cell sits at the ring's screen pos) — fixed by yielding the click to the ring pick (`slicePlane` consults `bluntEnds.isRingHit`) when not yet on a face. REMAINING Tier-3 (human-eye only): does the ghost footprint *look* right sitting on the face, snap feel, conflict-red. **Bent/deformed faces now shipped + e2e-verified** (the bent test in `primitive_face.spec.js`: bend the seed helix → ring-click flips slice to continuation+deformed → commit grows helices via `bundle-deformed-continuation`). Only **Circle-disc-on-face** remains deferred (needs new route + tangent-vs-dome decision; toast "not supported yet"). |
| | **MV-CIRCLE** | **Parametric "Circle" primitive gesture/visual**: New Part (empty) — try BOTH a **HONEYCOMB** and a **SQUARE** part → Tools → Add Primitive → **select the "Circle" card** (named "Circle", "DISC" badge, "Disc · variable radius" meta — NO lattice mentioned) → the inline length input is replaced by a **Radius (nm)** box prefilled to ~10.4 (the fit of the saved disc); mode line reads "PLACE PRIMITIVE…". → **move the cursor over the grid** → a translucent ghost **disc** follows, a row of ghost cylinders whose lengths trace a circle, sitting **entirely on the +bp side of the plane, tangent to it** (lowest point touches the plane, NOT straddling it); the **hovered cell is the disc CENTRE / tangent point** (disc centred on the cursor, NOT anchored at its first helix); hovering near existing DNA paints overlapping cells RED. → **edit the Radius box** → the ghost disc grows/shrinks and re-rounds live (more/fewer columns). → **click a clear cell** → the disc is built (N helices, additive — a Feature Log "Place circle" entry appears, revertable) and placement exits. Esc / Cancel tears it down. Confirm the placed structure **reads as a round disc** in 3D **in both lattices** (far more circular than the hand-built `small_circle.nadoc`), that it sits above (not centred on) the plane, and the radius→size mapping feels right. | (new, 2026-06-11 — circle primitive; +lattice-agnostic, +bp-tangent, renamed "Circle") | New Part (empty, HONEYCOMB **and** SQUARE) + seed `workspace/Primitives/small_circle.nadoc` | the geometry (`column_lengths`/`circle_footprint` circularity, even-bp symmetric trim, min-chord cutoff), the **tangent +bp** centred-segment build (revertable, additive, disc tangent to plane), **HONEYCOMB+SQUARE placement**, and the JS↔Python oracle parity are **fully unit/backend-pinned** (`tests/test_circle_primitive.py` 23 tests + `circle_primitive_logic.test.js` 7, same R=10.6 profile); live server path via TestClient. NOT hand-eyeballed: the live ghost-disc follow, the radius-edit re-round, conflict-red, tangency, and whether the placed disc **looks round in both lattices** — Tier-3 human-eye only (server unreachable via localhost under WSL this session) |
| | **MV-PRIM-PLACE** | **Primitive placement gesture/visual**: New Part (empty) → Tools → Add Primitive → **select a 6HB card** → inline plane dropdown (prefilled) + editable length appear under the card AND the lattice slice plane shows; mode line reads "PLACE PRIMITIVE…". → **move the cursor over the honeycomb grid** → a translucent ghost of the 6-helix cross-section follows, its anchor snapping to lattice cells; **confirm it ONLY ever snaps to shape-preserving (same-parity) cells so the ring never distorts into an "I"**, and that hovering near existing DNA paints the overlapping cells RED. → **click a clear cell** → the bundle is built (6 helices, additive — a Feature Log "Extrude segment" entry appears, revertable) and placement exits (one placement per selection). Change the **plane dropdown** → footprint re-arms on the new origin plane; change the **length** → ghost length updates live. Esc / Cancel tears it down. Also try it on a SQUARE part once square primitives exist (any cell valid). | (new, 2026-06-11 — primitive placement) | New Part (empty, HONEYCOMB) + seed `workspace/Primitives/6hb_primitive.nadoc` | the topology (segment-into-empty builds the footprint, revertable, additive), the **parity-snap rule** (`placementPreservesShape`/`validParityCandidates` + congruence), and the live raycast click→commit→parity-preserved were verified this session (unit/backend tests + 2 throwaway e2e probes that drove the real raycast); what's NOT hand-eyeballed is the **ghost-follow smoothness, the snap feel, and the conflict-red appearance** — Tier-3 "looks/feels right", human-eye only |
| | **MV-OXDNA-SEED** | **oxDNA → NAMD seed handoff (Phase 2) live gesture + physics payoff**: load a **sequenced crossover-routed** design (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics → oxDNA panel → run a **Relax** to **completed** (or pick an already-completed job) → the **"Use as NAMD seed"** button (next to Show RMSD) **enables only once status is completed** → click it → status line reads "Building NAMD seed + solvating…" then "NAMD seed job created — open the MD panel to start it" + a toast; a new MD job appears in the **MD panel** with provenance `seed_oxdna_job_id` set → start that MD job and confirm its **minimization shows FEWER startup clashes / lower initial energy than an ideal-B-DNA start on the same design** (§23 task 5 — compare the MD health/metrics first frame seeded-vs-unseeded). A still-running/queued/failed oxDNA job → the seed button stays disabled. | (new, 2026-06-14 — oxDNA Phase 2) | a **completed** oxDNA job for a sequenced crossover-routed design (`workspace/6hb_OxDNA_test.nadoc`; there is already a completed job `9d6f57f02ed0`) **+ a working NAMD install** (this machine had gmx but no NAMD) | the backend handoff is validated on the REAL completed 6hb job — `build_namd_seed('9d6f57f02ed0')` builds an 11,720-atom model from the equil `last_conf` with median cross-pair **backbone 1.60 nm vs raw CM 1.02 nm** (the §18 fix, near B-DNA); unit-pinned (`TestReadBackbonePositions`, `test_build_namd_seed_*`, bad-id→400) + the button is factory-tested (completed→enabled→createMdJob POST with `oxdna_job_id`). NOT done: the live browser click + the actual NAMD minimization clash/energy comparison — needs NAMD installed + the app running. **ALSO (2026-06-14) the Flexibility-map toggle** (below "OxDNA display"): on a job with a finished production run, toggle it on → loading bar → "✓ ready" + the model deforms to the production AVERAGE structure with backbone beads **recoloured viridis rigid→flexible by per-base RMSF** + a min→max nm legend; it's disabled ("Waiting for production") until production finishes and is mutually exclusive with OxDNA display. `production_rmsf` is verified on the real 6012-base job (RMSF 0.25–2.8 nm) + unit/route-pinned + the toggle is factory-tested, but the **on-screen recoloured-bead visual** is human-eye only |
| | **MV-FLEXSIM** | **Flexible ssDNA run follows the oxDNA/MD relaxed frame (was: stale geometric arc)**: load a design with a marked **flexible ssDNA segment** whose run bridges two clusters (e.g. `workspace/mini_hinge.nadoc`, or mark an unpaired scaffold run via right-click → "Mark flexible") → the magenta bowed **flexible arc** (tube + beads + slabs) renders between the arms → assign sequences → Dynamics ▸ oxDNA → **Relax** to completed → toggle **OxDNA display** ON. **PASS:** the flexible run's beads/slabs jump to their **simulated (relaxed ssDNA) positions** — a tube threading the real relaxed conformation between the (now sim-repositioned) rigid anchors — NOT the pre-relax geometric arc left floating. Toggle OFF → the run reverts to the geometric arc. Repeat with **View trajectory** scrub/play (beads track each frame) and the **Flexibility-map** overlay. EDGE: a frame missing some run beads → that connection falls back to the geometric arc (no gap/crash). EDGE: switch jobs / leave the tab → arc restores. | (new, 2026-07-02 — flexible-arc sim-position display, fix #2) | a flexible-segment design (`workspace/mini_hinge.nadoc`) + a **completed** oxDNA relax for it (needs a real GPU run) | the sim-position routing (`flexible_arcs.applySimPositions`: places beads at frame positions, null reverts to arc, missing-bead → arc fallback) is vitest-pinned (`flexible_arcs.test.js` 4) and the display chokepoint forwarding it (`_applyFem`→`onFrame`, null on stop) is pinned (`oxdna_display.test.js`); build + smoke clean. NOT hand-driven: the on-screen bead-follow during a real oxDNA display/trajectory — needs a flexible design + a GPU relax; human-eye only. **Companion to fix #1 (backend seed):** the run is now seeded as a contour-length arc, so display + seed agree |
| | **MV-DELJOBS** | **Delete-file → job-folder cleanup popup**: in the welcome screen Library panel (and the file-browser "Open from Server" modal), click the **×** on a file that HAS MD/oxDNA jobs (e.g. `workspace/3x6Sq_oxDNA.nadoc`, which has 2 MD + 3 oxDNA jobs) → after the normal "Delete file?" confirm, a **second popup** appears: "This file has 2 MD job folders and 3 oxDNA job folders. Also delete them?" with **Delete job folders** / **Keep job folders** buttons. Choosing *Keep* deletes only the file (jobs remain in the Dynamics tab); choosing *Delete* removes the job folders too and shows a toast. Repeat for a **folder** delete (matches every design file under it). EDGE: a file with NO jobs → only the single "Delete file?" confirm (no second popup). EDGE: when a matching job is **running** → the second popup instead warns "Job still running" and only offers "Delete file, keep jobs" / Cancel (running job folders are never deleted; backend also 409s). | (new, 2026-06-16 — delete-file job cleanup) | `workspace/3x6Sq_oxDNA.nadoc` (2 MD + 3 oxDNA jobs, all idle) | the path→job matching (`path_matches`/`find_associated_jobs`), the GET-jobs + DELETE-with-flag routes incl. the running-job 409 block, and the pure dialog helpers (`jobCount`/`jobCleanupSummary`/`runningJobIds`) are unit/route-pinned (`tests/test_job_cleanup.py` 16 + `file_deletion.test.js` 9); GET endpoint verified live against the real workspace. NOT hand-driven: the actual two-step dialog click-through in the running app + the running-job warning branch (needs a live running job) — human-eye only |
| | **MV-OXTRAJ** | **oxDNA continue-production + multi-run flexibility map + trajectory player**: load a sequenced crossover-routed design (`workspace/18hb2.nadoc` / `6hb_OxDNA_test.nadoc`) → Dynamics → oxDNA → select a **completed** job → **(a) Continue production:** click **Start Production**, let it finish, then click **Start Production AGAIN** — a SECOND production run starts and **continues from the last frame** (status reads "Production complete (N runs)…"; backend shows stages `…4_production, 5_production`). **(b) Flexibility map across runs:** with ≥2 production runs done, toggle **Flexibility map (RMSF)** → the recoloured average reflects ALL production runs pooled (not just the first). **(c) View trajectory:** toggle **View trajectory** → controls appear → **▶ play** animates the model through the COMPOSITE trajectory (relaxation stages → production run/s), the **slider scrubs** it, and **markers on the slider** sit at each stage transition (relax→production, production→production; hover shows "→ production (5_production)"). Confirm: the three overlays are mutually exclusive, playback is smooth, scrubbing tracks the model, markers line up with the actual transitions, and leaving the Dynamics tab stops playback. | (new, 2026-06-16 — continue-prod + multi-run RMSF + trajectory player) | a sequenced crossover-routed design with ≥2 production runs (`workspace/18hb2.nadoc`) | the continue-production stage-naming/chaining + `/rmsf` multi-traj pooling + `composite_trajectory` (downsample, keys-once, stage markers) + the player (play-loop/slider/markers via `markerPositions`) + `framesToUpdates` + mutual-exclusivity are unit/route/integration-pinned (`test_oxdna_relaxation.py` +6, `oxdna_trajectory_player.test.js`, `oxdna_display.test.js`, `oxdna_jobs_panel.test.js` +24); smoke clean. NOT hand-driven: the live multi-run GPU gesture + the on-screen play/scrub/marker visual — needs a real run with ≥2 productions; human-eye only |
| | **MV-OXSPIN** | **oxDNA running-job spinners (live animation)**: load a sequenced crossover-routed design → Dynamics → oxDNA panel → click **▶ Relax** → the **Relax button** shows a spinning circular indicator (and "Relaxing…"), and the new **jobs-list row** shows a spinning ring (instead of the static ● dot) while it's queued/preparing/running; finished jobs revert to the coloured dot. Run **Start Production** → the **Production button** spins (Relax does not). **Reload the page mid-run** → on re-open the spinners re-appear from live job state (list row + the matching button) WITHOUT having to click the job; they stop when the job completes/fails. Confirm the animation is smooth (does NOT visibly stutter/restart each ~1.5 s poll). | (new, 2026-06-16 — running-job spinners) | a sequenced crossover-routed design (`workspace/6hb_OxDNA_test.nadoc`) + a real GPU run | the spinner DOM (`.nadoc-spinner` presence on row + the correct button), the state drivers (`jobIsActive`/`isRelaxRunning`/`isProductionRunning`), the reload-safe path (buttons spin from live job state with no selection), and the no-restart list-signature guard are unit/integration-pinned (`oxdna_jobs_panel.test.js` +7); panel boots clean (smoke). NOT hand-driven: the actual on-screen spinning + smoothness during a real run + the live mid-run reload — Tier-3 human-eye only |
| | **MV-OXRESUME** | **oxDNA resume killed job + mid-run flexibility map**: load a sequenced crossover-routed design → Dynamics → oxDNA → click **▶ Relax** (or **Start Production**), then **kill the run** (stop the oxDNA process / restart the backend so the in-memory runner is lost). Re-open the panel → on next fetch the job reconciles to **stopped**, its detail shows a start button reading **"↻ Resume"** (NOT "Start"). Click it → the run **continues from the killed stage's own checkpoint** (`last_conf.dat`) rather than restarting that stage from the relaxed frame; the progress label reads **"Resuming from checkpoint"** (the bar resets to 0 because the resumed run re-runs the stage's full step budget, but it continues from the checkpoint STATE). Confirm the killed run's `trajectory.dat` is **archived to `trajectory.r1.dat`** (not truncated to 0) and the **flexibility map still pools those frames** after the resume completes. Separately, with a production run **still running** (not yet done), toggle **Flexibility map (RMSF)** → it is now **enabled mid-run** (was greyed "waiting for production") and the status reads **"N frames pooled · est. RMSF error ±X% · ⚠ Preliminary — production still running"**; as more frames accrue, toggling off/on lowers the error %. After production completes with many frames, the ⚠ Preliminary warning is gone. | (new, 2026-06-17 — resume + mid-run flex map) | a sequenced crossover-routed design (`workspace/18hb2.nadoc` / `6hb_OxDNA_test.nadoc`) + a real GPU run that can be killed mid-stage | the resume conf-selection (`_starting_conf` continue-from-own-checkpoint), the rmsf mid-run gate (done OR running stages with frames), and the confidence metric (`rmsf_confidence` 1/√(2N) + preliminary threshold) are unit/route-pinned (`test_oxdna_relaxation.py` +3: `_starting_conf`, mid-run rmsf availability, `rmsf_confidence`); the button label (`startButtonLabel`/`isResumable`), mid-run flex unlock, and confidence readout (`flexConfidenceText`) are pinned (`oxdna_jobs_panel.test.js` + `oxdna_display.test.js`). NOT hand-driven: the live kill→Resume gesture (continuing from checkpoint) + the on-screen preliminary-confidence readout during a real running production — needs a real GPU run that gets killed; human-eye only |
| | **MV-OXSEQ** | **oxDNA undefined-sequence guard popup**: load a design that still has **undefined bases** — either fully unsequenced (e.g. `workspace/Primitives/6hb_primitive.nadoc`) or **partially** sequenced (assign a scaffold but leave staples as 'N') → Dynamics tab → oxDNA panel → click **▶ Relax** → a **warning popup** appears ("Finish assigning sequences", ⚠, with the count of undefined bases) and **no job is created** (the jobs list does not grow); dismiss via OK / ✕ / click-outside → the panel status reads "Relaxation blocked — finish assigning sequences". THEN fully assign sequences (scaffold + staples) → Relax → the job is created normally (no popup). EDGE: a design whose ONLY undefined bases are on `is_reference` backdrop strands should NOT be blocked. | (new, 2026-06-16 — oxDNA undefined-base guard) | a partially-sequenced crossover-routed design + `workspace/Primitives/6hb_primitive.nadoc` (all-N) | the backend block (`count_undefined_bases` exact count incl. loop copies + reference exclusion; route 400 for all-N AND partial) and the popup-trigger detection (`isUndefinedSequenceError`) + modal DOM are unit-pinned (`tests/test_oxdna_relaxation.py` +5, `sequence_warning_modal.test.js` 5); NOT hand-driven: the live Relax-click → popup-appears → no-job-created gesture (the backend rejects, the panel shows the modal) — human-eye only |
| | **MV-EFIELD** | **oxDNA E-field setup — direction/magnitude gizmo + anchor picker (live gesture/visual)**: load a sequenced design (e.g. `workspace/6hb_OxDNA_test.nadoc`) → Dynamics → oxDNA panel → expand the **"Electric field"** sub-section → a blue **arrow gizmo** appears in the scene (points +Y, short = ~0 field). **(a) Magnitude+direction by drag:** grab the arrow's **tip handle** and drag → the arrow reorients (direction) and grows/shrinks (magnitude), OrbitControls are suppressed mid-drag, and the **Force/nt (pN)** box + **Direction x/y/z** boxes update live. **(b) By typing:** set the pN box → arrow length updates; edit the dir boxes → arrow reorients. **(c) V/m helper:** expand "from field (V/m)", enter e.g. 1e6 V/m + q_eff 0.25 → **Compute pN** fills ~0.04 pN. **(d) Anchors:** select an **overhang** (or domain/cluster) → **Add selection as anchor** → a chip appears; the readout flips to **"Ready · … pN/nt · N anchors"** only once force>0 + a direction + ≥1 anchor are all set (else "Not ready — …add ≥1 anchor (else the field just drifts the whole structure)"); chip ✕ + **Clear** remove anchors. Collapsing the section or leaving the Dynamics tab hides the gizmo. Confirm the arrow reads as direction+magnitude, the drag feels right, and the anchor chips reflect the selection. **(e) Anchor-tension colour + scaling (UPDATED 2026-06-18 — was raw per-nt):** the arrow colour and drag-range now grade **anchor-bond tension `T = (force/nt) × n_total/n_anchored`** (the quantity that actually blew up a VoltronCore field run — the net field force funnels through the held bonds), NOT raw per-nt force. So on a **big, lightly-anchored** structure (VoltronCore 14774 nt + a 36-nt domain anchor → 410× amplification) even a *small-looking* arrow turns **red** — a moderate gesture is correctly flagged destructive. Verify: with a completed relaxed job + ≥1 anchor selected, drag the arrow / raise pN → colour **blue (negligible) → green (T<50 pN/bond) → red (T≥243 pN/bond, the field-stage FENE-divergence threshold)**; the **full arrow length ≈ the disrupt force for THIS design** (on VoltronCore the arrow saturates by ~0.6 pN/nt, not 14.5); the readout shows **"net X nN / N anch"** and warns **"⚠ will blow up this run (anchor bond ~Y pN > 243)"** in red (or "straining anchors …" amber). Adding **more anchors** (re-resolved live via `POST …/field/anchor-preview`) lowers tension → colour shifts back toward green. With **no job / no anchors yet** it falls back to the old per-nt heuristic. *(amplification+zone+colour+scale math unit-pinned in `efield_math.test.js` incl. the 2.227 pN/nt→disrupt VoltronCore repro; preview route backend-pinned `test_field_anchor_preview_route`; backend route exercised live via curl against job 12ffe1e34028. **Un-hand-checked = the live colour/scale gesture against a loaded design in the browser.**)* **(f) Run field as a branch:** with a field set (force+dir+≥1 anchor) AND a **completed RELAXED** job selected, **"⚡ Run field"** enables; click it → a **child** field job is spawned from that relaxed structure and appears in the list as an **indented numbered "⚡ Field N" sub-item** under the parent (electric bolt icon; hover shows pN · dir · n_anchored). Run it again → "Field 2", etc. — multiple independent field branches off ONE relaxed job. Selecting a sub-item shows its field-deflected result via OxDNA display. A field sub-item itself is NOT branchable (readout asks for the parent). **(g) Cascade delete:** select a relaxed parent that has ≥1 field sub-item → **Delete** → a confirm dialog warns "…has N electric-field runs … deleting removes all N" (Continue/Cancel); confirming removes the parent AND every sub-item; cancelling leaves everything. A field sub-item / childless job gets a plain permanent-delete warning. (A running child blocks parent deletion with a 400.) | (new, 2026-06-18 — oxDNA E-field, full stack) | a sequenced design + a **completed** oxDNA relaxation job for it + an overhang/domain/cluster to anchor (`workspace/6hb_OxDNA_test.nadoc`) | the math core (`efield_math.js`), gizmo (`efield_gizmo.js`), and panel factory incl. the Run-field handoff (`efield_setup.test.js`: run disabled until ready+completed-job, POSTs the spec, no-op when not ready) are unit-pinned; the backend stage + oracle + full headless pipeline are backend-pinned with a deflecting mock (`run_field_validation`: anchor holds, rest deflects ALONG field, deflection∝magnitude); panel boots clean (smoke). NOT hand-driven: the live tip-drag gesture (pixel-precision raycast), the arrow/anchor on-screen appearance, and the live Run-field click→stage-appears against a real GPU job — Tier-3 / needs-GPU, human-eye only. |
| | **MV-PHOTO-1** | **Photo-mode tone-mapping + no-tint regression + Sun-sole (GPU-pixel visual)**: load a representative design (e.g. `Examples/26hb_platform_v3.nadoc`) → open the **Photo** tab in the left sidebar (enters photo mode — there is no keyboard shortcut) → stack the worst case: Environment **Room** + Post-processing **Bloom** ON + Materials **full=Metallic** + Fluorophores **emissive** at high intensity. **(a)** Confirm the frame is NOT washed by a large uniform **yellow/purple** region and is not black (the R1 tone-mapping fix) — drag the new **Exposure** slider (Post-processing) and watch highlights roll off instead of clipping. **(b) Sun = truly sole:** toggle **Sun** on → ONLY the sun lights the geometry — the preset rig, the **HDRI/Room image-based reflections** (metallic goes dark — expected), AND the **fluorophore point-lights** all drop; toggle off → all three return. **(c)** Export a PNG and confirm it matches the preview (filmic look, no wash). | (new, 2026-06-18 — R1 tone mapping + R2 sun-sole) | `Examples/26hb_platform_v3.nadoc` (or any fluorophore-bearing design for the emissive part) | the setter→object wiring (renderer.toneMapping/exposure, rig `.visible`, fluoro PointLight count, IBL `scene.environment`→null under sun) is unit-pinned in `frontend/src/scene/photo_renderer.test.js` (P-A), but the **rendered pixels** — whether the wash is actually gone, the tone-map *look*, the metallic-under-sun darkening, the export parity — need real WebGL2 (jsdom has none); Tier-3 human-eye / GPU-only |
| | **MV-PHOTO-2** | **Photo-mode mid-session state-churn — no garbage frame (R3 env re-bake + R4 mirror floor)**: (a) in photo mode with **Bloom ON**, change the **Environment** dropdown repeatedly (Off → Room → Off → a `.hdr` File → Room…) and watch for any transient **coloured garbage frame** / flash on the bake (R3 added a one-shot `resetState()` right after the mid-session PMREM re-bake). (b) Turn on a **Floor** with material **Mirror** AND **Bloom** + **Metallic** materials → confirm the mirror reflection renders without inducing a bloom/PBR-env desync flash in the rest of the frame (R4 wraps the Reflector's `onBeforeRender` to flush state after its nested render). Confirm each settles cleanly, metallic reflections update, and the mirror image is correct. | (new, 2026-06-18 — R3 env re-bake + R4 Reflector isolation) | any design in photo mode + a local `.hdr` for the File case + a Mirror floor for (b) | the re-bake/`resetState` placement (R3) + the Reflector `onBeforeRender` wrap (R4, pinned in `floor.test.js`) are code-level; whether a transient garbage frame actually appears on a real GPU is a per-frame timing/visual check no headless test can observe — human-eye only |
| | **MV-EXT** | **Extrude-tool sidebar rework — live cell-pick build + blunt-end continuation**: New Part → Tools → Extrude → on the slice plane, **click lattice cells** (count in the `#extrude-panel` header updates live; **length defaults to 0 so NO ghost cylinders appear while selecting**; canvas clicks do NOT close the panel) → the **scaffold-length recommendations** (7249 / 8064 chips) appear and clicking one sets the length → **once a non-zero length is set the translucent ghost cylinders appear** (length-gated) → **Extrude builds the bundle** (panel hides, helices render); an Extrude at length 0 is blocked with a toast. THEN on a populated design, **right-click a blunt end → Extrude** → the SAME `#extrude-panel` opens with the "Extrude from" dropdown **disabled** showing the locked plane → Extrude extends the strand. Also: `±` direction, Both/Scaffold/Staples filter, Ligate-adjacent still drive the build; an overlap extrude is blocked with a toast (no red ghost anymore). | (new, this session — extrude entry rework) | New Part (empty) for new-bundle; `Examples/26hb_platform_v3.nadoc` for the blunt-end continuation | panel/dropdown/slice-plane/teardown + new-bundle activate are unit- + e2e-pinned (incl. e2e probes that select a cell and assert: 7249/8064 recs render; length defaults to 0 with zero ghosts; a 0-length extrude is blocked; entering 42 makes the ghost appear; the build completes), but the **lattice cell-pick gesture feel** and the **blunt-end ring right-click → panel** are WebGL raycasts at pixel precision (human-eye only); extrude API/topology unchanged and already covered |

| | **MV-OX-PROT** | **Proteins in an oxDNA (DNANM) hybrid relax + display**: build/load a design with a **conjugated protein** (Conjugate Manager → attach a protein to an overhang, which creates the OH_BINDER handle strand) on a **sequenced crossover-routed** design → Dynamics → oxDNA panel → **▶ Relax**. The job must run on the **ANM-oxDNA fork** (`OXDNA_ANM_BIN` / `~/anm-oxdna/oxDNA/build_cuda/bin/oxDNA`), NOT mainline. Confirm it reaches **completed** (the hybrid topology/conf/par + protein traps loaded; the protein is a near-rigid ANM body with excluded-volume sterics + a mutual_trap tether to the handle). Toggle **OxDNA display** → the DNA deforms to relaxed positions AND **each protein moves to its relaxed pose tracking the DNA** (not left floating at its design pose); toggle off → proteins return to design pose. Also a **free** (un-conjugated) protein should stay near its anchored spot (positional trap), not drift off. Check the health cards show a sane bp-retention % (geometric metric, hybrid-aware). | (new, 2026-06-19 — proteins in oxDNA, Phases 1–4) | a sequenced crossover-routed design + an imported protein conjugated to an overhang; the built fork binary | the ENTIRE backend is unit + REAL-FORK pinned: file writers (`test_oxdna_protein.py` 27 — hybrid topology/conf/par, traps, binder-terminus, protein-first index offset), `prepare_oxdna_job` hybrid branch + a **prepared job run on the fork** (`test_prepared_hybrid_job_runs_on_fork`), the hybrid-aware conf readers (DNA-only byte-identical), and the display transform by **Kabsch recovery** (known 90°+trans → 1e-4); frontend `applyOxdnaTransforms` + `proteinTransformMap` unit-pinned (`oxdna_display.test.js`), vite build + smoke clean. NOT hand-driven: the live browser gesture — a real GPU hybrid relax + the **on-screen protein-follows-relaxation visual** when toggling OxDNA display — needs a conjugated design + a GPU run; human-eye only |
| | **MV-MD-PROT** | **Proteins in an all-atom NAMD/GROMACS package** (Part B / MD): load a design with a **conjugated protein** (Conjugate Manager → attach to an overhang → OH_BINDER handle) → export the **NAMD package** (and/or GROMACS) → unzip + run on a workstation (NAMD3 + psfgen are installed at `~/Applications/NAMD_3.0.2_*/`). Confirm: the PDB/PSF include the protein as its own CHARMM36m segment (psfgen built peptide angles/dihedrals + guesscoord hydrogens), `namd.conf` references `forcefield/par_all36m_prot.prm` + `extraBonds on`/`extrabonds.txt`, and the run is stable. Across an **ionic-strength sweep**, the protein should hold its fold (the Cα elastic network in `extrabonds.txt`) and stay tethered to the DNA handle (the click-linker extraBond), while the DNA relaxes. GROMACS protein jobs must pick **charmm36** (AMBER blocked). | (new, 2026-06-19 — Part B / MD) | a conjugated-protein design (sequenced); NAMD3 + psfgen (installed) / GROMACS | the file generation is fully unit-pinned (`test_protein_md.py` 13 — atoms appended w/ chain/count/world-pose, ENM cutoff/symmetry/ref-lengths/text-order, click linker via the shared `binder_terminus_nuc_key`, FF files, namd.conf, psfgen segment) AND a **REAL psfgen build** of a combined DNA+protein PSF passes the charge audit; end-to-end NAMD-package ZIP (FF bundle + extrabonds + protein-residue PSF) asserted in-process. NOT hand-driven: the live multi-ns NAMD/GROMACS **trajectory** — fold-holds-across-salt + linker-tether visual — needs a real run + human eye |
| | **MV-OXLIVEFIELD** | **Ephemeral "Live" oxDNA field mode — in-browser start + gizmo-steer + mutual-exclusion (live gesture/visual)**: load a **sequenced** crossover-routed design (`workspace/6hb_OxDNA_test.nadoc`) → Dynamics ▸ oxDNA → **▶ Relax** to completion → select that completed relaxed job → expand **Electric field**, optionally set a force (≈4 pN/nt) + a direction + anchors (Live now **composes any combination** of E-field / hard surface / anchors, like the **Full Sim** run — only a field requires ≥1 anchor; anchors-only / surface-only / nothing=free dynamics are all allowed). **(a) Start:** the **◉ Live** button (between ▶ Relax and **Full Sim**, the renamed "Start Production") is enabled (purple); click it → an ephemeral session starts (the live-status line reads "Live session running — drag the field to steer."), and the NADOC model deforms to the running structure, **stepping forward every ~0.5 s** as bursts land — NO new job appears in the jobs list and no frames are persisted (purely ephemeral). **(b) Steer:** drag the field **arrow gizmo's tip** (or edit the pN/dir boxes) → the running structure **re-aims toward the new field within a second or two** (best-effort). **(c) Mutual exclusion + toggle protection:** while Live runs, the **OxDNA display / Flexibility map / View trajectory** toggles are **disabled** (greyed, tooltip "Stop Live to use …") so a click can't create a display conflict; starting Live also clears any of those overlays that were on. Click **▶ Relax** OR **Full Sim** → Live turns OFF (button back to "◉ Live") and the session stops; the three toggles re-enable the instant Live stops (explicit Stop, Relax/Full Sim, tab switch, or session death). **(d) Disabled states:** with no completed relaxed job selected, or a field/production CHILD selected, the Live button is disabled with a tooltip; if oxpy lacks the F0/dir patch it's disabled "oxpy missing patch". Starting with no field or no anchor shows a warning toast + status, no session. **(e) Teardown:** clicking ■ Stop Live (or leaving the Dynamics tab / switching design) stops the session and restores the model. | (new, 2026-06-23 — ephemeral live oxpy field mode) | a **sequenced** design + a **completed** oxDNA relaxation for it + an overhang/domain/cluster to anchor (`workspace/6hb_OxDNA_test.nadoc`); needs the patched oxpy build | the worker (burst loop / pending-field / latest-frame / teardown) is unit-pinned GPU-free (`test_oxdna_live_session.py` 12); the controller's observable contract — button enable/tooltip, start POST body, refuse-without-field/anchor, live re-aim push, stop+restore — is vitest-pinned (`oxdna_live_controller.test.js`); `displayLiveFrame` pinned in `oxdna_display.test.js`; **the REAL oxpy live ROUTE is verified end-to-end on this box** (start → 504-position frame → live re-aim advances bursts → stop → rundir cleaned); vite build + smoke clean. **NOT hand-driven**: the in-browser button→gizmo-drag→structure-follows-live visual + the Relax/Production-stops-Live gesture (Tier-3 / needs a real relaxed GPU job + WebGL drag) — human-eye only |
| | **MV-OXALIGN** | **oxDNA "Align to design pose" for CHILD jobs + Dynamics settings reset on design switch**: load a sequenced crossover-routed design (`workspace/6hb_sim_tests.nadoc`) → Dynamics → oxDNA → run a **Relax** to completion → **Start Production** (creates a production CHILD job). Select the production child → toggle **OxDNA display** ON with **"Align to design pose"** ON → the relaxed structure must overlay the **design pose** (NOT appear far below/away from it — the pre-fix bug, where the child aligned to its own drifted `conf.dat`). Same check for an **E-field / View-trajectory / Flexibility-map** overlay on a child job. **(b) Settings reset:** set non-default values in the **oxDNA Advanced** card (salt/steps/bp-gate) and the **MD (NAMD) Advanced** card, enable the E-field/Hard-surface/Anchors cards → then **close the design and open a different one** (or switch parts) → all those controls **reset to their index.html defaults** (E-field/surface/anchor cards turn off, advanced inputs revert, device/threads re-seed from the host recommendation). | (new, 2026-06-20 — this session) | `workspace/6hb_sim_tests.nadoc` (has a relax + production child); any 2nd design to switch to | the alignment fix is confirmed NUMERICALLY on the live running child job `a3b6956bc4b2` (aligned centroid `[3.9,2.3,14]` = design pose, was `[32,-156,14]`); backend `_design_ref_conf` is now the reference for display/rmsf/trajectory (`just test` 2855 green). The reset uses `form_defaults.js` (7 vitest) wired into both panels' `nadoc:workspace-path-change`. **NOT hand-driven**: the live in-app display overlay landing on the design + the visible card/input reset on a real design switch — human-eye only |

---

## GENERATED (manual ops ready — run these, then report pass/fail)

### MV-5 — Assembly right-click context menu (part / linker / belt)
*Discharges fix #69 — the assembly right-click router moved onto the shared
`createContextMenu` primitive. The router (`assembly_pointer.js`
`onAssemblyContextMenu`, line 575) and the part menu builder
(`ui/assembly_context_menu.js`) have unit coverage for the item set and gating, but
the **live WebGL right-click that decides part vs linker vs belt, the right-drag pan-
suppress, and the enabled/disabled linker-relax state** are the never-hand-driven
parts.*

**Why this needs a human.** The unit tests pin which items the part menu builds and
the multi-select/group gating, but they cannot touch: the **raycast priority chain**
that decides whether your right-click resolves to a *linker*, a *belt path*, or a
*part instance* (and routes to a different menu for each), the **5-pixel pan-suppress**
that must let a right-*drag* orbit/pan without ever popping a menu, and the
**linker-relax enabled-vs-disabled** state that depends on a backend availability
call. A break in any of those passes every automated gate.

**Routing facts (so you know where to click) — `assembly_pointer.js:575-620`:**
- Right-click fires `onAssemblyContextMenu`. It first reads the right-button-down
  position: if the pointer moved **> 5 px** (squared dist > 25) since right-button-
  down, it treats the gesture as a **right-drag pan** and **suppresses the menu
  entirely** (line 584-586). No browser menu appears either (`preventDefault`).
- The hit is resolved in a **priority chain**, first match wins:
  1. **Overhang arrow** (only if the overhang tool's locations are visible) → handled
     by `selection_manager`, the part menu is skipped (line 591-595).
  2. **Linker** (`pickLinker` — complement/bridge beads or the connector arc) →
     **Linker menu** (line 598-599).
  3. **Belt path** (`pickBeltAt`) → **Belt menu** (line 603-609).
  4. **Part instance** (`pickInstance`) → selects it (`activeInstanceId = inst.id`,
     line 618) **and** opens the **part context menu** (line 619).
- **Linker menu** (`showAssemblyLinkerMenu`, line 548): header `Linker · {name}`, one
  item **"Relax linker"**. It is **disabled** when the backend
  `getAssemblyOverhangConnectionRelaxStatus(connId)` returns `available === false`
  (ds-only; needs a movable free part); when disabled, a second header line shows the
  **reason** (line 570).
- **Belt menu** (line 605): header `Belt path`, one item **"Attach part to belt"**.
- **Part menu** (`ui/assembly_context_menu.js`): header = part name; a **Repr**
  dropdown (Full/Beads/Cylinders/Hull Prism/VDW/Ball+Stick); **Move / Rotate**;
  **Define Connector**; **Fixed (anchored)** toggle; **Allow Part Joints** toggle;
  conditional **Show/Hide**, **Edit Part…**, **Duplicate**, **Polymerize…**;
  **Group (N parts)** *only when a multi-select exists*; **Ungroup** *only when the
  part is in a group*; danger-styled **Delete**.
- **Dismiss:** outside `pointerdown` or **Escape** → `hide()`; the dismiss listeners
  are bound on a `setTimeout(…,0)` so the originating right-click doesn't instantly
  close the menu. Exiting the assembly calls `hide()`.

**SETUP**
1. Start both servers (`just dev` + `just frontend`), open `http://localhost:5173`,
   keep devtools console open. This is an **assembly-mode** block.
2. For the **linker-relax** cases: File → Open → **`workspace/Linker_Assem_test.nass`**
   (verified present — a cross-part linker assembly). For the **attach-to-belt** case:
   File → Open → **`workspace/Belt_test1.nass`** (verified — 62 instances + 2 groups +
   1 belt). You'll do the part-menu + pan-suppress cases on either.
3. Wait for parts to render; zoom/orbit so an individual part, the linker beads/arc,
   and (in Belt_test1) the belt tube are each comfortably clickable.

**MAIN CASES**
1. **Right-click a bare part → part menu + selects it.** Right-click a part instance
   that is *not* on a linker or belt. Expect: the part is **selected** (its white
   per-instance outline appears) AND a `.context-menu` opens at the cursor with the
   header = the part's name and the item set listed in routing facts (Repr dropdown,
   Move / Rotate, Define Connector, Fixed, Allow Part Joints, … Delete).
2. **Right-click a part in a group → Ungroup appears.** In Belt_test1, right-click a
   part that belongs to a group. Expect the menu to include **Ungroup** (it is omitted
   for an ungrouped part).
3. **Multi-select then right-click → Group (N parts).** Ctrl-lasso ≥2 instances (the
   MV-4 gesture), then right-click one of them. Expect the menu to include
   **Group (N parts)** with N = the multi-selected count.
4. **Right-click the linker → Relax menu (ENABLED).** In Linker_Assem_test, right-click
   directly on the linker (a complement/bridge bead or the connector arc between the two
   parts). Expect a small menu: header **`Linker · {name}`** + an **enabled**
   **"Relax linker"** item. Click it → a toast "Relaxed linker — free part moved into a
   coaxial native-length duplex." and the free part rigid-translates into a coaxial
   duplex.
5. **Right-click the belt → Attach menu.** In Belt_test1, right-click on the **belt
   tube**. Expect a menu: header **`Belt path`** + **"Attach part to belt"**. (You can
   stop at confirming the menu + that clicking it begins the attach flow; the full
   attach pick is a separate feature.)
6. **Pan-suppress: right-DRAG never opens a menu.** Press right mouse button on a part
   and **drag** (> a few px) to pan/orbit the camera, then release. Expect: the camera
   pans/orbits and **NO context menu appears** on release.

**EDGE CASES**
1. **Linker-relax DISABLED state.** If the linker in the fixture is **not** relax-
   eligible (not ds, or no movable free part), the **"Relax linker"** item must be
   **greyed/disabled** and a second header line shows the **reason** (e.g. "Relax
   unavailable"). Confirm a disabled item does nothing when clicked. *(If
   Linker_Assem_test's linker is eligible and always enabled, note that and flag that a
   disabled-case fixture is missing.)*
2. **Outside-click dismiss.** Open any of the three menus, then left-click elsewhere
   (empty space or another part). Expect: the menu **closes** cleanly (and an
   outside-click that lands on a part also performs that part's normal selection).
3. **Escape dismiss.** Open a menu, press **Escape**. Expect: the menu closes.
4. **Originating click doesn't self-dismiss.** Confirm the menu actually **stays open**
   after the right-click that spawned it (the deferred-listener fix) — it must not flash
   open-and-immediately-closed.
5. **Right-click empty space → nothing.** Right-click on empty background (no part,
   linker, or belt under the cursor). Expect: **no menu**, no error (`pickInstance`
   returns null → early return).
6. **Pending-edit commit on re-target.** With a Move/Rotate (or other) edit pending on
   part A, right-click part **B**. Expect: the pending edit on A **commits** first
   (`commitAssemblyPending`), B becomes active, B's menu opens — no lost edit, no error.
7. **Assembly-exit teardown.** Open a menu, then exit the assembly (close doc / main
   menu / open a design). Expect: the menu is hidden, **no console error**, no orphaned
   `.context-menu` node left in the DOM.

**PASS CRITERIA**
- A right-click resolves to the **correct** target under the cursor: linker → Linker
  menu, belt → Belt menu, bare part → part menu (and selects the part). The priority
  chain never shows the wrong menu (e.g. a part menu on top of a linker).
- "Relax linker" is **enabled** when relax-eligible and **disabled with a reason** when
  not; clicking the enabled item relaxes (toast + free part moves).
- "Attach part to belt" appears on a belt right-click.
- The part menu's **Group / Ungroup** items obey the multi-select / in-a-group gating.
- A right-**drag** pans/orbits and **never** opens a menu (5 px threshold).
- Outside-click and Escape both dismiss; the spawning click does not self-dismiss;
  exiting the assembly leaves no orphan node and throws no error.
- No console errors at any step.

**WATCH FOR (suspect behaviors — confirm or deny)**
- **Pan-suppress threshold feel.** The cutoff is a fixed **5 px**. A *deliberate*
  micro-nudge right-click (< 5 px jitter) should still open the menu; a real pan should
  not. Confirm a normal "I didn't mean to drag" hand-tremor right-click still pops the
  menu (i.e. the threshold isn't so tight that ordinary clicks get eaten).
- **Linker vs part priority on overlap.** Where a linker bead sits directly in front of
  a part, the linker wins the chain (it's checked first). Confirm right-clicking the
  *part body* (away from the linker beads/arc) still gives the **part** menu, not the
  linker menu — i.e. the linker pick isn't grabbing too wide a screen radius.
- **Belt pick precision.** `pickBeltAt` uses the event directly (screen-proximity to the
  belt tube). On a foreshortened belt or where it overlaps a part, confirm the intended
  target wins and you can still reach the part underneath.
- **Relax-status call failure.** The relax-status fetch is wrapped in try/catch and
  *treats a failure as available* (line 552). If the backend is slow/erroring, the
  "Relax linker" item may show **enabled** but then fail on click with an error toast.
  If you see that, note it — it's a known soft-fail, not a crash.
- **Active-instance side effect.** A right-click on a part sets `activeInstanceId`
  *before* the menu opens (line 618), so right-clicking a part **changes the selection**
  even if you dismiss the menu without choosing anything. Confirm that's acceptable (it
  mirrors most apps) and that dismissing doesn't leave a half-state.

---

## VALIDATED (user-confirmed pass)

### MV-20 — oxDNA "Graphs and Metrics" card ✅ VALIDATED 2026-07-02
Twist / curvature / base-pairing graphs (spatial + temporal) over a job or its
parent/child lineage, with per-metric Generate/Display/Export + ETA loading bars,
in the oxDNA Dynamics panel (collapsible, last card; Export-ZIP button below it).
User confirmed the card in the running app. Wiring + collapse + card ordering +
real-canvas draw are e2e-covered (`e2e/oxdna_metrics_card.spec.js`); compute is
route + unit-pinned. See [[project_oxdna_metrics_card]].

### MV-6 — Belt polymerize ✅ VALIDATED 2026-06-07
Discharges #32. User manually validated the belt-polymerize gesture: a built belt
assembly → Polymerize along the belt path → evenly-spaced copies render along the tube.
(Marked complete directly by the user; no GENERATED block was required.)

### MV-7 — Coalesced assembly part-refresh ✅ VALIDATED 2026-06-07
Discharges #38. User manually validated that editing a part in part-context with a
save burst refreshes the shared instances **once** (coalesced), not per-instance.
(Marked complete directly by the user; no GENERATED block was required.)

### MV-4 — Assembly multi-select union box (parts + groups) ✅ VALIDATED 2026-06-07
Discharges extraction #34 (`scene/assembly_multi_box.js`, factory `initAssemblyMultiBox`;
pure union math `selection_bbox.js instanceUnionBox`). Confirmed live by the user during
assembly testing: Ctrl-lasso ≥2 instances → one **purple** union box around the union;
exactly 1 → **white**; group select → purple box around all members; plain click clears;
the box re-fits during a group-gizmo drag and survives the commit; assembly-exit teardown
throws no console error (the original #34 `const`-reassignment TypeError is gone). **The
live Ctrl-lasso / group multi-select gesture for parts + groups is now hand-driven.**

### MV-LNK — Assembly cross-part linker completion ✅ VALIDATED 2026-06-07
Pushed + validated same session (was never a mined PENDING row — it's this session's
assembly-linker feature work, shipped with "NOT hand-driven" caveats, then hand-checked
live by the user: "everything appears to work as desired"). Covers, in the **shared
assembly renderer** (default):

- **Indirect (zero-length ss) linkers render** — previously a `length_value==0` linker
  produced NO topology; now a single ss strand `[comp_a, comp_b]` (each overhang's
  binding-domain complement, no bridge) with the `comp_a→comp_b` backbone jump drawn as
  the connector **arc**. ss linker arcs now render in assemblies generally (was ds-only).
  ([[assembly-overhang-bindings]] "Indirect (zero-length) linkers"; pure arc module
  `scene/assembly_connector_arcs.js`.)
- **ss linker relaxation for 0-length (indirect) linkers** — single-translation rigid
  placement that collapses the lone complement↔complement arc (analog of the ds
  two-translation relax). Right-click "Relax linker" + popup Relax button both enabled
  for indirect via relax-status. ([[assembly-linker-relax]] "Indirect (zero-length ss)
  relax".)
- **Linkers follow part moves** — overhang labels follow a moved part (last turn's
  world-space-sprite fix), and after a relax the binding domains + arcs + any OTHER
  linker sharing the moved parts now refresh immediately (no rep-toggle needed): the
  transform-only fast path in `main.js` now calls `rebuildLinkers` when a linker-bearing
  part moved.
- **Delete linker** — right-click a linker → "Delete linker" (red); cascades to the 3D
  view, the strand spreadsheet, and the Overhangs Manager listing.

Backend pins: `test_assembly_overhang_bindings.py` (indirect topology + geometry endpoint),
`test_assembly_linker_relax.py` (3 new indirect-relax tests). Frontend: `assembly_connector_arcs.test.js`.

### MV-1 — Design-mode cluster Move / Rotate tool ✅ VALIDATED 2026-06-07
Discharges extractions #71 #78 #79 #80 #81 (the LESSONS H7 "cluster-gizmo 3D-drag
commit not hand-driven" caveat for the whole design-mode Translate/Rotate band).
Confirmed live by the user across all MAIN + EDGE cases: activate via right-click,
gizmo translate/rotate, ✓ commit (drag and panel-input), Escape-reverts, pivot
selection, re-edit-in-place, stale-op guard, and the Three-Layer spot-check.

Two UX gaps were found *during* validation and shipped the same session:

1. **Unified cluster selection** — clicking a cluster with the cluster selection
   filter active and clicking its row in the **Dynamics → Movable clusters** list now
   drive ONE selected-cluster state: same green glow (`0x3fb950`) + 1.3× bead scale +
   cluster `selectedObject` + sidebar row highlight. selection_manager owns the commit
   (`_applyClusterSelection` + exported `selectCluster`); a thin main.js subscriber
   mirrors the cluster `selectedObject` onto `activeClusterId`. The blue
   `clusterGlowLayer` is now reserved for the Move/Rotate tool's active cluster.
2. **Earlier Move/Rotate ops are independently editable** — the old "Edit blocked: a
   later move/rotate exists" guard (frontend toast + backend 409) was removed. Each
   `cluster_op` stores the cluster's ABSOLUTE pose after that step and the live pose is
   the LAST op for that cluster, so editing an earlier op (A1→A2) rewrites only that
   step's seek/scrub frame while the latest op keeps defining the final pose (B1).
   Backend: `_edit_cluster_op_feature` recomputes the live transform from the last op.

Backend `just test` 1747 passed (2 new cluster_op-edit pins); frontend
`just test-frontend` 1090 passed (2 new tool pins + the selection-unify wiring);
`just smoke` 23/23. Live-exercised: sidebar↔3D selection parity, and edit-earlier-op
→ seek-to-step → cancel → seek-back-to-latest, all zero console errors.

### MV-2 — Overhang Orientation panel + migrated context menu ✅ VALIDATED 2026-06-07
Discharges extraction #64 (`ui/overhang_orientation_panel.js`) and fix #7 (the
right-click menu on the shared `createContextMenu` primitive). Confirmed live by the
user across the GENERATED block: Edit Orientation opens the panel + rotate-only gizmo,
step/drag/typed previews are instant and client-side, Apply commits one
`overhang_rotation` Feature Log entry, single-vs-multi menu gating, and auto-close on
structural change. **The live WebGL raycast → gizmo → preview gesture is now
hand-driven and validated.**

Five bugs were found *during* validation and fixed the same session:

1. **Dual context menu on an overhang bead.** An overhang's free tip is "unpaired", so
   a right-click on its bead opened the short *flexible-segment* menu instead of the
   overhang menu — the unpaired-bead check ran first. Fixed in `selection_manager.js`:
   an overhang-bead right-click (`hitBead.nuc.overhang_id != null`) routes to
   `onOverhangRightClick`, matching the cone-on-overhang dispatch.
2. **Reset jumped the gizmo to the wrong location.** The in-panel **Reset** re-attached
   the gizmo without the cached pivot, falling back to `anchor.pivot` (`[0,0,0]` for
   inline overhangs). Fixed: Reset (and Apply) re-attach with
   `_ooPivotPositions[_ooRightClickedId]`.
3. **Reset → Cancel still left a Feature Log entry.** Reset committed to the server
   immediately. Reworked to a **client-side preview** (per-overhang baseline → identity,
   tracked via `_ooBaseRotations`): Reset→Apply commits identity; Reset→Cancel reverts
   with no entry.
4. **Multi-overhang menu pixel-dependence.** The multi-overhang divert was gated
   `!hitCone`, so a right-click on an overhang's terminal cone fell through to a
   single-id dispatch. Fixed: the divert fires for any overhang hit (cone OR frontmost
   bead) and dispatches the full `_multiOverhangIds` set.
5. **Flexible scaffold run rendered rigid after Generate OH binder → undo.** The binder
   add/remove undo routed `_design_replace_response` through the compact geometry form,
   omitting the per-bead `is_flexible_segment` flag. Fixed in `crud.py`: ship per-nuc
   geometry whenever the diff changed the marks OR the target design has flexible
   connections.

Backend `just test` 1747 passed (+1 new pin `test_topology_change_replace_keeps_flexible_flag`).
Frontend `just test-frontend` 1101 passed (+4 new overhang-panel pins). All five fixes
confirmed live by the user.

### MV-3 — Drill-v2 selection, part-3D editor ✅ VALIDATED 2026-06-07
Discharges fixes #10 #11 #12 #14 #15. Confirmed live by the user across an extended
selection-rules UX session (the original block + a large follow-on overhaul). **The
part-3D-editor selection-rules UX is now validated & finished.** What's in place:

- **Fixed levels select only their own type** — domain/end/xover/cluster no longer
  soft-fall to a whole-strand selection; a mismatched click is a no-op. End level
  selects only 5′/3′ termini.
- **Crossovers are arc-only, rendered as a glow TUBE** — single-click, lasso/additive
  multi-select, and Ctrl+click toggle all use the same green tube (full, double-sided,
  12 radial segments, depthTest-off, r = 0.147 nm). Cones never participate in crossover
  selection.
- **Ctrl+click** toggles a crossover in/out of the multi-set; **plain click** single-
  selects; lasso respects the engaged level.
- **Generic yellow hover preview** at every filter level: hovering shows — in yellow —
  exactly what a click will select (same FORM as the green selection), with snap-to-
  nearest within 80 px. The already-selected element stays green.
- **Cluster** removed from the Tab cycle (button-only) and its button moved to the gate
  group — a late-stage, occasional tool.
- No legacy red sf-pinned BoxHelper anywhere.

1087 frontend tests green; served `dist/` rebuilt. Tube geometry pinned by
`frontend/src/scene/arc_tube_geometry.test.js` (never NaNs). The earlier MV-3
REGRESSION-FOUND items (below) are resolved by the above.

---

## REGRESSION FOUND (user-confirmed fail → fix opened)

### MV-3 (2026-06-07) — two issues found running the block → **FIXED + re-validated same session** (see VALIDATED)
1. **Fixed levels still let a single click select a strand.** In domain / end / xover
   (and cluster) level, a click on an element that wasn't that type "soft-fell" to
   selecting the whole strand. Fixed: each fixed level now selects ONLY its own type;
   a mismatched click is a **no-op** (current selection kept). `selection_manager.js`
   `_v2HandleBead` / `_v2HandleCone` / `_v2HandleArc` — removed every `_selectStrandV2`
   fallback (cluster/domain/end/xover). `strand` level still selects the strand.
2. **Lasso crossover highlight ≠ single-click highlight.** Lasso/additive multi-select
   drew a cyan arc-line recolor + green glow spheres; single-click draws a green glow
   tube. Fixed: multi-select now renders the **same green glow tubes**
   (`design_renderer.setSelectionArcs` / `clearSelectionArcs`, a pooled mirror of
   `setSelectionArc`); `_applyMultiCrossoverHighlight` + the shift-click arc toggle both
   route through it.

## RE-TEST NEEDED (fix shipped, user not yet re-confirmed)

_(none — MV-3 re-validated 2026-06-07; see VALIDATED)_

---

## Next loop

**▶ Process MV-8 — Assembly config animation** (discharges #68).
Dig: the "animate to configuration" entry point (feature-log configuration → tween),
the saved-configuration data model, and how the instance tween is driven. **Fixture:**
an assembly that has ≥1 saved configuration — none is known offhand, so the SETUP may
need to *create* a configuration first (save a pose as a config) before animating to it.
This is an **assembly-mode** block. The live "tween instances between configurations"
gesture + the visual smoothness of the interpolation is the never-driven part.

(MV-6 + MV-7 were validated directly by the user 2026-06-07 without generated blocks;
MV-RSZ, the 3D overhang-resize-through-boundary fix, was pushed PENDING this session.)

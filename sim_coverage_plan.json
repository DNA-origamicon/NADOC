{
  "meta": {
    "purpose": "Authoritative, machine-parseable task inventory for the full simulation/feature-coverage loop. Drive four engines (CanDo FEM, mrDNA, oxDNA, NAMD) to cover the four unconventional design features (extra crossover bases, linkers/overhang connections, E-fields, anchors) AND emit a shared, comparable set of prediction descriptors so engines cross-validate each other.",
    "reference_policy": "per-observable: oxDNA is reference for relaxed shape + field-deflection; CanDo is reference for RMSF/flexibility; NAMD becomes gold for any observable once a NAMD run for that design exists (overrides).",
    "ux_overhaul_tracks": "User-authorized scope expansion (2026-07-08): two UX/architecture tracks ride the same loop. TRACK U (unified-panel, proposal A) collapses the 6 bespoke per-engine panels + triplicated E-field/Anchors/Surface cards into ONE Simulate section with an engine selector, driven by a capability descriptor. TRACK P (job-planner, proposal B) adds a generic MdPipeline so a user queues a multi-stage chain (e.g. E-field->surface deposition -> anchors immobilize -> E-field sweep) that runs unattended. BRIGHT LINE for U/P tasks: a capability or de-duplication PROVEN BY AN ORACLE - a per-engine PARITY test (the unified card emits the SAME payload the bespoke card did) or a CHAIN test (stage N runs seeded from N-1's output; halts + resumes from the failed stage) - never 'the panel renders' or 'a button exists'. Same module-first + oracle-first + fast/slow + auto-commit protocol; frontend tasks use vitest pure-helper oracles + a one-off display-vs-oracle check + an MV row.",
    "updated": "2026-07-08",
    "status_enum": [
      "pending",
      "in_progress",
      "done",
      "blocked"
    ],
    "loop_invariant": "The loop edits ONLY task.status and task.notes (and milestone.status, derived). It MUST NOT add, remove, reorder, or rewrite tasks, deps, oracles, or any other field. Scope changes, new tasks, or re-prioritization require the user. Seeded all-pending on purpose (nothing is 'done' until its oracle passes).",
    "three_layer_law": "Every engine output here is PHYSICAL/display-only. Anchors + field specs are JOB-REQUEST annotations, never Design edits. CanDo extra-base/linker ELEMENTS are mesh-build details derived from existing topology metadata (Crossover.extra_bases, overhang_connections), not topology mutations."
  },
  "milestones": [
    {
      "id": "M-METRIC-CORE",
      "title": "Cross-engine comparison card generates/views/exports shared descriptors + agreement",
      "tasks": [
        "S1",
        "S2",
        "S3",
        "S4",
        "S5"
      ],
      "status": "done"
    },
    {
      "id": "M-CANDO-FIELD",
      "title": "CanDo FEM E-field deflection cross-validates oxDNA within stated tolerance on a fixture",
      "tasks": [
        "C1",
        "C2",
        "S4",
        "S5",
        "O1"
      ],
      "status": "done"
    },
    {
      "id": "M-CANDO-COMPLETE",
      "title": "CanDo FEM covers all four features + feeds the comparison card",
      "tasks": [
        "C1",
        "C2",
        "C3",
        "C4",
        "C5"
      ],
      "status": "done"
    },
    {
      "id": "M-ALL-ANCHORS-FIELD",
      "title": "Every engine can run an anchored E-field job producing a comparable deflection descriptor",
      "tasks": [
        "C1",
        "C2",
        "M1",
        "M2",
        "N1",
        "N2"
      ],
      "status": "done"
    },
    {
      "id": "M-FULL-COVERAGE",
      "title": "All engines cover all four features and all feed the comparison card",
      "tasks": [
        "S1",
        "S2",
        "S3",
        "S4",
        "S5",
        "C1",
        "C2",
        "C3",
        "C4",
        "C5",
        "M1",
        "M2",
        "M3",
        "M4",
        "M5",
        "N1",
        "N2",
        "N3",
        "N4",
        "O1",
        "O2"
      ],
      "status": "pending"
    },
    {
      "id": "M-UNIFIED-PANEL",
      "title": "One Simulate section with an engine selector replaces the 6 bespoke panels; no duplicated Forces/jobs cards (proven by per-engine parity)",
      "tasks": [
        "U1",
        "U2",
        "U3",
        "U4"
      ],
      "status": "done",
      "notes": "CLOSED 2026-07-08 by U4 (U1+U2+U3+U4 all done). 6 panels fronted by one engine selector; card visibility driven by the U1 capability descriptor."
    },
    {
      "id": "M-JOB-PLANNER",
      "title": "Queue a multi-stage chain that runs unattended (local + Alpine, cross-engine, resume-from-failed-stage)",
      "tasks": [
        "P1",
        "P2",
        "P3",
        "P4"
      ],
      "status": "pending"
    },
    {
      "id": "M-DEPOSITION-CHAIN",
      "title": "Headline: the deposition->immobilize->field-sweep chain (E-field to a hard surface, then anchors, then E-fields in several directions) runs unattended from one Plan Run",
      "tasks": [
        "P1",
        "P2",
        "P4",
        "U2"
      ],
      "status": "pending"
    }
  ],
  "tasks": [
    {
      "id": "S1",
      "track": "shared-metric",
      "engine": "all",
      "feature": "comparison-metric",
      "title": "shape_metrics.py \u2014 engine-agnostic shape descriptors (global twist, bend angle + radius, Rg, end-to-end) from a display-position map",
      "deps": [],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast",
      "oracle": "synthetic straight -> twist~0; known-bend fixture -> arc-span within tol; generalize exp36 6-fold-twist + arc-span estimators; can-go-red on a scrambled frame",
      "milestone": "M-METRIC-CORE",
      "status": "done",
      "notes": "backend/core/shape_metrics.py compute_shape_descriptors(positions) composes the locked oxdna_health estimators into one engine-agnostic set: twist_total_deg, twist_per_turn_deg (axial_span/BDNA_PITCH), bend_angle_deg + bend_radius_nm (both from one bundle_slab_centreline via _chord_sagitta_bend), radius_of_gyration_nm, end_to_end_nm (axial-end cross-section centroids), axial_span_nm, n_nucleotides. Degenerate frames -> per-descriptor None (twist needs >=2 helices). Added ONE purely-additive helper bundle_slab_centreline to oxdna_health (+31/-0, locked estimators untouched). 9/9 oracle tests (tests/test_shape_metrics.py) fast. Caller must core-filter real frames. No UI (card is S5)."
    },
    {
      "id": "S2",
      "track": "shared-metric",
      "engine": "all",
      "feature": "comparison-metric",
      "title": "Unified RMSF profile + deviation profile (generalize cando_deviation.compute_deviation + fem_solver normalize_rmsf) into shape_metrics",
      "deps": [],
      "effort": "medium",
      "leverage": "high",
      "fast_slow": "fast",
      "oracle": "identical predicted==intended -> deviation RMSD 0; synthetic displaced frame -> known per-nt deviation; RMSF profile round-trips",
      "milestone": "M-METRIC-CORE",
      "status": "done",
      "notes": "shape_metrics.py gained 3 engine-agnostic primitives (read-only over positions, no topology): deviation_profile(cand, ref, align=True) \u2014 per-nt deviation + rmsd_nm; align=True Kabsch (generalizes oxdna_health.geometry_deviation_map/measure_geometry_rmsd via _kabsch_superpose, strips rigid pose, shape survives), align=False direct key-match (generalizes cando_deviation.compute_deviation, exact residual). rmsf_from_ensemble(frames, align=True) \u2014 per-nt RMS fluctuation about the mean over a frame list (generalizes production_rmsf's variance core minus oxDNA file I/O; the trajectory-variance RMSF source for oxDNA/NAMD/mrDNA; CanDo supplies NMA rmsf directly). normalize_rmsf_profile \u2014 generalizes fem_solver.normalize_rmsf to a keyed [0,1] map from any {helix_id,bp_index,direction?,rmsf_nm} list (dir-less -> both strands). Copy-aware keys throughout. 10/10 fast oracle (tests/test_shape_deviation_rmsf.py): identical->rmsd 0, known non-rigid displacement recovered exactly (align=False), rigid pose removed by Kabsch, shear survives, A/sqrt(2) fluctuation round-trip, align=True strips bulk drift keeps site fluctuation, normalize max->1 + rescale-back + all-zero safe. No UI (card is S5). Ready for S3 compare_descriptors."
    },
    {
      "id": "S3",
      "track": "shared-metric",
      "engine": "all",
      "feature": "comparison-metric",
      "title": "compare_descriptors(cand, ref) \u2014 per-observable reference selection + agreement math (scalar %delta, RMSF Pearson/Spearman, aligned-shape RMSD via _rigid_superpose)",
      "deps": [
        "S1",
        "S2"
      ],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast",
      "oracle": "identical inputs -> perfect agreement; known-perturbed input -> expected deltas/correlation; reference-selection honors per-observable policy incl. NAMD-override-when-present",
      "milestone": "M-METRIC-CORE",
      "status": "done",
      "notes": "shape_metrics.py gained the cross-engine agreement layer (pure over Physical-layer dicts, no topology). compare_descriptors(candidate, reference, *, align_shape=True) scores agreement on the three comparable observable classes S1/S2 emit: (1) COMPARABLE_SCALARS (7 shape descriptors) -> {candidate, reference, abs_delta, signed_pct_delta} where pct = (cand-ref)/|ref|*100, None scalars incomparable, zero-ref -> abs_delta w/ None pct (no div0); (2) rmsf -> Pearson+Spearman over shared base pairs, {pearson, spearman, n, candidate/reference_mean_rmsf_nm}, degenerate (constant/<2 shared) -> None coeff not NaN; (3) shape_rmsd_nm via deviation_profile(align=True) Kabsch (rigid pose zeroed, real shape survives). reference_for(engines, observable) applies per-observable policy (shape/field->oxdna, rmsf->cando) with NAMD gold-override across all observables, missing engine/unknown observable -> None. KEY FIX (review-caught, HIGH): _rmsf_per_bp collapses BOTH profiles to per-(helix,bp,copy) mean over strand direction -- CanDo NMA RMSF is direction-less (1/bp) while ensemble RMSF is per-strand (2/bp); direction keying left the designated CanDo-vs-oxDNA pairing with an empty intersection (silent None). 13/13 fast oracle tests/test_shape_compare.py incl. the mixed-direction cross-engine case. No UI (card = S5). M-METRIC-CORE now S3 done; S4 (field-response, deps S1) + S5 (card, deps S3) remain."
    },
    {
      "id": "S4",
      "track": "shared-metric",
      "engine": "all",
      "feature": "E-field",
      "title": "Unified field-response descriptor (generalize oxdna_health.measure_field_response into shape_metrics: per-nt deflection map, free-region projection-along-field, anchored drift, magnitude ratio, cosine similarity)",
      "deps": [
        "S1"
      ],
      "effort": "low",
      "leverage": "high",
      "fast_slow": "fast",
      "oracle": "synthetic anchored+deflected frame -> anchors held, free deflects along field, monotone in field magnitude; two-engine deflection cosine sim",
      "milestone": "M-METRIC-CORE",
      "status": "done",
      "notes": "shape_metrics.py gained the engine-agnostic field-response layer (pure read-over-positions, no topology). field_response_profile(field_positions, reference_positions, field_dir, anchor_keys, *, anchor_tol_nm=1.0, min_free_proj_nm=0.5) generalizes oxdna_health.measure_field_response: reproduces its aggregates + physical verdict byte-for-byte (passed = anchored_max<=tol AND free_proj_along_field>=min; same two ValueErrors on zero field-dir / no free nts) and ADDS a copy-aware per_nt deflection map [{helix_id,bp_index,direction,copy,disp_vec_nm,disp_nm,proj_along_field_nm,anchored}] + a mean free deflection_vec_nm. NOT Kabsch-aligned (the anchored region IS the common frame; aligning would erase the measured motion). Keys via _dev_key (copy-distinct so inserted-base copies stay separate); anchor membership copy-AGNOSTIC (an anchored bp pins all its copies). compare_field_response(cand, ref) scores cross-engine deflection agreement over SHARED FREE nts: cosine_similarity (concatenated free disp vectors: identical->+1, opposite->-1, orthogonal->0) + magnitude_ratio (||cand||/||ref||: 3x compliance->3.0), None on degenerate/empty (n_shared_free=0). Both profiles normalize direction identically -> no S3-style silent-empty-intersection. 13/13 fast oracle tests/test_shape_field_response.py; full just test 4155 passed. No UI (field panel lands in the S5 card). S4 done -> M-METRIC-CORE only S5 remains."
    },
    {
      "id": "S5",
      "track": "shared-metric",
      "engine": "all",
      "feature": "comparison-metric",
      "title": "Cross-engine comparison CARD: routes_shape_metrics.py (daemon-thread registry, POST /shape/compare/start + GET /shape/compare/{run_id}) + shape_compare_card.js (mirror metrics-card pattern; overlay profiles, scalar+agreement table; PNG/CSV export via metric_graph/metric_export_modal)",
      "deps": [
        "S3"
      ],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast+MV",
      "oracle": "pure helpers (chart spec, CSV rows, agreement table) unit-tested; smoke green; one-off Playwright display-vs-oracle check; MV row for live cross-engine data. S4 enriches the field panel.",
      "milestone": "M-METRIC-CORE",
      "status": "done",
      "notes": "Closes M-METRIC-CORE. backend/core/shape_compare.py build_comparison_report(sources) \u2014 PURE assembly composing S3/S4 (reference_for + compare_descriptors + compare_field_response) into one card payload from per-engine source bundles {engine, descriptors?, rmsf?, shape_frame?, field?}: scalar table (value + signed %-delta vs SHAPE reference), per-engine RMSF overlay profiles (per-bp collapsed), agreement rows (shape-RMSD vs shape-ref, RMSF Pearson/Spearman vs rmsf-ref, field cosine/ratio vs field-ref), field panel (held+deflected verdict + cosine-vs-ref). Per-observable reference honors S3 policy (oxDNA=shape/field, CanDo=RMSF, NAMD overrides); references dict reports POLICY ref (data-agnostic like shape/rmsf) but the field comparison/panel resolve the reference among FIELD-CARRYING engines only (review-caught: else the policy engine is mislabelled the reference while contributing no deflection). Graceful: 1 engine->raw values no deltas; missing observable->no rows; empty->not-ready. backend/api/routes_shape_metrics.py daemon registry POST /shape/compare/start + GET /shape/compare/{run_id} (registered in main.py; compute instant now, pattern kept so per-engine source-gathering can go slow later). frontend/src/ui/shape_compare_card.js \u2014 card factory + PURE helpers (fmtNum/fmtDelta/scalarTableModel/rmsfOverlaySpec via shared metric_graph.buildChartSpec/comparisonCSVs); Generate->gather sources->POST/poll->render scalar table + RMSF overlay canvas + agreement + field panel; Export via shared metric_export_modal (PNG overlay + CSV tables). Collapsible card in oxDNA Dynamics panel, wired from initOxdnaJobsPanel (getSources: ()=>[] for now; live per-engine sources = O1/C5/M5/N4 -> MV-21). main.js LOC Delta=0. Oracle tests/test_shape_compare_report.py 15 fast (per-observable ref incl NAMD-override + field-carrier resolution, scalar +/-%delta + zero-ref no-div0, identical RMSF->Pearson 1 + overlay pts, rigid-shift->shape-RMSD~0, field cosine +/-1 + mag-ratio 3, 1-engine/empty/missing-observable degradation, REST start->poll->404). vitest shape_compare_card.test.js 9 (pure helpers + Generate->poll->render + empty-sources not-ready + refresh). One-off display-vs-oracle Playwright: real card+client+backend on synthetic 2-engine sources -> rendered table shows the backend oracle's +10.0% twist delta, oxdna.ref, RMSF Pearson 1.000; displayed==oracle, passed, deleted. Fresh-context review: no correctness bugs. Gates: 15/15 oracle, 2200/2200 vitest, just test green, ruff clean on touched files, smoke green."
    },
    {
      "id": "C1",
      "track": "cando",
      "engine": "CanDo FEM",
      "feature": "anchors",
      "title": "Anchors: generalize apply_boundary_conditions from centroid-pin to pin selected nodes (Dirichlet BC); predict_shape(design, anchors=...); reuse oxDNA anchor scopes (cluster/domain/overhang) + resolve_anchor_particles-style resolver -> node indices",
      "deps": [],
      "effort": "low",
      "leverage": "high",
      "fast_slow": "fast+slow",
      "oracle": "FAST: pinned nodes fixed (u==0 at pinned DOFs), free nodes move under a test load; free-free NMA preserved when no anchors. SLOW: real nonlinear solve on a fixture holds the anchor.",
      "milestone": "M-CANDO-COMPLETE",
      "status": "done",
      "notes": "Dirichlet anchors for the CanDo FEM (backend-only; anchors are a JOB-REQUEST annotation, never a Design/topology edit -- Three-Layer Law). apply_boundary_conditions(K,f,mesh,fixed_nodes=None) pins all 6 DOF of each anchor node; None OR empty list falls back to the legacy centroid pin (a stale selection that resolves to nothing never makes the system singular). solve_prestress_shape(...,fixed_nodes=) clamps them at every corotational load step -> the anchored region stays exactly at rest while the rest deflects under the loop/skip eigenstrain. resolve_anchor_nodes(design,mesh,anchors) reuses the SHARED oxDNA scope resolver (oxdna_interface.resolve_anchor_particles: overhang/cluster/domain/strand/base) -> per-nt (helix,bp,dir) keys collapsed onto the single duplex-core axis node per bp (both strands -> one node; out-of-core nts drop silently -- ssDNA ends, extra-base sentinel keys). predict_shape(design,*,anchors=None) threads anchors through BOTH nonlinear + linear paths and returns anchor_keys:[[helix,bp],...]. RMSF stays free-free NMA regardless (intrinsic flexibility; anchoring RMSF is a later refinement, not needed here). Oracle tests/test_cando_anchors.py 10 fast: synthetic beam pins held (u==0) & free tip moves under a test load; BC pins exactly the requested nodes / empty->centroid; resolver maps base+cluster scopes & drops stale; prestress solve holds the anchored node <1e-9 while the rest deflects >1e-3; unresolved anchor is a no-op (positions identical, free-free RMSF preserved via Pearson>0.999+mean-within-2% -- ARPACK no-v0 jitter, not an anchor effect). Fresh-context review: no correctness gaps (honest note: the free-free-RMSF assertion is green-by-construction since the RMSF path never receives anchors -- consistent w/ the stated oracle; positions no-op is the load-bearing check). Gates: 10/10 oracle, just test 4186 passed/66 skip/1 xfail (+1 known pre-existing job-archive xdist flaky, passes isolated), ruff clean on touched files. No card/UI -> display-vs-oracle N/A. main.js LOC delta=0. Substrate for C2 (E-field deflection needs an anchor to hold against)."
    },
    {
      "id": "C2",
      "track": "cando",
      "engine": "CanDo FEM",
      "feature": "E-field",
      "title": "E-field: add uniform q*E per-node nodal load to the assemble_prestress_force vector (editable effective-charge const, same convention as oxDNA); predict_shape(design, field=..., anchors=...); nonlinear solve for large deflections",
      "deps": [
        "C1"
      ],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast+slow",
      "oracle": "FAST: shared measure_field_response (S4) on the FEM deflected frame -> anchor holds, free deflects along field, deflection monotone in field magnitude, zero-field -> no deflection. SLOW: nonlinear field solve on a tethered-arm fixture.",
      "milestone": "M-CANDO-FIELD",
      "status": "done",
      "notes": "Uniform E-field body load for the CanDo FEM (backend-only; field is a JOB-REQUEST annotation like anchors, never a Design/topology edit -- Three-Layer Law). assemble_field_force(mesh, field) builds the equivalent nodal-force vector from the shared oxDNA descriptor {\"field_pN\":<force per NUCLEOTIDE, pN>, \"dir\":[x,y,z]} -- the SAME per-nucleotide force oxDNA applies per bead (OXDNA_FORCE_PN convention). Each duplex axis node carries FEM_FIELD_CHARGES_PER_NODE=2 backbones, so its translational load is 2*field_pN*dir_hat (pN), rotational DOF zero (a pure body force, no couple). None/{}/zero-mag/zero-dir -> exact zero vector; magnitude linear. The field is a DEAD load: assembled ONCE in global coords before the corotational loop and added to the per-step-reframed eigenstrain each of n_steps increments, so it stays fixed in the lab frame as the bundle bends (unlike the co-rotating loop/skip eigenstrain). Threaded through solve_prestress_shape(...,field=) (nonlinear) + predict_shape(design,*,anchors=,field=) both paths; a field needs >=1 anchor to hold against (COM drift), reusing C1 resolve_anchor_nodes. ORACLE = shared S4 shape_metrics.field_response_profile on the FEM deflected frame vs the same design field-OFF frame, measured on the RAW solved axis-node positions -- NOT predict_shape display positions, which are per-frame Kabsch-REPOSED onto the design (deformed_positions_with_axis) so straight(ref)+bent(cand) land in different poses and the anchor would spuriously drift ~5nm; the raw clamped-solve frame is a genuine common frame (6 end nodes fully constrain rigid body). tests/test_cando_field.py 7: assemble_field_force unit props (none/zero no-op, 2*chg/node translational-only direction, linear magnitude) FAST; end-to-end (anchored 6HB, transverse field) anchors held (drift ~0) + free deflects ALONG field (proj>=0.5nm) + monotone in |E| (fp 0.05->5.2nm, 0.1->10.4nm) + zero-field->no deflection (RED guard) + predict_shape(field=) threads through & field=None is a byte-identical no-op -- these 4 nonlinear-solve tests registered SLOW in conftest (each multiple corotational solves), unit tests stay fast. n_steps=8 converged+stable (fp>=~0.5 blows up the corotational solve; gentle fields chosen). Fresh-context review: no correctness gaps; predict_shape public S4 verdict proven indirectly (shares the exact solve_prestress_shape(field=) the property tests validate) -- documented, inherent (S4 on the Kabsch display frame would falsely fail). Gates: 7/7 oracle, just test 4194 passed/66 skip/1 xfail, ruff clean on touched. No card/UI -> display-vs-oracle N/A (like C1). main.js LOC delta=0. Closes M-CANDO-FIELD (C1,C2,S4,S5,O1 all done): the cheap FEM now predicts the oxDNA field-deflection regime (anchored tethered-arm, deflection along field, monotone) with the SAME per-nt force convention -- the C5 field-source builder must emit field-response from the RAW frame, not predict_shape display positions."
    },
    {
      "id": "C3",
      "track": "cando",
      "engine": "CanDo FEM",
      "feature": "extra-bases",
      "title": "Extra crossover bases as mechanical elements (compliant FJC / nick-soft connector, CanDo CONN3D2 analogue) in build_fem_mesh, from Crossover.extra_bases; currently display-only gap-fill",
      "deps": [],
      "effort": "medium",
      "leverage": "high",
      "fast_slow": "fast+slow",
      "oracle": "FAST: a design with inserts vs without -> inserts measurably soften local twist/bend (compare_descriptors delta beyond tol, correct sign). Mesh node/element count reflects inserts.",
      "milestone": "M-CANDO-COMPLETE",
      "status": "done",
      "notes": "The compliant-connector MECHANISM already lived in build_fem_mesh (an extra-base crossover meshes as a 2-node WLC ssDNA spring k_trans=3*KBT/(2*L_c*L_P_SS), k_rot=0, instead of a rigid link -- CanDo CONN3D2 analogue, shipped untested in Phase-5 commit 9121b78). C3's bright-line deliverable was therefore the OMISSING property ORACLE, not new production code. New tests/test_cando_extra_bases.py (3 FAST + 1 SLOW, slow registered in conftest). FAST (deterministic): (1) census -- an extra-base crossover meshes as a compliant spring (k_rot==0, k_trans==WLC formula, <K_PENALTY/1e3) and leaves the rigid-link set; spring count == #inserted crossovers, springs+rigid conserved (mesh reflects inserts; CanDo models ssDNA as a connector between EXISTING bp nodes, so no added nodes -- 'reflects inserts' = connector TYPE, not node count); (2) k_trans softens monotonically with insert length (T>TT>TTTT, k1==2*k2, k proportional 1/L_c); (3) synthetic 2-node compliance -- under the SAME transverse load a WLC-spring crossover deflects >1e3x more than a rigid link (u==F/k_trans vs F/K_PENALTY exactly). SLOW (real predict_shape + free-free NMA): inserts on a middle-third BAND of crossovers raise LOCAL per-bp RMSF at the inserted-crossover nodes (~1.87x observed, assert >1.3x), EVERY affected node more flexible, surfaced through the shared S3 compare_descriptors RMSF channel (candidate_mean_rmsf > reference*1.1); RED-guard: a self-vs-self base rerun is flat (<1.05x) so the softening is attributable to inserts not NMA jitter. DELIBERATELY asserts NO twist/bend DIRECTION -- softening inter-helix coupling redistributes a distributed field/eigenstrain load non-monotonically (verified: free along-field projection is NOT a clean function of insert count), and reasoning about the sign geometrically is forbidden (crossover rules); RMSF/flexibility is the physically-unambiguous softening signal AND CanDo's designated reference observable. Three-Layer clean: _with_inserts does model_copy(deep) before stamping extra_bases; solver only READS Crossover.extra_bases (never a topology mutation). Fresh-context review: no correctness gaps, math hand-verified, RED-guard meaningful. Gates: oracle 4/4, just test 4210 passed/66 skip/1 xfail (was 4206, +4=new tests, no drops), ruff clean on touched. No card/UI -> display-vs-oracle N/A (like C1/C2). main.js LOC delta=0 (backend-only). M-CANDO-COMPLETE now only C4 (linkers, deps=C3) remains."
    },
    {
      "id": "C4",
      "track": "cando",
      "engine": "CanDo FEM",
      "feature": "linkers",
      "title": "Linkers / overhang connections as FJC connector elements coupling parts/domains in build_fem_mesh, from overhang_connections/extensions",
      "deps": [
        "C3"
      ],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast+slow",
      "oracle": "FAST: two parts joined by a linker element show coupled response under a test load on one part (the other moves); no linker -> decoupled.",
      "milestone": "M-CANDO-COMPLETE",
      "status": "done",
      "notes": "CLOSES M-CANDO-COMPLETE (C1,C2,C3,C4,C5 done). USER CLARIFICATION reframed the task: a linked overhang is DUPLEX (overhang staple hybridizes to the linker's RC binding domain) and connect_overhangs materializes REAL topology \u2014 the overhang complement strand + a __lnk__ bridge helix (ds: duplex 2-strand bridge; ss: single-stranded bridge = the flexible tether). The gap was NOT a missing connector element but that build_fem_mesh couldn't SEE the duplex the linker builds: _duplex_bp_per_helix counted scaffold\u2227staple only, dropping staple\u2227linker (linked overhang) and linker\u2227linker (ds bridge) -> linkers meshed to NOTHING. TWO additive changes (fem_solver.py): (1) _duplex_bp_per_helix now returns (scaf\u2227stap) \u222a (fwd\u2227rev\u2227linker) \u2014 UNION gated on a LINKER strand covering the bp, so a design with no linker strands is byte-for-byte identical (zero exp36 regression, asserted); a ss bridge (one backbone) is correctly excluded. (2) _add_linker_hops closes the load path at each LINKER strand's helix-hop junctions (NOT Design.crossovers): consecutive meshed duplex domains on different helices couple RIGID if adjacent (ds bridge, per user), WLC SPRING (k_rot=0, contour=skipped-ssDNA-run\u00d7RISE_SS) if flanking an unmeshed ss run (ss linker, per user). Reuses C3's WLC formula + existing beam/crossover/spring machinery; ds hops\u2192rigid_links, ss hops\u2192springs, no new mesh field, no assembler change. Oracle tests/test_cando_linkers.py 5 FAST: synthetic two-part mesh (THE bright line: WLC linker transmits a load part A->part B, NO linker->part B exactly 0; rigid couples >10x the soft tether); additive-no-regression (independent legacy scaf\u2227stap recompute == _duplex_bp_per_helix on a linker-free 6HB); real routed-6HB ds (overhangs+bridge gain duplex bp, bridge meshed, rigid hops WIRE bridge to BOTH overhang helices \u2014 connectivity, not just count, 0 springs) + ss (bridge stays ssDNA/unmeshed, exactly +1 WLC spring spanning the two DISTINCT overhang helices, k_trans==WLC(6)). Real fixture = _place_two_overhangs_on_6hb (well-formed 5'->3' domains; the hand-built _seed_two_overhang_leaves has malformed REVERSE start<end -> empty domain_bp_range -> won't mesh; noted for future FEM fixtures). Fresh-context review: BOTH code changes correct + genuinely additive, no bug; strengthened 2 oracle tests it flagged as census/green-by-construction. Gates: oracle 5/5, just test 4215 passed/66 skip/1 xfail (+5 new, no drop), FEM+exp36 calibration guards green (no regression), ruff clean on touched. No card/UI -> display-vs-oracle N/A (like C1/C2/C3). main.js LOC delta=0 (backend-only)."
    },
    {
      "id": "C5",
      "track": "cando",
      "engine": "CanDo FEM",
      "feature": "comparison-metric",
      "title": "CanDo contribution to comparison card: emit shared descriptors from predict_shape output + register CanDo as a compare-source (RMSF reference column)",
      "deps": [
        "S5"
      ],
      "effort": "low",
      "leverage": "high",
      "fast_slow": "fast+MV",
      "oracle": "CanDo descriptors match the exp36 calibration values within tol; appears as a selectable engine in the card; MV live check.",
      "milestone": "M-CANDO-COMPLETE",
      "status": "done",
      "notes": "CanDo is now the SECOND live source of the S5 cross-engine comparison card (was O1's oxDNA-only getSources) -> the FIRST real oxDNA-vs-CanDo agreement numbers. backend/core/cando_shape_source.py build_cando_shape_source(shape_frame, core_reference, *, rmsf=None, field=None) -- the CanDo twin of O1's oxdna_shape_source, following the SOURCE-BUNDLE CONTRACT: core-filters predict_shape()['positions'] to the rigid dsDNA core (_filter_to_reference_core vs core_reference_geometry, ssDNA ends dropped), emits CanDo's ABSOLUTE compute_shape_descriptors (S1, same locked estimator -- NOT a differential), and maps predict_shape()['rmsf'] ({helix_id,bp_index,rmsf_nm}) to the card's rmsf shape with direction=None. KEY: CanDo NMA RMSF is DIRECTION-LESS (both strands share one axis node) -- _rmsf_per_bp collapses over direction anyway (S3 lesson), so direction=None still pairs CanDo's per-bp RMSF with oxDNA's per-strand ensemble RMSF (test asserts n=24 shared, Pearson 1.0) instead of the silently-empty intersection direction-keying would give. Route GET /cando/jobs/{id}/shape-source (routes_cando.py) reads the job's cached display+rmsf+snapshot design, builds core_reference_geometry, returns {ready(=descriptors is not None), ...bundle}; graceful no-display->{ready:False}, no-snapshot->500 (mirrors sibling /cylinders,/deviation). Frontend: api.getCandoShapeSource + oxdna_jobs_panel getSources now fetches BOTH the selected oxDNA job's bundle AND the selected CanDo job's bundle (new lazy getCandoJob dep, main.js captures const candoPanel + passes ()=>candoPanel?.getSelectedJob?.()) -> [oxdna, cando]. With both, reference_for makes oxDNA the SHAPE reference + CanDo the RMSF reference; agreement rows carry CanDo-shape-RMSD-vs-oxDNA + oxDNA-RMSF-Pearson-vs-CanDo. FIELD emission deferred (field:None; when added MUST use the RAW solve_prestress_shape frame not predict_shape display positions -- C2/handoff Kabsch-drift lesson). Oracle tests/test_cando_shape_source.py 7 (6 fast pure + 1 SLOW real-predict_shape, registered in conftest): engine tag+descriptor self-consistency, core mask drops ssDNA ends, rmsf remap direction-less+drops None, field passthrough, empty-core->None (RED), cross-engine integration (oxdna+cando -> ready, refs shape=oxdna/rmsf=cando, cando shape-RMSD finite ~0 on rigid shift, oxDNA RMSF Pearson 1.0 n=24, cando rmsf_profile is_reference); SLOW: routed 6HB -> real predict_shape -> finite descriptors+rmsf -> ready cando source. Fresh-context review: CONFIRMED-CORRECT, no bugs, no TDZ (getSources only fires on Generate click), Three-Layer clean. Gates: 7/7 oracle, just test 4206 passed/66 skip/1 xfail, ruff clean on touched (20 pre-existing debt in OTHER files untouched), vitest 2214, smoke green (assembly_exit_cleanup flaked in parallel, passes isolated). Display-vs-oracle: the S5 card's cando-column RENDERING was already scraped-vs-oracle in S5 (synthetic 2-engine oxdna+cando sources -> +10% twist delta, RMSF Pearson 1.000); C5 only wires the REAL backend route into the S5-validated render path, so live cross-engine eyeball = MV-21 (updated). main.js LOC Delta=+4 (pure wiring: 1 lazy dep + capture existing factory return)."
    },
    {
      "id": "M1",
      "track": "mrdna",
      "engine": "mrDNA",
      "feature": "anchors",
      "title": "Anchors via ARBD restraints / fixed particles, headless entry point; reuse the shared anchor-scope resolver",
      "deps": [],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast+slow",
      "oracle": "FAST: ARBD input carries the restraint block for resolved anchor beads. SLOW: real ARBD run -> anchored beads hold, free beads move.",
      "milestone": "M-ALL-ANCHORS-FIELD",
      "status": "done",
      "notes": "Shared resolve_anchor_particles \u2192 per-nt keys \u2192 nearest CG bead by 3D position (mrDNA groups helices by base-pairing not NADOC helix id + collapses each bp to 1 fwd bead; input r-array & beads share the \u00c5 frame). install_anchor_restraints wraps generate_bead_model so RESTRAINTs survive clear_beads()+regen between multiresolution stages; coarse path pins the as-built beads. FAST oracle asserts real ARBD .restraint.txt (dry_run) carries a line for exactly the resolved beads; SLOW real ARBD: anchored beads hold 0.55A vs free 3.81A (7x). backend/core/mrdna_anchors.py + MrdnaJob.anchors + route passthrough + runner install."
    },
    {
      "id": "M2",
      "track": "mrdna",
      "engine": "mrDNA",
      "feature": "E-field",
      "title": "E-field via ARBD constant / grid force per bead (charge-scaled); headless",
      "deps": [
        "M1"
      ],
      "effort": "medium",
      "leverage": "high",
      "fast_slow": "fast+slow",
      "oracle": "FAST: shared field-response on a mock deflected frame; force block written correctly. SLOW: real ARBD anchored field run -> deflection along field.",
      "milestone": "M-ALL-ANCHORS-FIELD",
      "status": "done",
      "notes": "mrDNA uniform E-field via ARBD grid-potential forces (backend-only, JOB-REQUEST annotation, never a Design edit). backend/core/mrdna_field.py turns the SHARED {field_pN (per NUCLEOTIDE), dir} descriptor into a constant per-bead force scaled by each bead's nucleotide content (= bead_mass/dalton_per_nucleotide; DNA beads carry charge 0 so force is applied directly, not q\u00b7E), applied through ARBD's per-ParticleType grid potential: a linear ramp U=-(F\u00b7r) via add_grid_potential/gridFile whose negative gradient is the uniform force F (kcal/mol/\u00c5). KEY: the forceXGrid tabulated-force path CRASHES ARBD (illegal memory access) on a constant grid \u2014 the ramp-potential idiom (arbdmodel.grid.constant_force) is the correct mechanism. Total applied force = field_pN\u00d7total_nt exactly (mass terms collapse), matching the per-nt engines. install_field_force wraps generate_bead_model so the grids re-attach to the fresh ParticleType objects after mrDNA's bead regeneration between multiresolution stages (idempotent: grid_potentials overwritten). MrdnaJob.e_field (named e_field not 'field' to avoid shadowing dataclasses.field) + CreateMrdnaJobRequest.field + guards (malformed/zero \u2192400 incl non-numeric caught; field-needs-anchor \u2192400) + runner install after anchors, which RAISES if the field's anchors resolved to 0 held beads (else COM-drift). Oracle tests/test_mrdna_field.py 9 fast + 1 slow: fast = per-bead force vs FIRST-PRINCIPLES pN\u2192kcal/mol/\u00c5 (not the code constant), 2\u00d7mass\u21922\u00d7force, ramp .dx round-trip (-\u2207U==F via loadGrid), per-type grid wiring, dry-run conf emits gridFile field_*.dx, regen-survival RED-checked, 2 REST guards. SLOW real ARBD (2 coarse runs, field-on vs off, one strand anchored): anchored held (0.5\u00c5) while free bulk deflects ALONG +field (proj ~8.5\u00c5 vs field-off \u00b12\u00c5 wander), magnitude within [0.45,2.0]\u00d7 the overdamped Brownian prediction D\u00b7F\u00b7T/(k_B\u00b7T) from the engine's OWN diffusivity/mass + field_pN via the first-principles constant (~12% agreement observed; independent of the emission constant, so a corrupted constant falsifies). Registered slow in conftest. ruff clean; main.js LOC \u0394=0 (backend-only). Closes M-ALL-ANCHORS-FIELD (every engine \u2014 CanDo C1/C2, NAMD N1/N2, mrDNA M1/M2 \u2014 now runs an anchored E-field job with a comparable along-field deflection descriptor)."
    },
    {
      "id": "M3",
      "track": "mrdna",
      "engine": "mrDNA",
      "feature": "extra-bases",
      "title": "Extra crossover bases into the ARBD MODEL (verify past the existing display bridge extra_base_flank_keys -> beads present in the simulated model)",
      "deps": [],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast",
      "oracle": "FAST: model bead count includes inserts; inserts are ssDNA (flexible, non-rigid) in the ARBD topology.",
      "milestone": "M-FULL-COVERAGE",
      "status": "done",
      "notes": "Model-level oracle added to tests/test_mrdna_extra_bases.py (pins #M3a-c) proving the extra-base inserts survive mrDNA coarse-graining into the BUILT SegmentModel as flexible ssDNA. New _model_seg_stats(design) builds the model (model_from_basepair_stack_3prime) and sums nucleotides by segment class + bead children. Three FAST pins (mrdna-gated, ~0.8s each, not slow): (a) built-model total nt grows by EXACTLY n_extra for a single insert AND all crossovers; (b) ALL growth lands in SingleStrandedSegments while DoubleStrandedSegment nt is invariant (ds_nt=504 unchanged) -> inserts are ssDNA/flexible/non-rigid, never WC-paired into a rigid segment; (c) bulk bead cloud grows (136->229). Strengthens the pre-loop coarse pin #6 (with_ss>base_ss) into a quantitative present-as-flexible-ssDNA proof. Can-go-red 3 ways (dropped insert -> d_tot!=n_extra; paired into ds -> ds changes; other type -> ss!=n_extra). PRESENCE+FLEXIBILITY only, no bend/twist direction. No production code (bridge shipped e47edb8); M3 was the VERIFY task. Fresh-context review: no real gaps. Oracle 15/15 green. Full just test 4366 passed/72 skip/1 xfail + 1 PRE-EXISTING xdist isolation flake (test_cando_extra_bases, unrelated FEM file, passes in isolation, non-deterministic victim); logged to issues_ledger."
    },
    {
      "id": "M4",
      "track": "mrdna",
      "engine": "mrDNA",
      "feature": "linkers",
      "title": "Linkers / overhang connections threaded through to the ARBD model",
      "deps": [
        "M3"
      ],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast+slow",
      "oracle": "FAST: linker beads/bonds present in the ARBD model. SLOW: joined parts co-move.",
      "milestone": "M-FULL-COVERAGE",
      "status": "pending",
      "notes": ""
    },
    {
      "id": "M5",
      "track": "mrdna",
      "engine": "mrDNA",
      "feature": "comparison-metric",
      "title": "mrDNA metrics-card + shared descriptors (RMSF from CG trajectory; fix the copy-key gap in _display_positions flagged in project_oxdna_extra_bases)",
      "deps": [
        "S5"
      ],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast+MV",
      "oracle": "descriptors emitted + selectable in card; copy-key coverage test; MV live check.",
      "milestone": "M-FULL-COVERAGE",
      "status": "done",
      "notes": "mrDNA is the THIRD live source of the S5 comparison card (after O1 oxDNA + C5 CanDo). backend/core/mrdna_shape_source.py build_mrdna_shape_source(shape_frame, core_reference, *, rmsf=None, field=None) \u2014 the mrDNA twin of O1/C5, same SOURCE-BUNDLE CONTRACT {engine, descriptors, rmsf, shape_frame, field}: core-filter the reconstructed relaxed display frame to the rigid dsDNA core (_filter_to_reference_core vs core_reference_geometry), emit mrDNA's ABSOLUTE compute_shape_descriptors (S1), map a per-nt RMSF list to the card shape. CG-TRAJECTORY RMSF: mrdna_runner.mrdna_trajectory_rmsf(design, job_dir) reconstructs the per-nt relaxed frame at each DCD timestep (nuc_pos_override_display_from_coarse per frame, evenly subsampled to max_frames=40) and feeds the ensemble to the shared S2 rmsf_from_ensemble(align=True) (Kabsch strips CG box diffusion) \u2014 the trajectory-variance RMSF source. COPY-KEY GAP FIX: mrDNA's _display_positions emits crossover extra-base inserts as {helix_id:'__xb__', bp_index:<crossover-id STRING>, direction:k}; the string bp_index crashes the shared _dev_key (int(bp_index)) oxDNA never lets in. The gap bites the SHAPE column: _filter_to_reference_core/_core_column_key drop the non-int-bp inserts before descriptors run; _rmsf_profile also skips non-int bp defensively (the production RMSF path is int-key-only, no __xb__). Route GET /mrdna/jobs/{id}/shape-source (routes_mrdna.py) reads cached display + threadpooled trajectory RMSF + snapshot design core reference \u2192 {ready(=descriptors is not None), n_frames, ...bundle}; no-display\u2192ready:False, no-snapshot\u2192500 (mirrors C5). Frontend: api.getMrdnaShapeSource + compare card getSources fetches the selected mrDNA job's bundle \u2192 [oxdna, cando, mrdna]; main.js captures const mrdnaPanel + lazy getMrdnaJob (no-TDZ, same as getCandoJob). Field deferred (field:None) \u2014 like O1/C5. Oracle tests/test_mrdna_shape_source.py 9 (8 fast + 1 slow): engine tag+descriptor self-consistency, core mask drops ssDNA ends, COPY-KEY coverage (string-bp __xb__ inserts build a valid bundle, dropped, descriptors unchanged; RED if guard removed), rmsf remap (copy preserved, drops None+non-int), field passthrough, empty-core\u2192None RED, TRAJECTORY-RMSF path (monkeypatched reconstruction+fake traj length: subsamples to max_frames, int keys feed rmsf_from_ensemble, <2-frame\u2192None), cross-engine integration ([oxdna,mrdna]\u2192build_comparison_report ready, shape ref=oxdna, mrdna shape-RMSD \u22480 on rigid shift); SLOW (conftest-registered) real ARBD coarse run \u2192 _display_positions + mrdna_trajectory_rmsf (n_frames\u22652, finite rmsf) \u2192 ready mrDNA source. Fresh-context review: product CONFIRMED-CORRECT; it flagged the RMSF __xb__ guard as defending an impossible production input \u2192 addressed with the fast trajectory-RMSF pin + honest docstring (gap's real locus = shape column). Gates: oracle 8 fast+1 slow, just test 4352 passed/72 skip/1 xfail (+1 documented xdist active-design flake test_namd_efield::test_no_field_skips_both_guards \u2014 passes isolated + alongside; additive mrDNA/card code never touches design_state), ruff clean on touched, vitest 2294, smoke 23 green. main.js LOC \u0394=+4 (pure wiring). Display-vs-oracle: NOT a new card \u2014 new backend source route into the S5-validated render path (like C5) \u2192 live eyeball MV-21 (updated with M5 slice). No mrDNA card-source in-app gesture hand-driven yet \u2192 owes MV-21."
    },
    {
      "id": "N1",
      "track": "namd",
      "engine": "NAMD MD",
      "feature": "E-field",
      "title": "E-field: emit native eField / eFieldOn block in md_protocols.py conf generation (q*E on charged atoms) + a Dynamics-tab UI control",
      "deps": [],
      "effort": "low",
      "leverage": "high",
      "fast_slow": "fast+slow",
      "oracle": "FAST: generated conf contains eFieldOn + eField vector with the correct magnitude/direction. SLOW: short run + shared field-response descriptor.",
      "milestone": "M-ALL-ANCHORS-FIELD",
      "status": "done",
      "notes": "NAMD native uniform E-field via eFieldOn/eField (q\u00b7E on every charged atom). Backend (a JOB-REQUEST annotation, never a Design edit \u2014 Three-Layer Law): (1) md_protocols.namd_efield_vector({field_pN,dir}) converts the SHARED cross-engine descriptor (force per NUCLEOTIDE in pN \u2014 the same load oxDNA/LAMMPS apply per bead, CanDo per duplex node) to NAMD's eField vector in kcal/mol/\u00c5/e. NO effective-charge fudge: an INTERNAL DNA nucleotide carries -1 e by force-field construction (its one phosphate; the value namd_solvate._count_dna_charge counts). Sign: backbone charge is negative \u2192 eField antiparallel to the requested force. (2) external_forces_block(anchors_file,field) is the ONE emitter every conf writer shares (DRY'd the N2 fixedAtoms block into it); threaded through _segment_conf/_min_conf + BOTH production writers (_conservative/_seed) which previously DROPPED anchors too + md_shell_reprep + manifest field block; anchors/field=None byte-identical no-ops. (3) prepare raises if a field's anchors resolve to nothing (the API guard counts CHIPS; a stale/ssDNA scope would else launch the COM-drift run it prevents). (4) routes_md CreateJobRequest.field + guards: field-needs-anchor 400, field+multi-GPU 400 ('EField is not compatible with multi-GPU GPUresident', read from the NAMD binary), malformed-field 400. (5) UI: MD-panel Electric-field card = 2nd instance of the shared numeric field factory (initCandoEfieldSetup gained an `ids` bag, mirroring initOxdnaAnchorsSetup's 3-panel reuse); launch guards mirror CanDo/oxDNA. main.js LOC \u0394=0. MEASURED-NOT-ASSUMED (oracle-first caught my own error): psfgen 5TER/3TER hydroxyl termini carry -0.47/-0.53 e (sum -1, one phosphate), so a strand feels -(N-1) e \u2014 NAMD right, oxDNA approximating; do NOT rescale eField (corrupts the shared internal per-nt load). Also fixed 2 pre-existing frontend bugs the review surfaced: toast severity 'warn'\u2192'warning', and the V/m sub-panel's duplicate inline `display:grid` (started visible then inverted). ORACLE tests/test_namd_efield.py 24 (fast: unit constant from 1st principles, F=q\u00b7E inversion pins magnitude+sign+normalisation, zero/None no-op, conf emission on all 4 writers, byte-identical zero-field, remote+local resume preserve field+anchors, 3 REST guards; slow: real psfgen reads -1 e internal + terminal deficit from the actual PSF, unresolvable-anchor raise, and THE REAL-NAMD DIFFERENTIAL PROBE \u2014 field-on vs field-off at T=0 with one strand fixed: fixed atoms move exactly 0, free strand \u0394COM cosine 0.99996 along +dir, |\u0394COM| within 10% of \u00bd(F/M)t\u00b2 predicted from field_pN and NAMD's OWN -7 e strand charge (independent of KCAL_MOL_A_IN_PN \u2014 RED-checked: doubling the constant makes real NAMD falsify it). Gates: oracle 24/24; just test 4328 passed/72 skip/1 xfail (no drop); ruff clean; vitest 2294 (+5: ids-binding + two-instance independence + V/m grid); smoke green (1 pre-existing assembly_exit flake, passes isolated). Display-vs-oracle N/A (input picker, not a display); live card render+toggle+readout HAND-VERIFIED in the running app (throwaway spec, deleted) \u2192 MV row. Closes NAMD's anchor+field pair; M-ALL-ANCHORS-FIELD now needs only mrDNA M1,M2."
    },
    {
      "id": "N2",
      "track": "namd",
      "engine": "NAMD MD",
      "feature": "anchors",
      "title": "Anchors surfaced in the MD job UI: harmonic restraints (consref/conskfile already wired) driven by an anchor picker + shared scopes; optional fixedAtoms",
      "deps": [],
      "effort": "low",
      "leverage": "medium",
      "fast_slow": "fast+slow",
      "oracle": "FAST: restraints PDB marks exactly the resolved anchor atoms; conf enables constraints. SLOW: short run -> anchored atoms hold.",
      "milestone": "M-ALL-ANCHORS-FIELD",
      "status": "done",
      "notes": "NAMD anchors via fixedAtoms (Dirichlet-style hold; NOT the ramped consref/conskfile block \u2014 NAMD allows only one conskfile, already used by the slow-release all-DNA restraint, so anchors ride the orthogonal fixedAtoms mechanism and persist across the whole ladder). Backend (a JOB-REQUEST annotation, never a Design edit \u2014 Three-Layer Law): (1) namd_topology.resolve_anchor_residue_indices(design, anchors, *, model, full_topology) reuses the SHARED oxDNA scope resolver (resolve_anchor_particles: overhang/cluster/domain/strand/base) -> (helix,bp,dir) keys, then maps each to its 0-based residue ORDINAL in the built {stem}.pdb via built_pdb_residue_keys (the Atom.helix_id/bp_index/direction provenance, same bridge protein_enm uses). ORDINALS not segid/resid because psfgen blanks the segid col + the 1-char chain aliases past 62 strands. (2) md_protocols.write_anchor_restraints_pdb marks B=1 on the anchored residues' heavy atoms (0 else) by contiguity-walking the built PDB (same walk as the base-ring ENM's _parse_base_ring_residues); NAMD fixedAtomsCol B holds them immobile => 'held' follows from the exact marking + NAMD's fixed-atom guarantee, no GPU run needed for the fast oracle. (3) _segment_conf/_min_conf emit fixedAtoms on/File/Col B when anchors present; (4) prepare_mgh_slow_release(anchors=) resolves + writes restraints_anchors.pdb + threads it into every ladder conf + manifest; anchors=None is a byte-identical no-op. (5) CreateJobRequest.anchors field (captured in prep_params). REVIEW-CAUGHT (HIGH, fixed): the residue-ordinal bridge must mirror WHICH generator built the package PDB \u2014 export_pdb (require_full_topology=False, the mgh_slow_release default) groups chains in NATURAL first-occurrence order (A,B,..,Z,AA,..) while psfgen (require_full_topology=True) SORTS them lexicographically (A,AA,..,B); past 26 strands they diverge and the wrong order silently pins offset residues. Fix: built_pdb_residue_keys(sort_chains=) + resolve_anchor_residue_indices(full_topology=) selects the matching order (+ include_proteins), threaded from prepare's require_full_topology; proven on the 176-strand make_18hb_routed_design (export_pdb output residue sequence == natural, != sorted). UI (mirrors the shipped oxDNA/CanDo pickers): an Anchors card in the MD launch panel binds a 2nd initOxdnaAnchorsSetup (md-anchors-* ids), payload carries anchors:getAnchors()||null; main.js +1 line (getSelection dep). Oracle tests/test_namd_anchors.py 11 (9 fast + 2 slow: real psfgen prepare end-to-end + 176-strand export_pdb divergence proof): resolver base->exactly that nucleotide / strand->all its residues / stale->empty; sort_chains natural-vs-lexicographic divergence; marker PDB marks EXACTLY the resolved residue ordinals (heavy only, H free, HETATM never, RED empty->0 fixed); conf emits fixedAtoms only with anchors; end-to-end real prepare marks exactly + every ladder conf enables it + manifest records it. KNOWN edge (documented, low): anchoring a strand that contains extra-crossover-base inserts leaves the ssDNA inserts free (sentinel keys drop) \u2014 arguably correct (flexible inserts). NPT caveat noted in a code comment (fixedAtoms adds no virial; fine for a small end-anchor / NVT field run). Gates: oracle 11/11, just test 4226 passed / 66 skipped / 1 xfailed (was 4215, +11, no drop), ruff clean on touched (removed 1 pre-existing dead import), vitest 2214, smoke render/console-error gate green (2 pre-existing env failures reproduce on master: assembly_exit flake + harness loadScaffoldedPart). Live anchor-picker gesture NOT hand-driven (no GPU MD job) -> MV row. main.js LOC Delta=+1 (pure wiring). Substrate for N1 (an anchored E-field run reuses this fixedAtoms plumbing)."
    },
    {
      "id": "N3",
      "track": "namd",
      "engine": "NAMD MD",
      "feature": "extra-bases",
      "title": "Extra bases + linkers: validation coverage for the already-wired atomistic path + emit shared descriptors from MD frames",
      "deps": [],
      "effort": "low",
      "leverage": "medium",
      "fast_slow": "fast",
      "oracle": "FAST: atomistic model includes insert/linker atoms; descriptors computable from an MD frame.",
      "milestone": "M-FULL-COVERAGE",
      "status": "pending",
      "notes": ""
    },
    {
      "id": "N4",
      "track": "namd",
      "engine": "NAMD MD",
      "feature": "comparison-metric",
      "title": "NAMD contribution to comparison card: bridge existing md RMSF/metrics into shared descriptors; register NAMD as gold-override compare-source",
      "deps": [
        "S5"
      ],
      "effort": "medium",
      "leverage": "medium",
      "fast_slow": "fast+MV",
      "oracle": "NAMD descriptors selectable in card; when present, override reference per policy; MV live check.",
      "milestone": "M-FULL-COVERAGE",
      "status": "done",
      "notes": "NAMD is the FOURTH/LAST live source of the S5 comparison card (after O1 oxDNA + C5 CanDo + M5 mrDNA) and the GOLD-OVERRIDE reference: whenever a NAMD source is present it becomes the reference for EVERY observable (shape, RMSF, field), overriding the oxDNA-shape/CanDo-RMSF policy (shape_metrics.reference_for _GOLD_ENGINE='namd', already wired in S3). backend/core/namd_shape_source.py build_namd_shape_source(shape_frame, core_reference, *, rmsf_positions=None, field=None) \u2014 the NAMD twin of O1/C5/M5, same SOURCE-BUNDLE CONTRACT {engine:'namd', descriptors, rmsf, shape_frame, field}: core-filter the frame to the rigid dsDNA core (_filter_to_reference_core vs core_reference_geometry, ssDNA ends drop), emit NAMD's ABSOLUTE compute_shape_descriptors (S1), remap the per-nt rmsf->rmsf_nm. It is behaviorally the oxDNA builder with engine='namd' \u2014 md_rmsf emits the SAME positions shape as production_rmsf (each entry carries BOTH backbone_position AND rmsf, string direction), so shape+RMSF come from ONE md_trajectory.md_rmsf pass (Kabsch-aligned time-mean structure = the low-noise shape frame; per-nt trajectory variance = the RMSF). Route GET /md/jobs/{id}/shape-source (routes_md.py get_md_shape_source) reuses _md_traj_inputs (job's FROZEN prepared design snapshot, not live editor state) + _run_md_analysis(...'rmsf','md_rmsf'...) (shares the flexibility-map rmsf cache) -> passes result['positions'] as BOTH shape_frame and rmsf_positions -> {ready(=descriptors is not None), n_frames, ...bundle}; no-traj->ready:False. Frontend: api.getMdShapeSource + compare-card getSources fetches the selected MD job's bundle -> [oxdna, cando, mrdna, namd] (all four live); main.js captures const mdPanel + lazy getMdJob (no TDZ: mdPanel created via initMdJobsPanel BEFORE oxdnaPanel's init). Field deferred (field:None) \u2014 like O1/C5/M5. Oracle tests/test_namd_shape_source.py 8 (7 fast + 1 slow): engine tag+descriptor self-consistency, rmsf remap key+None-drop, core mask drops ssDNA ends, field passthrough, empty-core->None RED, THE HEADLINE gold-override ([oxdna,cando,namd]->references.shape=='namd' AND references.rmsf=='namd', overriding both policy engines) + negative control (absent NAMD -> shape=oxdna/rmsf=cando, proving the flip is caused by NAMD); SLOW (conftest-registered test_real_namd_trajectory_builds_ready_source): real 2hb NAMD DCD -> md_rmsf -> ready namd source, gold override holds on real data (this ran on-machine \u2014 the 2hb fixture is present). Fresh-context review: CONFIRMED-CORRECT, no bugs, no TDZ, Three-Layer clean. Gates: oracle 8/8, just test 4362 passed/72 skip/1 xfail (no drop; the prior xdist active-design flake test_namd_efield passed this run), ruff clean on touched (19 pre-existing debt in OTHER files untouched), vitest 2294, smoke green (pre+post). main.js LOC Delta=+3 (pure wiring). Display-vs-oracle: NOT a new card \u2014 new backend source route into the S5-validated render path (like C5/M5) -> live 4-engine eyeball MV-21 (updated with the N4 slice)."
    },
    {
      "id": "O1",
      "track": "oxdna",
      "engine": "oxDNA",
      "feature": "comparison-metric",
      "title": "Emit shared descriptors from oxDNA relaxed/production frames into the comparison card (the reference column for shape + field-deflection)",
      "deps": [
        "S1"
      ],
      "effort": "low",
      "leverage": "high",
      "fast_slow": "fast",
      "oracle": "oxDNA descriptors match existing oxdna_health twist/curvature values within tol; selectable + default reference in card.",
      "milestone": "M-CANDO-FIELD",
      "status": "done",
      "notes": "Wires the FIRST live source into the S5 comparison card (was getSources:()=>[]). backend/core/oxdna_shape_source.py build_oxdna_shape_source(shape_frame, core_reference, rmsf_positions?, field?) \u2014 PURE Physical-layer assembly: core-filters the relaxed frame to the rigid dsDNA core (_filter_to_reference_core against core_reference_geometry, same mask the metrics use; ssDNA ends dropped), computes compute_shape_descriptors (S1) on it, maps production_rmsf positions ({...,'rmsf'}) -> the card's rmsf profile shape ({helix_id,bp_index,direction,copy,rmsf_nm}), returns {engine:'oxdna', descriptors, rmsf, shape_frame(core), field}. Empty core mask -> descriptors/shape_frame None (no comparable frame). Route GET /oxdna/jobs/{id}/shape-source in routes_oxdna.py reads the latest relaxed last_conf via _relaxed_full_map (SAME frame /display shows) + optional RMSF from any production/field stage with frames; returns {ready, stage_name, n_frames, ...bundle}. Frontend: api.getOxdnaShapeSource + oxdna_jobs_panel getSources now async-fetches the selected job's bundle -> [src] (main.js Delta=0). REVIEW-CAUGHT (claim, not bug): descriptors are oxDNA's ABSOLUTE twist/bend on the relaxed frame (the cross-engine-comparable value), NOT the DIFFERENTIAL (measured-analytic) twist the Graphs-&-Metrics card plots over the production trajectory -> corrected the docstrings + MV-21 wording so nobody expects the two cards to show equal numbers; absolute is the right choice for oxDNA-abs-vs-CanDo-abs. Oracle tests/test_oxdna_shape_source.py 7 fast: descriptors==measure_bundle_twist(core) (same estimator, self-consistent), core mask drops ssDNA ends, rmsf 'rmsf'->'rmsf_nm' remap (None dropped), field passthrough, bundle drops into build_comparison_report as ready oxdna SHAPE reference, RED: empty core ref -> None descriptors. Gates: 7/7 oracle, just test 4177 passed/66 skip/1 xfail, ruff clean on touched, vitest 2200, smoke green (pre-work). Live-on-real-relaxed-job eyeball = MV-21 (updated: oxDNA column now live; cross-engine deltas await C5). Field emission from a field JOB (field_response_profile w/ pre-field ref + anchor_keys) NOT done here -> field:None for now; a natural follow-up when C2 needs the oxDNA field reference."
    },
    {
      "id": "O2",
      "track": "oxdna",
      "engine": "oxDNA",
      "feature": "maintenance",
      "title": "Close residual oxDNA gaps as they surface (extra-base CG render v2; linker coverage completeness)",
      "deps": [],
      "effort": "low",
      "leverage": "low",
      "fast_slow": "fast",
      "oracle": "per-gap; only pull when a concrete gap is logged.",
      "milestone": "M-FULL-COVERAGE",
      "status": "pending",
      "notes": ""
    },
    {
      "id": "U1",
      "track": "unified-panel",
      "engine": "all",
      "feature": "engine-consolidation",
      "title": "Engine capability descriptor + registry: one source of truth (engine_capabilities.js) mapping each engine -> {supported cards, protocols, advanced-param schema}; the data that drives one shared card stack instead of 6 bespoke panels",
      "deps": [],
      "effort": "low",
      "leverage": "critical",
      "fast_slow": "fast",
      "oracle": "PARITY census: each engine's descriptor lists EXACTLY the cards its current bespoke panel renders today (oxDNA/LAMMPS/mrDNA/CanDo/NAMD vs the index.html ox-card inventory); unsupported cards are marked present-but-disabled, never absent (NN/G spatial-consistency). Pure; can-go-red if a descriptor omits or invents a card for an engine.",
      "milestone": "M-UNIFIED-PANEL",
      "status": "done",
      "notes": "engine_capabilities.js: per-engine card matrix (8 cards) + protocols + advanced-param schema; unsupported cards present-but-disabled w/ reason (not absent). PARITY oracle (19 tests): census matches descriptor + every enabled anchor EXISTS in index.html + every unsupported probe ABSENT. Caught real lammps joblist anchor (bare lammps-jobs-list, no toggle). Pure data+helpers; main.js LOC delta=0."
    },
    {
      "id": "U2",
      "track": "unified-panel",
      "engine": "all",
      "feature": "forces-card",
      "title": "Shared Forces card factory: collapse the triplicated E-field/Anchors/Surface cards (efield_setup.js / cando_efield_setup.js / lammps_forces_setup.js) into one initForcesCard({engine,deps}) over the shared efield_math/oxdna_floor_math; grey unsupported sub-cards per the U1 descriptor",
      "deps": [
        "U1"
      ],
      "effort": "medium",
      "leverage": "high",
      "fast_slow": "fast+MV",
      "oracle": "PARITY: for each engine the unified factory emits the SAME force payload (field vector, anchors, surface) its bespoke card produced today (shape/byte parity per engine); an unsupported sub-card is greyed not absent. ADAPTED-CODE PIN RULE (CLAUDE.md): prove each moved helper in-place-first or via a git-stash rerun, not green-first-run. Stateful -> one app exercise + smoke.",
      "milestone": "M-UNIFIED-PANEL",
      "status": "done",
      "notes": "Shared Forces (E-field) card factory: frontend/src/ui/forces_card.js initForcesCard({engine,ids?,gizmo?,getBaseCount?,getAnchorCount?,onChange?}) -> {getFieldSpec,isEnabled,refresh,applyConfig,detachGizmo}; getFieldSpec()->{field_pN,dir,enabled}. Collapses the 3 triplicated field cards: DELETED efield_setup.js (oxDNA, gizmo) + cando_efield_setup.js (CanDo+NAMD, numeric); the LAMMPS field third of lammps_forces_setup.js now DELEGATES to the factory (its Anchors+Surface cards + public API getForces/getAnchors/fieldNeedsAnchor/detachGizmo unchanged). Rewired call sites: main.js (engine:'oxdna'+gizmo), cando_jobs_panel.js (engine:'cando'), md_jobs_panel.js (engine:'namd'). Per-engine divergences are DATA (FORCES_FIELD_VARIANTS/FORCES_FIELD_IDS), not code paths: gizmo-vs-numeric (driven by whether a gizmo is passed), V/m sub-panel (driven by DOM-id presence -- LAMMPS omits vpm ids), default dir [0,1,0] vs LAMMPS [1,0,0], ready-line style (apply 'needs >=1 anchor' verb run/solve vs lammps weak-warn + contextual anchor note from getAnchorCount), gizmo-visibility gate (open-or-job oxDNA vs open-and-enabled LAMMPS), job-arrow persistence, close-on-leave-tab. ORACLE (forces_card.test.js, 14 FAST): PER-ENGINE PARITY as the ADAPTED-CODE PIN -- drove the LIVE old factories AND the new one through the same input sequence on fresh DOMs and asserted byte-equal payloads for all 4 engines (13/13 green while both existed), then durable explicit per-engine payload + gizmo-drag + applyConfig + V/m + ready-line pins; the refactored LAMMPS module still passes its 9 PRE-EXISTING tests (behaviour-preservation, tests predate the change). All field DOM ids verified present in index.html (cards mount in the real app). Fresh-context review caught ONE real regression (LAMMPS gizmo lingered cross-tab when card open+enabled -- old LAMMPS detached unconditionally; shared handler's else _syncGizmo() re-attached under the open-and-enabled gate) -> FIXED (handler now _close_()s where closeOnLeaveTab else gizmo.detach() directly) + RED-verified regression test (both oxDNA+LAMMPS detach on leave). Gates: 14/14 oracle, frontend 182 files/2316 passed (-16 = 2 deleted bespoke test files folded in + preserved LAMMPS tests, no product regression; +1 regression test), smoke 23/23 console-error gate, ruff N/A (frontend-only, no .py). main.js LOC Delta=+1 (import swap + engine:'oxdna' arg; field logic MOVED out, factory init is thin wiring). Display-vs-oracle: DOM byte-identical rewire (same ids/markup, JS factory swapped) -> covered by parity oracle + id-presence + smoke boot-clean; live 4-panel field-card gesture (esp. gizmo drag) owes MV-22. M-UNIFIED-PANEL still needs U3 (jobs-panel base) + U4 (engine selector)."
    },
    {
      "id": "U3",
      "track": "unified-panel",
      "engine": "all",
      "feature": "jobs-panel-base",
      "title": "Shared jobs-panel base: extract the common run-buttons + job-list (via job_tree) + Advanced-card host from the 5 bespoke *_jobs_panel.js into initJobsPanelBase({engine,descriptor,deps}); each engine supplies its U1 descriptor",
      "deps": [
        "U1"
      ],
      "effort": "high",
      "leverage": "high",
      "fast_slow": "fast+MV",
      "oracle": "PARITY: the base renders each engine's job list + run controls identically to its bespoke panel (job-row model, run-button enablement) reusing the already-shared job_tree/metrics_card/shape_compare; pure helpers unit-tested; stateful -> app exercise + smoke. Adapted-code pin rule applies.",
      "milestone": "M-UNIFIED-PANEL",
      "status": "done",
      "notes": "SLICE 1 shipped 2026-07-08. User steer: oxDNA panel is CANONICAL; all engines converge to conform to it. Extracted the oxDNA row/list SHAPE into PURE jobs_panel_model.js (buildJobRowModel/buildJobListModel/jobListSignature/runButtonEnabled) + DOM jobs_panel_render.js (renderJobRow/renderJobList). Rewired oxDNA onto them = BYTE-IDENTICAL DOM (parity pin in jobs_panel_model.test.js drives a VERBATIM copy of the old _jobRow/_renderList vs new \u2192 identical outerHTML across every branch; +oxDNA panel 87/87 +smoke). Converged mrDNA (smallest) = intentional visual upgrade (rows gain [N] index + spinner-while-running + legend + poll short-circuit); bespoke mrDNA row rendering DELETED. makeSpinner moved to job_status_symbol.js (re-exported from oxDNA for importers/tests). main.js LOC \u0394=0; net -48 LOC across panels. Gates: oracle 9/9, frontend 183/2325 (+9 no drop), smoke 23/23, ruff N/A (frontend-only). Live mrDNA-row pixels owe MV-23. REMAINING (U3 not done): converge cando/lammps/md onto the renderer (md=2882-line outlier: tree+collapse chevron, setInterval + remote/Alpine timers, no returned api); factor the shared run-button/poll/collapse/advanced-drawer into a stateful initJobsPanelBase({engine,descriptor,deps}) each engine drives with its U1 descriptor; runButtonEnabled exists but no panel consumes it yet. SLICE 2a shipped 2026-07-08 (U3 still in_progress). Converged cando + lammps job LISTS onto the canonical model+renderer (flat like mrDNA). Renderer gained an OPTIONAL trailing per-row control (m.action/onAction, gated on ctx.rowAction) so LAMMPS keeps its inline Stop button (stopPropagation \u2192 no row-select); oxDNA/mrDNA/cando omit rowAction \u2192 action=null \u2192 no button \u2192 oxDNA BYTE-PARITY UNCHANGED (pin still green). cando: Coarse/Fine mode rides the canonical leading-tag slot (the [AR] slot), old row + statusBadge/statusKeyFor imports DELETED; both panels gained [N] index + spinner-while-active + legend + poll-sig short-circuit. Oracle jobs_panel_model.test.js +5 it() (14/14) \u2014 CONFORMANCE (mode-as-tag, active-only Stop that fires onAction w/ stopPropagation, no-rowAction\u21d2no-button guard) + the existing oxDNA byte-parity pin. Gates: affected panels 166/166, full frontend 2330/2330 (+5 no drop), smoke 23/23, frontend lint N/A. main.js LOC \u0394=0. Live cando/lammps row pixels owe MV-24. REMAINING (U3 not done): slice 2b converge md (2882-line outlier: hierarchical flattenJobTree + collapse chevron + ensemble summary + setInterval/remote timers, no api) \u2014 renderer needs an optional chevron field gated on childCount>0 without breaking oxDNA byte-parity; slice 2c factor the shared run-buttons + poll loop + section-collapse + advanced-drawer into a stateful initJobsPanelBase({engine,descriptor,deps}) (runButtonEnabled built+tested, no panel consumes it yet). SLICE 2b shipped 2026-07-08 (U3 still in_progress). Converged the 2882-line NAMD (md) job LIST onto the canonical model+renderer \u2014 the renderer now serves 5/5 panels. Renderer gained THREE generic optional slots, each gated on ctx opt-in so oxDNA byte-parity is untouched: chevron (leading expand/collapse span; leaves get an empty spacer, parents get \u25b8/\u25be \u2192 onChevron, stopPropagation), postLabelMarkers(job,{childCount,collapsed}) (spans between label and timestamp), symbolOverride(job) (replaces the spinner/badge status glyph, carries an optional dataset). buildJobListModel now threads childCount from flattenJobTree; collapsed derives from ctx.collapsedIds. NAMD's Fix button reuses the existing trailing action slot. md: row ctx EXTRACTED to a module-scope pure factory mdJobRowCtx({selectedId,collapsedIds,jobs,dimColor,warnColor,formatTime}) + shared mdJobRowSig (mdListSignature and rowSig now key off the same per-row fields); _renderList rewired through jobListSignature/buildJobListModel/renderJobList (Alpine-ensemble auto-collapse preserved); bespoke _jobRow + statusBadge/statusKeyFor/makeStatusLegend/flattenJobTree imports DELETED (net -104 source LOC). Oracle md_jobs_panel.test.js +9 it (payload parity: chevron+summary, replica labels, seed/Alpine post-label badges, \u29d7 remote-queued symbolOverride + [data-md-queued] hook, Fix-action-only-on-OOM + out-of-date, chevron-click fires onChevron not onClick, leaf gets empty chevron); oxDNA byte-parity pin jobs_panel_model.test.js re-ran 14/14 green. Gates: jobs-panel files 237/237, full frontend 2339/2339 (+9 no drop), smoke 23/23, frontend lint N/A. main.js LOC \u0394=0. Live NAMD-row pixels owe MV-25. REMAINING (U3 not done): slice 2c \u2014 factor the shared run-buttons + poll loop + section-collapse + advanced-drawer host into a stateful initJobsPanelBase({engine,descriptor,deps}) (runButtonEnabled built+tested, still no panel consumes it). SLICE 2c-1 shipped 2026-07-08 (U3 still in_progress). Extracted the shared STATEFUL scaffold \u2014 section-collapse + advanced-parameters drawer + REST poll loop \u2014 into a new factory initJobsPanelBase({section,els,pollMs,hasActive,tick,onOpen,onClose,arrowStyle}) in jobs_panel_base.js (129 LOC) with pure exported decisions (bodyDisplay/arrowChar/shouldPoll/applyArrow). Converged mrDNA + CanDo (the two panels whose bespoke _applyCollapsed/heading-click/advToggle/_clearPoll/_scheduleNextPoll were BYTE-IDENTICAL) onto it: each deletes ~21 LOC of scaffold + keeps its own onOpen/hasActive hooks; CanDo's separate display-card collapse stays bespoke. Oracle jobs_panel_base.test.js (+17 it): pure-helper units + a jsdom CONFORMANCE block pinning the exact bespoke DOM effects (collapse toggles body+arrow+persists via section_collapse_state; adv drawer shows/hides body+\u25be/\u25b8; poll fires tick after pollMs ONLY when open&&active, cleared on collapse) with fake timers. arrowStyle supports text/class/rotate (all unit-tested) but only 'text' has a live consumer now \u2014 so converging LAMMPS('class' + poll lacks open-guard = adapted)/oxDNA/md('rotate' + inline-collapse + remote setInterval) is a follow-on slice 2c-2. Gates: base+mrdna+cando+model tests 83/83, full frontend 2356/2356 (+17 no drop), smoke 23/23, one-off Playwright exercised mrDNA+CanDo collapse+adv gestures in-app (zero console errors, deleted after), frontend lint N/A. main.js LOC \u0394=0; panels \u221242 LOC. No new pixels (behavior-preserving) \u2192 no MV row owed. SLICE 2c-2 shipped 2026-07-08 (U3 still in_progress). Converged the LAMMPS panel scaffold onto initJobsPanelBase \u2014 the FIRST live consumer of arrowStyle:'class' (section arrow via the is-collapsed class) and the first onClose cleanup hook (_viewsOff()+forcesSetup.detachGizmo()); dropped section_collapse_state import + _pollTimer + the _applyCollapsed/heading/advToggle/_schedulePoll/_clearPoll blocks (net -9 LOC, 334->325); rewired _schedulePoll->_base.schedulePoll, mount->_base.initCollapsed(true), body-display reads->_base.isOpen(). The base adds the open-guard the bespoke LAMMPS poll lacked = the ONE adapted delta (incidental hardening: cancels a collapse-during-in-flight-fetch timer the old code would have armed). Bespoke viz-card collapse (no persistence) stays bespoke. Oracle lammps_jobs_panel.test.js +3 (19/19): a PARITY block drove the REAL initLammpsJobsPanel factory through the actual heading-click collapse (body hidden + is-collapsed + view reset to Off + detachGizmo), the advanced-drawer toggle (advBody + text arrow), and the open+active poll gate (fires while open, STOPS once collapsed, fake timers) \u2014 run GREEN against the pre-rewire code FIRST (pins real behaviour, not the base wiring) then green after = adapted-code pin (not green-by-construction). Fresh-context review CONFIRMED all six parity checks (collapse, adv drawer, poll, no-dangling-refs, viz-card untouched, no TDZ) + flagged the open-guard as a fix not a regression. Gates: LAMMPS 38/38, full frontend 2359/2359 (+3 no drop), smoke 23/23, one-off Playwright drove the real LAMMPS collapse+adv gesture in-app (0 console errors, deleted after), frontend lint N/A, no Python touched. main.js LOC \u0394=0. No MV row owed (behaviour-preserving, byte-identical DOM). REMAINING (U3 not done): slice 2c-3 converge oxDNA + md scaffold onto the base (both use a module-level _collapsed var + header-toggle + arrowStyle:'rotate' adv drawer; oxDNA section arrow is actually 'class' not 'rotate'; md adds a 2nd remote setInterval poll + cross-panel md-collapse coordination); then U4 selector. | SLICE 2c-3a shipped 2026-07-08: converged the oxDNA panel's SECTION-COLLAPSE + POLL onto the shared initJobsPanelBase (arrowStyle:'class'; hasActive gate = visible-active OR selected-running; removed module-level _collapsed/_pollTimer/_scheduleNextPoll; _revealMdPanel + left-tab-change + workspace-path-change + init-ordering all rewired to _base.isOpen()/applyCollapsed/clearPoll/schedulePoll/initCollapsed). ADVANCED drawer LEFT bespoke on purpose (oxDNA's _advOpen boolean opens on first click; the base reads display and the markup's `display:none;display:grid` computes visible, so converging would flip first-click). PARITY oracle drives the REAL initOxdnaJobsPanel: section arrow (is-collapsed class), poll-fires-while-open+active, poll-STOPS-on-collapse \u2014 the stop-on-collapse assertion FAILED pre-rewire (3 vs 2: bespoke let one trailing poll fire) and PASSES post-rewire = adapted delta proven, not green-by-construction. oxdna_jobs_panel.test.js +3 (90/90); full frontend 2362/2362 (+3, no drop); smoke 23/23; main.js \u0394=0; no Python. Fresh-context review: all 6 adapted sites CONFIRMED equivalent + no TDZ. In-app collapse GESTURE not hand-driven (one-off Playwright blocked by unrelated left-tab-switch plumbing) -> MV-26. REMAINING for U3: slice 2c-3b (converge md scaffold: module-level _collapsed reads + 2nd remote poll + cross-panel md-collapse coordination), then U4. | SLICE 2c-3b shipped 2026-07-08 \u2014 CLOSES U3 (all 5 panels converged). Converged the 2803-line md (NAMD) panel's SECTION-COLLAPSE + ADVANCED DRAWER onto initJobsPanelBase (the 5th/last consumer). Investigation SHRANK the feared scope: md's `_collapsed` lived in only 3 logical sites (init-apply, heading handler, mount gate) \u2014 NOT '~6 scattered reads'; md uses NO primary setTimeout poll (`_pollTimer` was vestigial/dead \u2014 declared+cleared, never scheduled); live updates ride a WebSocket + `_remotePollTimer` (setInterval) + `_prewarmTimer` + `_displayTimer`, ALL bespoke, torn down by the onClose hook. `const _base = initJobsPanelBase({section:'md-jobs-panel', els:{heading,body,arrow,advToggle,advArrow,advBody}, arrowStyle:'class', advArrowStyle:'rotate', onOpen:()=>_onOpen(), onClose:()=>{_stopMdPrewarm();_stopRemotePoll()}})`; NO tick/hasActive (md has no primary poll \u2192 base's clearPoll is a harmless no-op). Deleted `let _collapsed`/`let _advOpen` + the bespoke collapse+adv blocks; removed the now-unused getSectionCollapsed/setSectionCollapsed import; rewired the terminal `if(!_collapsed) _onOpen()`\u2192`_base.initCollapsed(true)` (same end-of-init slot \u2192 preserves apply-then-onOpen ordering). KEY DIFFERENCE FROM oxDNA (2c-3a): md's ADVANCED DRAWER CONVERGES TOO \u2014 its markup (index.html:4588) is a clean `display:none` (no `display:none;display:grid` double-declaration), so the base's display-reading toggle opens on first click exactly as md's old `_advOpen`-false-start boolean did \u2192 no flip hazard. PARITY oracle md_jobs_panel.test.js +3 (drives the REAL initMdJobsPanel via an auto-mocked api client: section collapse toggles body-display + is-collapsed arrow; adv drawer first-click-opens + rotate(90deg) arrow; open fires _onOpen fetch, collapse STOPS the remote SLURM poll via onClose) \u2014 behaviour is byte-identical (no oxDNA-style delta), so proven via GIT-STASH RERUN: the 3 specs pass against the stashed bespoke md.js AND the converged code = behaviour-preserving adapted-code pin, not green-by-construction. Fresh-context review: all 6 review points CONFIRMED, no TDZ (_onOpen/_stopMdPrewarm/_stopRemotePoll are hoisted function decls referenced via lazy arrows), no stray reader, _collapsedParents (chevron tree) untouched, cross-panel _revealMdPanel reveal intact. Gates: md 75/75 (+3), full frontend 2365/2365 (+3, no drop), smoke 23/23, frontend lint N/A, no Python. main.js LOC \u0394=0. Live md collapse+adv gesture not hand-driven (behaviour-preserving, DOM-pinned by the real-panel PARITY + smoke boot) -> MV-27. RUN-BUTTON HOST deliberately NOT extracted into the base: run controls are genuinely engine-divergent (Relax/Production vs Coarse/Fine vs Alpine-submit vs LAMMPS) \u2014 factoring them would give the base many reasons to change (violates cohesion); the shared de-duplication (canonical job-list renderer + collapse/advanced/poll base across all 5 panels) is the U3 deliverable, and the pure `runButtonEnabled` helper remains available for U4's selector."
    },
    {
      "id": "U4",
      "track": "unified-panel",
      "engine": "all",
      "feature": "engine-selector",
      "title": "Engine selector + single Simulate section: collapse the 6 sidebar panels into ONE collapsible section with an engine segmented-control/dropdown; the card stack is driven by the U1 descriptor, unsupported cards greyed with a why-tooltip (CHARMM-GUI model)",
      "deps": [
        "U2",
        "U3"
      ],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast+MV",
      "oracle": "selecting engine X shows EXACTLY X's supported cards (from U1) and greys the rest; pure selector-state helper unit-tested; one-off display-vs-oracle Playwright (rendered card set == descriptor) + MV row. main.js LOC flat-or-lower (consolidation, not growth).",
      "milestone": "M-UNIFIED-PANEL",
      "status": "done",
      "notes": "DONE 2026-07-08. engine_selector.js: pure panelVisibility/selectedEngineCards derive from U1 descriptor + initEngineSelector factory (segmented control shows selected panel/hides other 4 + capability strip: supported chips + greyed-with-tooltip unsupported). Simulate section in index.html fronts the 5 stacked panels; main.js +16 pure wiring (import+factory init, no cohesive logic). Oracle engine_selector.test.js 11/11 (pure state proven vs U1 enabledCardKeys/cardReason + jsdom factory panel show/hide + chip census). Frontend 2376/2376 (+11 no drop), smoke 23/23, lint clean for my files (19 pre-existing Python ruff errors untouched). One-off Playwright confirmed real-app DOM (5 buttons, panel show/hide per engine, chip set==descriptor, greyed titles) but ran vs welcome overlay \u2192 live-with-design gesture+hover-tooltip deferred to MV-28. De-dup/capability proven: unsupported cards greyed-not-absent, single U1 source drives visibility."
    },
    {
      "id": "P1",
      "track": "job-planner",
      "engine": "all",
      "feature": "chaining",
      "title": "MdPipeline stage-spec data model: a generic ordered list of stage specs {engine, protocol, forces(field/anchors/surface), run_target} with provenance, generalizing the special-cased parent_job_id + run_kind='production' + seed_oxdna/mrdna_job_id hops into ONE object",
      "deps": [],
      "effort": "medium",
      "leverage": "critical",
      "fast_slow": "fast",
      "oracle": "a 3-stage plan builds N child MdJobs chained by parent_job_id, stage N start_checkpoint == stage N-1 output; a 1-stage plan is byte-identical to today's spawn_md_production (parity, no regression). Pure builder; can-go-red if a stage's seed points at the wrong checkpoint.",
      "milestone": "M-JOB-PLANNER",
      "status": "done",
      "notes": "Opens Track P (the chaining backbone every other P task builds on). backend/core/md_pipeline.py = the generic MdPipeline data model + a PURE chain builder (no job creation, no submission, no topology touch \u2014 Three-Layer clean, imports only md_ensemble). PipelineStage{engine,protocol,field,anchors,surface,run_target,cluster_name,length_ns,steps,label} + .forces() bundle; MdPipeline{stages,root_job_id,root_engine} + to_dict/from_dict round-trip (P2/P4 persist it). build_pipeline_plan(pipeline,*,root_checkpoint,base_seed=54321,stage_id_for) resolves the ordered stages into a linear chain of StagePlan descriptors: stage 0 seeds from root_job_id@root_checkpoint; stage N>0 seeds from its IMMEDIATE predecessor (parent_job_id=stage{N-1}, start_checkpoint=stage_output_ref('stage{N-1}')='stage{N-1}::output'). Every hop run_kind='production'; seeds = md_ensemble.generate_seeds(base,n) (base..base+n-1) so a 1-stage plan's seed==54321==spawn_md_production's first-child seed. cross_engine flag (parent_engine!=stage.engine) REPRESENTS the seed_oxdna/mrdna generalization; P3 does the coordinate conversion. GENERALIZES the 3 special-cased provenance hops (parent_job_id + run_kind='production' + seed_oxdna/mrdna_job_id) into ONE object. ORACLE tests/test_md_job_pipeline.py 9 FAST: forces-bundle grouping, empty/bad-run_target/no-engine validation raises, 3-stage chain each-from-immediate-predecessor with LITERAL-CONSTANT red guards (parent!=root, checkpoint!=root's, not-two-back), distinct per-stage seeds==generate_seeds, 1-stage parity vs spawn_md_production provenance (parent_job_id/run_kind/seed=54321/checkpoint-passthrough/cross_engine False), cross-engine hop flagged (oxdna->namd), dict round-trip + StagePlan.to_dict JSON-serializable. RED-VERIFIED offline: a wrong-predecessor mutation (always-seed-from-root) fails the chaining test; reverted->green. Fresh-context review: CONFIRMED-CORRECT, no bugs, no Three-Layer violation, oracle genuinely red-capable (noted a by-design semantic: a pipeline stage's seed axis = sequential stages, not production-sibling fan-out; only 1-stage parity is required + holds). Gates: oracle 9/9, just test 4393 passed/110 skip/1 xfail (+9 new, no drop; xdist flake didn't fire), ruff clean on touched (20 pre-existing debt in OTHER files untouched). No card/UI -> display-vs-oracle N/A (like C1/C2); P4 owes the planner-UI MV. main.js LOC delta=0 (backend-only). Substrate for P2 (executor consumes StagePlan) + P3 (cross_engine hop) + P4 (Plan Run UI builds an MdPipeline)."
    },
    {
      "id": "P2",
      "track": "job-planner",
      "engine": "all",
      "feature": "chaining",
      "title": "Chain executor: run stages back-to-back unattended (on stage completion submit the next), local + Alpine targets, partial-failure recovery (halt + resume-from-failed-stage, not full restart) reusing resumable/resubmit_count",
      "deps": [
        "P1"
      ],
      "effort": "high",
      "leverage": "critical",
      "fast_slow": "fast+slow",
      "oracle": "FAST: a mocked 3-stage chain advances stage->stage on completion and HALTS on a stage failure with the chain resumable from the failed stage (LogRocket retry-only-failed); a stage's forces/anchors carry into its child conf (reuses external_forces_block). SLOW: a real 2-stage local chain runs unattended end-to-end.",
      "milestone": "M-JOB-PLANNER",
      "status": "done",
      "notes": "The chain EXECUTOR (P1 gave the data model + pure builder). backend/core/md_chain_executor.py = an engine-agnostic state machine that turns an MdPipeline into a live, self-advancing chain. BRIGHT LINE (CHAIN): stage N runs SEEDED FROM stage N-1's realised child on completion; a stage FAILURE HALTS the chain (no downstream spawn); resume_chain retries-ONLY-failed (completed stages keep their job_id + done status, never re-run). Engine touch-points are two INJECTED callbacks only: spawn(ctx)->job_id + job_status(job_id)->running|completed|failed. Primitives reconcile_running/next_spawn/mark_spawned let the async driver await a real spawn between two pure transitions; step_chain composes them sync (the oracle). resume_chain resets from failed_stage_index() down to pending. Persistence save/load/list_chains -> workspace/md_chains/{id}/chain.json round-trips plan+stage states. stage_forces_conf(forces) reuses md_protocols.external_forces_block so a stage's field/anchors reach the child conf through the SAME fixedAtoms+eField block every launch card writes. NAMD wiring lives in the API layer (backend/core imports nothing from backend/api): routes_md._chain_job_status maps MdStatus; _chain_spawn REUSES spawn_md_production VERBATIM (it already seeds a production child from ANY completed job -- relaxation OR production -- which IS the chain hop; run target/length flow from the stage, local autostarts, alpine queues); advance_chains(workspace) drives every persisted chain one transition and is called from the MD supervisor loop (main.py) -> a chain marches stage-to-stage UNATTENDED. Routes POST /md/chains (build+persist+kick stage 0) + GET /md/chains[/{id}] + POST /md/chains/{id}/resume. ORACLE tests/test_md_chain_executor.py 12 FAST (mocked spawn/status, RED-verified offline via 2 mutants -- always-seed-from-root fails test_advances_seeded_from_predecessor+test_resume; swallow-failure fails test_halts): advance-seeded-from-predecessor (literal parent!=root guard), single-stage one-spawn, running-blocks-next, halt-on-failure-no-downstream, resume-reruns-ONLY-failed (stage0 keeps job0, spawn list [0,1,1] never re-runs 0), resume-noop-on-non-failed, forces-carry-into-SpawnContext, stage_forces_conf==external_forces_block, dict roundtrip, save/load/list. Route oracle tests/test_md_milestone1.py::TestMdChain 5 (real stage-0 production child, start stubbed): create-spawns-real-child, create-requires-completed-root (400), halt-on-failure-then-resume-from-failed (new stage0 child, stage1 stays pending), resume-rejects-non-failed, transient-spawn-failure-retries-then-halts. Fresh-context review: CONFIRMED the core state machine has NO double-spawn/skip/re-run-completed/fail-to-halt bug, persistence round-trips, failed_stage_index+resume-reset correct, no off-by-one, no event-loop concurrency double-spawn (no await suspension inside a spawn). Acted on its 3 edge findings: (1 MED/HIGH) broad-except spawn failure was a PERMANENT halt -> HARDENED advance_chains with a bounded retry (StageState.spawn_attempts, _MAX_STAGE_SPAWN_ATTEMPTS=3, mirrors namd_runner MAX_*_RESUMES): a transient precondition (e.g. previous stage's remote outputs not yet downloaded) leaves the stage pending + retries next supervisor tick, halting only past the cap; resume_chain resets the budget (test_transient_spawn_failure_retries_then_halts). The ALPINE root cause it flagged -- md_executor.fetch_outputs marks a remote job completed even when the output download failed, so the seed checkpoint can be missing while status=completed -- is a PRE-EXISTING md_executor bug out of P2 scope -> filed to issues_ledger. (2 LOW) _chain_spawn discards the plan's per-stage seed (spawn_md_production re-derives it from sibling count -> every chain stage gets 54321; harmless since stages seed from DIFFERENT coords) -> documented follow-up, rides with the reseed-conf ProductionRunRequest additions. (3 latent) added the all-or-nothing spawn-invariant comment (the except path assumes no job was created; true today since spawn_md_production only raises on preconditions before start). KNOWN P2 FOLLOW-UP (documented): forces threaded to the conf-SNIPPET level (external_forces_block) but not yet into the production RESEED conf end-to-end -- needs ProductionRunRequest.field/anchors(+seed) + reseed emission. SLOW real 2-stage local chain NOT run (needs live NAMD) -> owes an MV row (precedent md_cutoff/N1). Three-Layer clean (forces = job-request annotations, no Design/topology touch). Gates: oracle 12/12 + route 4/4, just test 4409 passed/110 skip/1 xfail (was 4393, +16, no drop, xdist flake didn't fire), ruff clean on all touched. main.js LOC delta=0 (backend-only, no frontend). Substrate for P3 (cross_engine hop) + P4 (Plan Run UI builds an MdPipeline)."
    },
    {
      "id": "P3",
      "track": "job-planner",
      "engine": "all",
      "feature": "cross-engine-chaining",
      "title": "Cross-engine output->input: generalize seed_oxdna_job_id/seed_mrdna_job_id so a plan can hand one engine's relaxed coords to another (e.g. oxDNA relax -> NAMD production) in the correct coordinate convention",
      "deps": [
        "P1"
      ],
      "effort": "medium",
      "leverage": "high",
      "fast_slow": "fast+slow",
      "oracle": "FAST: a 2-stage cross-engine plan seeds stage 2 from stage 1's frame with the right frame/units (parity with the existing seed hop). SLOW: a real oxDNA->NAMD seed handoff produces a valid start structure.",
      "milestone": "M-JOB-PLANNER",
      "status": "done",
      "notes": "Cross-engine coordinate handoff wired through the P2 chain executor. KEY INSIGHT (investigation-confirmed): the oxDNA/mrDNA->NAMD coordinate+unit conversion ALREADY EXISTS and is validated -- oxdna_runner.build_namd_seed reconstructs an atomistic model from the oxDNA relaxed last_conf via CG spline (sim-units->nm inside read_configuration), mrdna_runner.build_namd_seed_from_mrdna does the mrDNA analogue (Angstrom->nm in mrdna_bridge); both feed build_atomistic_model + are wired into create_md_job via oxdna_job_id/mrdna_job_id -> MdJob.seed_oxdna/mrdna_job_id. So P3 was NOT a new unit conversion but ROUTING the chain's cross-engine hop through that existing converter. The seam was _chain_spawn (routes_md.py), which ignored plan.cross_engine/parent_engine and ALWAYS called spawn_md_production (a same-engine NAMD .coor/.xsc checkpoint restart that _load_job's the parent as an MdJob -> fails on an oxDNA/mrDNA parent). THREE additive changes: (1) backend/core/md_pipeline.py gained the PURE resolver cross_engine_seed(plan, resolved_parent_job_id)->CrossEngineSeed|None generalizing the two hand-wired seed hops into one table CROSS_ENGINE_SEED_FIELD={oxdna:'oxdna_job_id',mrdna:'mrdna_job_id'}; returns None for a same-engine hop (checkpoint restart), raises ValueError on an unsupported hop (sink!=namd -- only NAMD rebuilds atomistic from coarse; unknown source engine; unresolved parent). seed_job_id is the RESOLVED predecessor (next_spawn's chain.root_job_id/prev child), never the stageN placeholder. (2) routes_md._chain_spawn now branches on cross_engine_seed: cross-engine -> build a CreateJobRequest(oxdna_job_id|mrdna_job_id=parent, field/anchors from the stage forces, run_target, autostart) and call create_md_job (the create-time seed path + all its guards + the transient-precondition raise the executor's bounded retry already handles); same-engine -> unchanged spawn_md_production. REVIEW-CAUGHT BUG (real, fixed): a stage's protocol defaults to 'production' (ChainStageRequest) but create_md_job rejects any protocol not in SUPPORTED_PROTOCOLS={mgh_slow_release,equilibrium_aware_namd} -> every DEFAULT oxDNA/mrDNA->NAMD stage would 400 -> retry x3 -> chain fails; fix maps a non-relaxation stage protocol onto EQUILIBRIUM_AWARE_PROTOCOL for the (relaxation-)create path (an explicit valid relaxation protocol is forwarded). (3) create_md_chain now accepts an oxDNA/mrDNA root (root_engine), validated via the SAME assert_namd_seed_available/assert_mrdna_namd_seed_available frame-on-disk check the launch card uses (FileNotFoundError->400), instead of _load_job (MdJob-only); the namd branch is byte-unchanged. Realizable cross-engine hop = a completed oxDNA/mrDNA ROOT -> NAMD stage 0 (the executor only spawns NAMD jobs; a mid-chain oxDNA/mrDNA STAGE would need an oxDNA/mrDNA spawn adapter -- out of P3 scope). ORACLE: 6 PURE (tests/test_md_job_pipeline.py) -- oxdna-root->oxdna_job_id + resolved-parent, mrdna-root->mrdna_job_id, same-engine->None, unsupported-sink raise, unknown-source raise, unresolved-parent raise. 7 ROUTE/CHAIN (tests/test_md_milestone1.py::TestMdCrossEngineChain, stubbed spawns) -- cross-engine uses create path w/ oxdna_job_id+field+anchors (NOT spawn_md_production), mrdna->mrdna_job_id, same-engine uses checkpoint path (no create), production-protocol REMAPPED to a valid relaxation protocol (RED-verified against the pre-fix forward-verbatim code -> 'Unknown protocol: production'), explicit relaxation protocol forwarded, and the E2E CHAIN: oxDNA root seeds NAMD stage 0 via reconstruct -> completed stage 0 chains same-engine NAMD stage 1 via checkpoint off the realised job -> chain completed; + halt-on-stage0-failure then resume re-runs ONLY stage 0 via a fresh reconstruct. Three-Layer clean (forces = job-request annotations; cross_engine_seed is pure; no Design/topology write). Fresh-context review: routing/validation/raise-conditions/all-or-nothing-invariant (every create_md_job guard precedes the single _spawn_prep_job that creates the job) all CONFIRMED; the one real bug (protocol) fixed + pinned. Gates: oracle 13 new fast (6 pure + 7 route), full just test green (see log/metrics), ruff clean on touched. No card/UI -> display-vs-oracle N/A (backend-only, like C1/C2/P1/P2). main.js LOC delta=0. SLOW real oxDNA->NAMD handoff (a live completed oxDNA job + real NAMD prep) NOT run -> owes MV-29 (precedent: P2's real 2-stage chain owed an MV). Substrate for P4 (the planner UI builds an MdPipeline whose cross-engine stages now execute). M-JOB-PLANNER now needs only P4."
    },
    {
      "id": "P4",
      "track": "job-planner",
      "engine": "all",
      "feature": "planner-ui",
      "title": "Planner UI (linear stage builder overlay): the 'Plan Run' popup - ordered stage list, per-stage engine+protocol+Forces editor (reuses U2's shared card), duplicate-stage for field sweeps, Queue; status vocabulary (stage N of M, queued-behind, partial-failure)",
      "deps": [
        "P2",
        "P3",
        "U2"
      ],
      "effort": "high",
      "leverage": "critical",
      "fast_slow": "fast+MV",
      "oracle": "pure stage-list model helpers (add/dup/reorder/remove, payload build) unit-tested; the built payload == a valid MdPipeline (P1); one-off display-vs-oracle Playwright (queued chain == payload) + MV row. main.js gains only imports + factory init + thin wiring.",
      "milestone": "M-JOB-PLANNER",
      "status": "pending",
      "notes": ""
    }
  ]
}